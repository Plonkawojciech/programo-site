# Programo Site — Progress Log

**Projekt:** Strona portfolio firmy Programo — studio software Wojciecha Płonki i Bartosza Kolaja, Poznań. PL/EN.

---

## Uwaga: 100% AI Codebase

> Ten projekt jest w **100% pisany i utrzymywany przez agenty AI** (Claude opus-4.6 / sonnet) za pomocą Claude Code. Nie ma kodu pisanego ręcznie przez człowieka.

## Instrukcja dla agentów AI

> **OBOWIĄZEK:** Jako agent AI edytujący ten codebase — **musisz** zaktualizować ten plik po wprowadzeniu zmian. Dodaj wpis do sekcji Changelog poniżej z:
> - datą (YYYY-MM-DD)
> - krótkim opisem co zostało zmienione i dlaczego
> - dotkniętymi plikami/modułami
>
> To jest główny mechanizm śledzenia historii projektu. Git log pokazuje commity, ale ten plik daje AI kontekst semantyczny — co, dlaczego, i jakie moduły były dotknięte. Bez tego kolejny agent nie ma pełnego obrazu stanu projektu.

---

## Changelog

<!-- Dodawaj wpisy od najnowszego do najstarszego -->

| Data | Opis zmian | Dotknięte moduły |
|------|-----------|-----------------|
| 2026-08-04 | **`/crm/analytics` — wewnętrzny czytnik zdarzeń first-party (nie dashboard z wykresami), na bazie warstwy analitycznej z poprzedniego commita.** Świadoma decyzja architektoniczna z briefu: przy skali kilkuset sesji/mies. wykres udaje trend, więc strona to **lista sesji do czytania wierszami**, nie agregaty w słupkach. Auth, `noindex`, `force-dynamic` — dokładnie ten sam wzorzec co `/crm` (powielony z `crm-auth.ts`/`page.tsx`, nie wymyślony na nowo). **Nowy plik `src/lib/analytics/query.ts`** (jedyne dozwolone miejsce na nową logikę odczytu, reszta `src/lib/analytics/**` tylko czytana) — czyste funkcje agregujące (`buildSessions`, `buildFormFunnel`, `buildQualitySignals`, `buildConsentSummary`, `buildAiCrawlerSummary`) + `getAnalyticsDashboardData()` jako jedyna funkcja dotykająca Redis. Rozdział celowy: agregacja testowalna bez żywego store'u. **`buildSessions` grupuje `EventBatch[]` (do 800, `getEventBatches(800)`) po `session_id`** — jedna sesja to zwykle kilka paczek (klient flushuje co ~3s), zdarzenia spłaszczone i posortowane po `ts` klienta. Lejek formularza liczony **na sesjach, nie zdarzeniach**: `abandonmentRate = 1 - sesje z generate_lead / sesje z form_start` (zgodnie z komentarzem w `events.ts:161` — `form_abandon` to wyłącznie diagnostyka, niedostarczalna niezawodnie). **Zero bibliotek do wykresów** — rozkład Web Vitals to plaski div z trzema segmentami `width:%` (emerald/amber/red), zero SVG/canvas. **Cała strona serwerowa, zero client JS** — rozwijanie wiersza sesji przez natywny `<details>/<summary>`, nie `useState`; strona nie miała żadnej innej interaktywności (brak wyszukiwarki, w przeciwieństwie do `/crm`), więc hydracja byłaby czystym kosztem bez zysku. **Błąd znaleziony i naprawiony PRZED commitem, nie po:** pierwsza wersja `groupClicks` (rage/dead click) łączyła `section`+`element` przez `` `${section} ${element}` `` i rozbijała `.split(" ")` z powrotem na dwa pola — `element` to opis DOM-u z tekstem (`describe()` w `engagement.ts:26-39`), więc regularnie zawiera spacje (`button.cta"Zamów wycenę"`), co **obcinałoby element na pierwszej spacji**. Przepisane na zagnieżdżoną `Map<section, Map<element, count>>` — brak wymyślonego separatora, brak możliwości kolizji. Zweryfikowane osobnym testem regresyjnym (patrz niżej) i realnym POST-em z cudzysłowem+spacjami w `element`, który przeszedł przez cały potok bez utraty ani znaku. **Krajobraz równoległej pracy:** `src/lib/analytics/**` miało w trakcie sesji świeże, nieukończone pliki (`ai-crawlers.ts`, `consent-cookie.ts`, `src/proxy.ts`) pisane przez inną sesję bezpośrednio w głównym repo (nie w tym worktree) — zgodnie z briefem odczytywane wyłącznie do orientacji, nietknięte; `consent_update`, `form_field_skip`, `session_summary` są zadeklarowane w `events.ts`, ale **nigdzie jeszcze nie odpalane** (grep po całym `src/` — zero wystąpień poza definicją), więc sekcja Zgody i kolumna „pominięcia” w lejku będą puste do czasu, aż te zdarzenia zostaną podłączone gdzie indziej; strona renderuje to jako czytelny stan pusty, nie błąd. **Landmina środowiskowa (worktree), nie mój kod — naprawiona w `next.config.ts`:** `npm run build` padał na `Module not found: '@/lib/analytics/ai-crawlers'` w `src/proxy.ts` — plik, którego **w tym worktree w ogóle nie ma** (`git ls-files | grep proxy` → puste). Przyczyna: Turbopack wykrył DWA `package-lock.json` (worktree + główne repo) i błędnie wybrał główne repo jako "workspace root" (patrz ostrzeżenie w outpucie builda), przez co resolvował `src/proxy.ts` z **sąsiedniego, żywego checkoutu**, gdzie inna sesja właśnie dopisywała AI-crawler middleware. Potwierdzone empirycznie: skopiowanie prawdziwego `node_modules` (punkt 6b instrukcji buildera) NIE naprawiło błędu, dopiero `turbopack: { root: process.cwd() }` w `next.config.ts` (rekomendacja z własnego ostrzeżenia Turbopacka) — po zmianie build zielony, 34 trasy, `/crm/analytics` jako `ƒ dynamic`. Zmiana lokalna dla tego worktree (`process.cwd()`, nie zaszyta ścieżka), więc bezpieczna też po zmergowaniu do main. **Testy — 29 nowych (107→136):** `src/__tests__/analytics-query.test.ts` (19 testów na czystych funkcjach z `query.ts`, w tym jawny regression guard na błąd `groupClicks` opisany wyżej — element z cudzysłowem i spacją musi zostać nietknięty) + `src/__tests__/components/analytics-dashboard.test.tsx` (9 testów renderujących `AnalyticsDashboard` z wstrzykniętymi danymi: stan „brak Redisa", pełna sesja z odczytywalną ścieżką zdarzeń, lejek z realnym wskaźnikiem porzucenia liczonym na sesjach, sygnały jakości, crawlery AI, zgody). **Weryfikacja na żywo, nie tylko bramki:** `npm run dev` na porcie 4021 (3000 zajęty), `CRM_PASSWORD` tymczasowe tylko lokalnie; realne logowanie przez `/api/crm-login` (nie obejście) → stan „Magazyn zdarzeń nieskonfigurowany" na żywo w przeglądarce (brak `.env.local` w tym worktree = brak Upstash lokalnie, zgodnie z oczekiwaniem briefu). Jednorazowy skrypt (`/private/tmp/.../scratchpad/verify-collect.mjs`) POST-ował 5 paczek na `/api/collect` symulujących sesję z leadem, porzucony formularz z rage_click, sesję z dead_click+js_error+web_vitals, sesję AI-referral i jedną z nieznaną nazwą zdarzenia — log serwera potwierdza **4× przyjęte (204, bez ostrzeżenia) + 1× `[collect] rejected payload: unknown event name`** (schemat `collect-schema.ts` faktycznie odrzuca nazwy spoza taksonomii). Pełny render z realnymi danymi zweryfikowany **tymczasową podmianą danych w `page.tsx`** (cofniętą przed commitem — `git status` po cofnięciu pokazuje `page.tsx` jako część nowego, nietkniętego katalogu) + `read_page`/screenshot w przeglądarce: 3 sesje, poprawne odczytywalne ścieżki zdarzeń, poprawna matematyka lejka (67%/38%/63%), sygnały jakości z poprawnie zgrupowanym `element` zawierającym cudzysłów i spację, słupki Web Vitals, 3 boty AI z rozbiciem na ścieżki, karty zgód. `curl .../sitemap.xml \| grep -c crm` → **0**. Bramki końcowe: `tsc` 0, `build` 0 (34 trasy), `lint` 0, `vitest` **136/136** (107 zastanych + 29 nowych). **Poza zakresem, celowo nienaprawione:** `useFormAnalytics` (hak z `use-form-analytics.ts`) nie jest podłączony do `compact-lead-form.tsx`/`quick-contact.tsx` — bez tego `form_view`/`form_start`/`form_field_complete`/`form_error`/`form_abandon` nigdy nie polecą z prawdziwego ruchu; to osobne zadanie (komponenty formularzy nie są w moim zakresie ani w liście zablokowanych plików, ale podłączenie hooka to inna, większa zmiana). `consent_update` i `session_summary` również niepodłączone nigdzie w kodzie. | `src/lib/analytics/query.ts` (nowy), `src/app/crm/analytics/page.tsx` (nowy), `src/app/crm/analytics/AnalyticsDashboard.tsx` (nowy), `src/__tests__/analytics-query.test.ts` (nowy), `src/__tests__/components/analytics-dashboard.test.tsx` (nowy), `next.config.ts` |
| 2026-08-03 | **Audyt polityki prywatności względem RODO art. 13 - uzupełnienie pięciu braków w obowiązku informacyjnym (PL + EN).** Właściciel dał mandat: „rob wszystko co uwazasz za sluszne i dobre i poprawne z prawem" (przy okazji przesądzając, że podmiana `s.c.` → `s.j.` to korekta redakcyjna, więc sama w sobie nie wymagała bumpu daty). Polityka miała 7 sekcji i pokrywała administratora, cele, podstawy prawne, retencję i większość praw - ale **milczała o rzeczach, które faktycznie się dzieją w kodzie**. Braki ustalone przez czytanie kodu, nie zgadywanie: `consent.tsx:23,68-87,107-109` (Clarity), `tracking.ts:183-198` (Enhanced Conversions), `api/contact/route.ts:236-318` (Resend + Telegram). **Naprawione (5 braków × 2 języki):** (1) **Microsoft Clarity w ogóle nie był ujawniony** - narzędzie nagrywa ruchy kursora, kliknięcia, przewijanie i odtwarza sesję, więc jest istotnie bardziej inwazyjne niż GA4, a §5 wymieniał tylko Vercel/Resend/Telegram/GA4; art. 13 ust. 1 lit. e. (2) **Enhanced Conversions nieujawnione** - po zgodzie marketingowej do Google leci zahaszowany SHA-256 e-mail i telefon z formularza; to przekazanie danych kontaktowych, nie samo cookie, i wymagało nazwania wprost. (3) **Brak informacji o transferze poza EOG** (art. 13 ust. 1 lit. f + rozdział V) - wszyscy dostawcy są amerykańscy; dodana nowa sekcja 6 z podstawą (SCC albo EU-US Data Privacy Framework, zależnie od dostawcy) i ofertą wydania kopii zabezpieczeń. Świadomie **nie** przypisano konkretnej podstawy konkretnemu dostawcy - tego nie da się zweryfikować z kodu, a fałszywa precyzja w dokumencie prawnym jest gorsza niż uczciwe „zależnie od dostawcy". (4) **Brak prawa do przenoszenia danych** (art. 20) w liście praw - było 5 z 6. (5) **Brak oświadczenia o profilowaniu** (art. 13 ust. 2 lit. f), mimo że §3 sam wymieniał remarketing. Dodano też **dobrowolność podania danych + konsekwencję jej braku** (art. 13 ust. 2 lit. e). §5 przepisane z jednego zdania na listę per-dostawca z rolą i bramką zgody, bo sześciu odbiorców w jednym zdaniu przestało być czytelne. Numeracja przesunięta 6→7, 7→8. **Data „ostatnia aktualizacja" 2026-05 → 2026-08** - tu bump jest uzasadniony, bo to zmiana merytoryczna zakresu przetwarzania, a nie korekta nazwy. **Strefa nietykalna nietknięta** - `consent.tsx` i `tracking.ts` otwierane wyłącznie do odczytu, żeby opisać przetwarzanie zgodnie ze stanem faktycznym; zero zmian w GA4, Clarity, Consent Mode v2, gtag, JSON-LD. **NIEROZWIĄZANE, wymaga danych od właściciela:** obowiązek informacyjny z **art. 5 uśude** - skan `src/` po `NIP|KRS|REGON|ul\.` daje zero trafień dla Programo (jedyne trafienia to opisy projektu eProfil, który sam przeszukuje KRS). Adres siedziby, NIP i KRS spółki jawnej są wymagane na stronie usługodawcy i **nie da się ich wymyślić**. Osobno: polityka §1 pisze „Bartosza Kolaja", stopka „Bartosz Kołaj" - w dokumencie prawnym rozjazd w nazwisku waży więcej niż w copy, ale imienia i nazwiska też nie zgaduję. Bramki: `tsc` 0, `vitest` **107/107**, `lint` 0 błędów (135 warningów wyłącznie w nieśledzonym `.claude/skills/`), `build` 0. **Weryfikacja w przeglądarce, oba języki** (uwaga: port 3000 współdzielony - `digital-dent-shop` zajął `[::1]:3000`, programo słucha na `127.0.0.1:3000`): PL - 8 sekcji w kolejności, `2026-08`, obecne „Microsoft Clarity", „SHA-256", „poza Europejskim Obszarem Gospodarczym", „art. 20 RODO", „Podanie danych jest dobrowolne"; 0 pustych węzłów, 0 trafień `s.c.`, 0 trafień „Programo Studio" jako administratora. EN po przełączeniu - te same 8 sekcji, `Transfers outside the EEA`, `Art. 20 GDPR`, `solely on automated processing`, 0 pustych węzłów. Konsola: wyłącznie wcześniej istniejący mismatch hydracji na `data-theme`. | `src/app/polityka-prywatnosci/PrivacyPageClient.tsx` |
| 2026-08-03 | **Korekta formy prawnej w całym serwisie: „spółka cywilna / s.c." → „spółka jawna / s.j.", plus ujednolicenie administratora danych w RODO.** Właściciel potwierdził: „tak mamy spolke jawna a nie cywilna". Poprzednia tura zgłosiła rozjazd (PL `footer.companyName` zmienione ręcznie na `s.j.`, reszta serwisu nadal `s.c.`) i wstrzymała się z podmianą, bo to nie było usunięcie tekstu, a nazwa administratora danych w zgodzie RODO to oświadczenie prawne - nie zmienia się go z własnej inicjatywy. Po potwierdzeniu podmiana wykonana. **Zakres szerszy, niż wynikało z samego grepa po `s.c.`** - zrzut ekranu `/o-nas` pokazał, że stara forma siedziała też w **polskiej prozie**: „Programo to **spółka cywilna** Wojciecha Płonki...", dwie sekcje nad stopką mówiącą „Programo **s.j.**". Strona przeczyła sama sobie po polsku, nie tylko między językami. **Zmienione (9 miejsc / 6 plików):** `about.ts` - intro PL (proza) i linia danych firmy EN; `common.ts` - `footer.companyName` EN; `forms.ts` - **cztery klauzule RODO** (`compact.consentLabel` i `quick.consentLabel`, PL i EN); `app/o-nas/page.tsx` - description i OG description; `CLAUDE.md` - nagłówek projektu (żywa instrukcja dla agentów, więc zamyka wektor regresji na przyszłość). **Trzecia nazwa administratora, znaleziona przy okazji i naprawiona.** Polityka prywatności §1 nie mówiła ani `s.c.`, ani `s.j.` - identyfikowała administratora jako „**Programo Studio** z siedzibą w Poznaniu". Serwis miał więc **trzy różne nazwy administratora danych**, przy czym checkbox zgody („Administratorem danych jest Programo s.j.") **linkuje bezpośrednio do tej właśnie polityki**, która nazywała inny podmiot. RODO art. 13 wymaga tożsamości administratora, a „Programo Studio" to nazwa handlowa, nie podmiot. §1 zrównane do `Programo s.j.` w PL i EN (przy okazji `prowadzone` → `prowadzona`, uzgodnienie rodzaju ze „spółką"). **Świadomie NIE zmienione:** „Programo Studio" jako nazwa **marketingowa** - `main.intro.label`, `alternateName` w JSON-LD, meta description polityki i zdanie wprowadzające polityki, które definiuje skrót „dalej «Programo»". To legalne użycie nazwy handlowej; poprawiona została tylko formalna identyfikacja administratora. EN `about.intro` mówi „the partnership of Wojciech Płonka and Bartosz Kolaj" - **poprawne dla spółki jawnej** (general partnership), więc bez zmian; przepisywanie tego byłoby redagowaniem copy poza zleceniem. **Testy przeszły bez jednej poprawki - 107/107.** To walidacja decyzji z poprzedniej tury: `footer.test.tsx` i `about.test.tsx` miały zaszyte literały `"Programo s.c."`, przeniesione wtedy na `translations[...]`. Gdyby zostały literały, ta zmiana formy prawnej wywróciłaby trzy testy renderowania, które z formą prawną nie mają nic wspólnego. **Weryfikacja w przeglądarce, oba języki:** `/o-nas` - intro „spółka jawna", linia firmy `Programo s.j., Poznań`; `/polityka-prywatnosci` §1 - `Programo s.j. z siedzibą w Poznaniu`; `/kontakt` - **oba** checkboxy zgody nazywają `Programo s.j.` i linkują do `/polityka-prywatnosci`, czyli zgoda i polityka wskazują wreszcie ten sam podmiot; po przełączeniu na EN to samo (`Programo s.j. is the data controller`). Skan `s.c.` w `src/` po zmianie: **0 trafień**, w obu językach w runtime też 0. Bramki: `tsc` 0, `vitest` **107/107**, `lint` 0 błędów (135 warningów wyłącznie w nieśledzonym `.claude/skills/`), `build` 0. **Nie ruszone:** `docs/content-deck-2026-07.md` i `docs/redesign-brief-2026-07.md` - datowane artefakty historyczne, przepisywanie ich fałszowałoby zapis decyzji; regresję zamyka aktualizacja `CLAUDE.md`, którą agenci ładują co sesję. **Strefa nietykalna (GA4, Clarity, Consent Mode v2, gtag, JSON-LD, `cookie-banner`, `analytics-tracker`, `tracking.ts`) bez zmian** - JSON-LD sprawdzone, nie zawiera `legalName` ani formy prawnej, więc nie było czego poprawiać. | `src/lib/i18n/dictionaries/{about,common,forms}.ts`, `src/app/o-nas/page.tsx`, `src/app/polityka-prywatnosci/PrivacyPageClient.tsx`, `CLAUDE.md` |
| 2026-08-03 | **Zrównanie EN z PL po redakcji właściciela + zabezpieczenie renderu wyczyszczonych stringów; wypchnięcie wersji z localhosta na produkcję.** Zgłoszenie: „poprawiłem teksty, możesz tę wersję z localhosta wrzucać, jedyne co to zobacz które teksty usunąłem z polskiej wersji i usuń te z angielskiego (...) porównuj względem polskiej wersji, żebyś nie robił od nowa tych napisów”. Właściciel czyścił copy przez edytor wizualny na localhoście, a edytor zapisuje **tylko język, w którym się edytuje**, więc powstał rozjazd: 12 kluczy pustych po polsku, z pełną treścią po angielsku - czyli **dziura dla jednej publiczności i widoczny string dla drugiej**. **Inwentaryzacja skryptem, nie na oko:** parser po wszystkich słownikach zwrócił trójki klucz/PL/EN i wypisał dokładnie te, gdzie PL jest puste, a EN nie. EN wyzerowane dla **12 kluczy**: `about.wojciech.desc`, `about.bartosz.desc`, `footer.tagline`, `footer.reply`, `compact.directContact`, `home.hero.showcaseCaption`, `home.process.terms.1`, `founders.wojtek.focus`, `founders.bartek.focus`, `home.team.wojtek.desc`, `home.team.bartek.desc`, `offer.pillar4.title`. Podmiana szła regexem z asercją „dokładnie jedno trafienie” na klucz, więc żaden nie został trafiony przypadkiem ani pominięty. **Żaden tekst nie został napisany od nowa** - wyłącznie kasowanie, zgodnie z poleceniem. **Kasowanie stringa to nie to samo co usunięcie elementu - siedem miejsc renderu zabezpieczonych.** Pusty string nadal renderuje znacznik, więc bez tego na produkcję poszłoby: **[P0/WCAG 2.4.4]** `<a href="/strony-tracking-reklamy">` **bez tekstu** w stopce - link bez dostępnej nazwy, niewidoczny dla widzących i błąd audytu (`footer.tsx`, filtr `offerLinks` **przed** `.map`); **[P1]** pusty `<h2>` na `/oferta` - zajmuje linię 36-40px i psuje konspekt nagłówków dla czytników ekranu (`offer.tsx`); **[P1]** pusty `<li>` w pasku warunków na stronie głównej - separator `/` jest liczony z indeksu, więc wyczyszczona pierwsza pozycja przepychała **wiodący ukośnik** na następną (`process.tsx`, filtr przed mapowaniem, nie w środku); **[P1]** trzy `border-t` wiszące nad niczym (`compact-lead-form.tsx`, `marketing-tracking.tsx`, stopka); **[P2]** zielona kropka statusu bez etykiety, czyli kolorowa plamka bez znaczenia - opakowana **razem** z tekstem; oraz puste `<p>`, które i tak niosą swój `margin-top` (stopka, `founders.tsx`, `about.tsx`). **Rytm sekcji „Kto to robi” uzależniony od treści, nie zaszyty.** Odstęp `gap-16 md:gap-20` był dobrany pod bloki założycieli z linią roli i biogramem (~150px); po wyczyszczeniu obu blok zwija się do **jednej linii 41px**, a 80px odstępu przestaje być rytmem i staje się dziurą (zmierzone w przeglądarce). Jeden boolean `hasBios` przełącza na `gap-6 md:gap-8` - **80px → 32px** - i wraca do pierwotnego rytmu, jeśli właściciel przywróci biogramy. **Testy przepisane na regułę, nie na literał.** `i18n.test.ts` zabraniał **jakiegokolwiek** pustego stringa, a pusty string jest teraz legalnym stanem - właściciel czyści copy, gdy linia ma przestać się renderować, a klucz musi zostać, żeby edytor dalej go znajdował. Zamiast tego **parzystość**: „wyczyszczone jest wyczyszczone w obu językach” - bo to jest niedopuszczalny stan, nie pustka sama w sobie. Test „nested/dotted keys” sprawdza teraz **rozstrzygalność** (typ `string`), a nie treść. `footer.test.tsx` i `about.test.tsx` miały zaszyte literały copy („Programo s.c.”, „Odpowiadamy w 24 h”) - asercje przeniesione na `translations[...]`, bo testy nazywają się „renderuje linię danych firmy”, więc pinowanie brzmienia sprawiało, że **każda korekta copy łamała test renderowania**. Test promisu 24h zamieniony na realny niezmiennik: **kropka statusu i etykieta pojawiają się i znikają razem**. **Weryfikacja w przeglądarce, nie tylko bramkami:** `/`, `/oferta`, `/o-nas` przeskanowane pod kątem pustych `h1/h2/h3/p/li/a` - **0 na każdej**; pasek warunków renderuje 3 pozycje bez wiodącego `/`; stopka bez kropki statusu i bez pustych linków. Bramki: `tsc` 0, `vitest` **107/107**, `lint` 0 błędów (135 warningów wyłącznie w nieśledzonym `.claude/skills/impeccable/scripts/`), `build` 0. **⚠️ Do decyzji właściciela - dwie rzeczy świadomie NIE naprawione:** **(1) `offer.pillar4.title` wygląda na wyczyszczony przez przypadek, nie celowo** - tytuł filaru jest pusty, ale jego opis, przykład i **wszystkie 5 punktów zostały nietknięte**, czego nie robi się przy świadomym usuwaniu sekcji. Skutek: filar „04” na `/oferta` ma pełny opis bez nagłówka, a **stopka straciła jedyne wejście do `/strony-tracking-reklamy`** (kolumna Oferta ma teraz 3 linki zamiast 4). Zrównałem EN zgodnie z poleceniem, ale wystarczy wpisać tytuł z powrotem w edytorze i wszystko wraca. **(2) forma prawna rozjechana: PL mówi „Programo s.j.”, EN nadal „Programo s.c.”** Właściciel zmienił `footer.companyName` i `about.company.line` **tylko po polsku**, ale `s.c.` zostało jeszcze w **klauzuli RODO o administratorze danych** (`forms.ts`, dwa miejsca, PL i EN) oraz w metadanych SEO `/o-nas`. To nie jest usunięcie tekstu, więc wykracza poza zlecenie, a **nazwa administratora danych w zgodzie RODO to oświadczenie prawne** - nie zmieniam jej z własnej inicjatywy. Do potwierdzenia i jednorazowej podmiany w całym repo. **Strefa nietykalna (GA4, Clarity, Consent Mode v2, gtag, JSON-LD, `cookie-banner`, `analytics-tracker`, `tracking.ts`) nie została ruszona.** | `src/lib/i18n/dictionaries/{about,common,forms,home,offer}.ts`, `src/components/{footer,offer,about,marketing-tracking,compact-lead-form}.tsx`, `src/components/home/{process,founders}.tsx`, `src/__tests__/i18n.test.ts`, `src/__tests__/components/{footer,about}.test.tsx` |
| 2026-08-03 | **Mockup czterech realizacji na górze strony głównej (prośba właściciela) - tylko localhost.** Zgłoszenie: „użyj tego zdjęcia aby wrzucić to na górę strony tam gdzie było W. Safe Finance (...) dopasuj to zdjęcie aby wyglądało dobrze, są to nasze realizacje na które od razu będzie można sobie rzucić okiem jak ktoś wejdzie na stronę bez schodzenia do portfolio". Miejsce po usuniętym screenshocie WSafeFinanse (wpis wyżej, ten sam dzień) dostaje **jeden kadr z czterema wdrożeniami**: sklep Jedmar na laptopie, W. Safe Finance na laptopie, CRM Estalo na tablecie, aplikacja Jedmar na telefonie. Jeden klient to słabszy dowód niż czterech. **Asset:** PNG 1672x941 / 1,8 MB przekonwertowany przez `sharp` (`webp({quality:86, effort:6})`) do `public/screenshots/realizacje-hero.webp` - **113 KB**, czyli 6% wagi oryginału. `priority`, bo to nadal powierzchnia LCP. **Layout: świadomie NIE wracamy do dwóch kolumn.** Kuszące było przywrócić `lg:grid-cols-[1.1fr_0.9fr]` i wstawić obraz w prawą kolumnę, ale przy `text-display` (84px) nagłówek potrzebuje ~890px żeby zostać na trzech wierszach - podział na dwie kolumny wypycha go na pięć, a cztery urządzenia wciśnięte w kolumnę ~600px są za małe, żeby czytać się jak prawdziwa praca. Zamiast tego **pełna szerokość pod tekstem i formularzem**, wewnątrz kontenera, poza `max-w-4xl` bloku tekstowego. Pomiary zapisane w komentarzu przy `figure`, żeby kolejny agent tego nie cofnął. **Mobile: full-bleed.** W gutterze 24px obraz wychodził 327px, czyli ~90px na urządzenie - rozmazana plama na ekranie, z którego przychodzi większość tej grupy docelowej. `-mx-6 w-[calc(100%+3rem)] max-w-none md:mx-0 md:w-full md:rounded-2xl` daje 375px od krawędzi do krawędzi (+15%), zaokrąglenie wraca od `md`. **Świadomie `calc(100%+3rem)`, nie `w-screen`:** 100vw liczy klasyczny pasek przewijania, którego `clientWidth` nie liczy, więc na Windows/Linux Chrome `w-screen` wylewa się poza ekran o ~15px; headless (overlay scrollbars) tego nie odtworzy, więc byłby to błąd wykryty dopiero u klienta. Procent mierzy kontener - to jest poprawna miara. **`figcaption` warunkowy** (`t(...).trim() && ...`): podpis jest edytowalny z edytora localhost, a wyczyszczony zostawiłby wiszącą kreskę bez tekstu. **Weryfikacja w przeglądarce, nie na oko:** 375 / 700 / 768 / 1440, light i dark - `document.documentElement.scrollWidth <= clientWidth` na każdym (zero poziomego przelewu), formularz nad foldem na 375, nagłówek trzy wiersze na 1440, obraz `complete: true`, `next/image` serwuje wariant `w=828`. Bramki: `tsc` 0, `lint` 0 błędów (135 warningów wyłącznie w nieśledzonym `.claude/skills/impeccable/scripts/`), `build` 0. **`vitest` 105/107 - dwa czerwone NIE z tej zmiany:** to puste stringi PL, które właściciel zostawił w edytorze localhost (patrz sekcja „Do decyzji właściciela"). | `src/components/home/hero.tsx`, `public/screenshots/realizacje-hero.webp` (nowy), `src/lib/i18n/dictionaries/home.ts` |
| 2026-08-03 | **Usunięcie screenshotu WSafeFinanse z hero strony głównej (prośba właściciela) - tylko localhost, bez wypychania na produkcję.** Zgłoszenie: „usuń z tego miejsca ten projekt na razie i zostaw go w realizacjach niżej, na razie rób wszystko na localhoście i później zmienimy to na główną stronę wszystko za jednym zamachem". Hero niosło screenshot WSafeFinanse jako dowód (`figure` z podpisem „strona z lejkiem leadów"), a to nie jest projekt, którym firma chce otwierać stronę - zgodnie z wcześniejszą decyzją z tej samej gałęzi WSafeFinanse ma iść **na końcu**. Case study **zostaje** w `client-work.tsx`, nadal na trzeciej pozycji, i logotyp zostaje w pasku zaufania - usunięty jest wyłącznie wizual z góry strony. **Layout: siatka dwukolumnowa zwinięta do jednej kolumny.** Hero było `lg:grid-cols-[1.1fr_0.9fr]` z tekstem po lewej i zdjęciem po prawej; po usunięciu prawej kolumny siatka nie ma czego trzymać, więc zeszła do zwykłego kontenera. Kolumna tekstu poszerzona `max-w-2xl` → **`max-w-4xl`**, bo przy `text-display` (84px na desktopie) nagłówek łamie się na **cztery** wiersze poniżej ~890px, a na trzy powyżej - zmierzone w przeglądarce, nie oszacowane. Formularz osobno **przycięty do `max-w-2xl`**: jedno pole telefonu rozciągnięte na 896px czyta się jak niedokończony layout. **Martwy kod skasowany, nie zostawiony na wszelki wypadek:** import `next/image` (jedyne użycie), reguła `.hero-figure-settle` z `@keyframes heroFigureSettle` i jej wariantem `prefers-reduced-motion` w `globals.css` (27 linii, jedyne użycie), oraz klucze `home.hero.shotAlt` i `home.hero.shotCaption` w słowniku (PL i EN). Wszystko wraca jednym `git revert`, jeśli właściciel zmieni zdanie. **Komentarz nagłówkowy pliku przepisany** - opisywał układ dwukolumnowy i „screenshot proof", więc bez aktualizacji kolejny agent odtworzyłby usunięty blok; teraz notuje wprost, kto i dlaczego go zdjął. Uwaga o LCP przeredagowana: hero nie ma już żadnej animacji, a nie „jedną na screenshocie". **Efekt uboczny na plus:** hero straciło `<Image priority>`, więc kandydatem na LCP jest teraz nagłówek renderowany w SSR-owym HTML-u, a na mobile (375×812) nagłówek, lead i formularz mieszczą się nad zgięciem - wcześniej screenshot spychał formularz w dół. **Weryfikacja:** serwerowy HTML strony głównej bez `hero-figure-settle` i bez podpisu; sekcja `#realizacje` nadal renderuje trzy case studies z `wsafefinanse-hero.webp` i linkami `/projects/wsafefinanse`; brak przepełnienia poziomego przy 1440px (`scrollWidth` 1425); zrzuty przy 375, 879 i 1280. Bramki: `tsc` 0, `vitest` 107/107, `lint` 0 błędów, `build` 0. **Świadomie NIE wypchnięte na produkcję** - właściciel prosił o batch na później. **Strefa nietykalna nie ruszona.** | `src/components/home/hero.tsx`, `src/app/globals.css`, `src/lib/i18n/dictionaries/home.ts` |
| 2026-08-03 | **Naprawa generowania `description` na stronach projektów - znalezione przy weryfikacji w przeglądarce po redakcji copy.** Meta description stron `/projects/*` jest **wyprowadzany z `description.pl`**, nie pisany ręcznie, a wyprowadzenie miało dwie wady, których żadna bramka nie widziała (`tsc`, `lint`, `vitest`, `build` przechodziły na zielono, bo defekt jest w **treści wyniku**, nie w typach). **[P0] Inicjał w nazwie klienta kończył zdanie.** Podział `split(/(?<=\.)\s/)` traktuje „kropka + spacja" jako koniec zdania, a klient nazywa się **W. Safe Finance** - opis strony tego projektu szedł do Google jako **39-znakowy ogryzek „Strona firmowa doradztwa finansowego W."**, urwany w połowie nazwy firmy. Podział ma teraz zagnieżdżony lookbehind `(?<!\b[A-ZĄĆĘŁŃÓŚŹŻ])`, więc pojedyncza wielka litera przed kropką nie kończy zdania. 39 → **136 znaków**. **[P1] Jedno zdanie to za mało na opis.** Brane było wyłącznie pierwsze zdanie, więc `domki-poznaniak` miał 36 znaków, a `rejestr-pro` 63 - poniżej progu, od którego Google w ogóle używa naszego opisu zamiast własnego streszczenia. Teraz dokładane są **całe kolejne zdania**, dopóki mieszczą się w 160 znakach; jeśli kolejne nie mieści się w całości, opis kończy się na poprzednim. Świadomie **nie** doklejamy urwanego zdania - krótki, ale skończony opis bije dłuższy urwany na przyimku. **[P1] Pierwsze zdanie dłuższe niż 160 znaków cięte w połowie słowa** - `jedmar` kończył się na „6 integrac", `estalo` na „w skali 0-100, ge", `solvio` na „wyd". Cięcie przesunięte na granicę słowa z wielokropkiem. **Wynik na wszystkich 10 projektach:** 60-159 znaków, każdy kończy się kropką albo wielokropkiem, żaden nie urywa się na inicjale, `og:description` zgodny z `description`. **Testy:** wyprowadzenie w `seo.test.ts` jest **kopią** logiki ze strony, więc zostało zmienione razem z nią; dołożone dwa strażniki - „nie kończy się na inicjale" i „nie kończy się w połowie słowa". Guard opisuje **regułę**, nie ten jeden string, więc kolejny klient z inicjałem w nazwie jest bezpieczny. Suite 105 → **107 testów**. Bramki: `tsc` 0, `vitest` 107/107, `lint` 0 błędów, `build` 0; wyniki odczytane z **serwerowego HTML** wszystkich 10 tras, nie z DOM. **⚠️ Do decyzji właściciela: pięć `<title>` przekracza limit SERP-a** (jedmar 95 znaków, wsafefinanse 91, wks-poznan 83, eportal-prawny 67, pooltimer 65). Tytuł składa się jako `${title} - ${subtitle} | Programo`, a skracanie `subtitle` odpala się **tylko** na dwukropku albo spacjowanym myślniku - te pięć podtytułów nie ma żadnego z nich, więc nie ma czego skrócić. Automatyczne przycięcie dałoby „...z interaktywnymi sche", więc naprawą jest **skrócenie samych podtytułów**, a te renderują się na stronie projektu jako widoczny tekst - to zmiana copy, nie mechanizmu, więc czeka na decyzję. | `src/app/projects/[slug]/page.tsx`, `src/__tests__/seo.test.ts` |
| 2026-08-03 | **Cały serwis przepisany „po ludzku" - usunięcie śladów pisania przez AI + długie myślniki na zwykłe (prośba właściciela).** Zgłoszenie wprost: „zrób wszystko po ludzku, żeby nie było widać, że AI pisało tę stronę, usuń te długie myślniki na zwykłe". Wpis konsoliduje trzy równoległe wątki (dwa landingi SEO, dwa landingi Ads, słowniki i18n + `projects.ts`), bo to jedna zmiana redakcyjna, nie trzy. **Podmiana myślnika była skanem, nie sed-em.** W repo było **764 wystąpień `—`, ale tylko 324 w tekście widocznym dla użytkownika** - pozostałe 440 siedzi w komentarzach kodu, gdzie podmiana to czysty szum w diffie. Skaner (`/tmp/scan.mjs`) rozdziela jedno od drugiego, podmieniono **315 wystąpień** i tylko w stringach/tekście; komentarze zostawione nietknięte. Weryfikacja końcowa: `in-code/text 0`. **[P0] Jedna zależność złapana ZANIM podmiana ją zepsuła.** `src/app/projects/[slug]/page.tsx` skracał `<title>` przez `project.subtitle.pl.split(/[—–:]/)` - regex rozpoznawał **wyłącznie długi myślnik**. Po globalnej podmianie na `-` każdy z 10 tytułów projektów przestałby się skracać i cicho przekroczyłby limit SERP-a, bez żadnego błędu w bramkach. Regex przepisany na `/:|\s[-\u2013\u2014]\s/` (spacjowany dywiz **albo** półpauza **albo** pauza) **przed** podmianą, więc skracanie działa i dla starych, i dla nowych danych. **Redakcja - co wycięte:** triady przymiotników („szybkie, dostępne i bezpieczne"), kontrast binarny z wyimaginowanym przeciwnikiem („Nie jesteśmy outsourcingową fabryką", „Nie demo, nie koncepty"), objawienie po dwukropku („Zaczynamy od MVP: prawdziwych użytkowników, nie slajdów", „Krótko: koszt zależy od trzech rzeczy"), równania („Szybsza strona = wyższe pozycje"), doklejane zapewnienia („bez ukrytych kosztów", „Bez zobowiązań", „indywidualnie" - powtarzane po 3-4 razy na stronę, zostało po jednym wystąpieniu na plik), pusta wata („realny", „nowoczesny", „kompleksowo", „dedykowany", „w pełni"), domykające podsumowania sekcji, importance puffery („Najlepszy dowód, że...", „Ten projekt pokazuje...") oraz myślnik jako rytm zdania. **Zróżnicowany rytm kart:** opisy w `services`/`projects`/`factors` miały identyczną długość i budowę (zdanie + doklejone podsumowanie); teraz część jest krótka i bez domknięcia, część dłuższa i opisowa. **Złamane szablony powtórzone między plikami:** `about.title` „Dwóch inżynierów, jeden standard pracy" i `founders.title` „Dwie osoby, cała odpowiedzialność" to była ta sama konstrukcja „X, jeden Y" w dwóch miejscach - rozjechane na „Dwóch inżynierów, którzy robią to sami" i „Kto to robi". **123 martwe klucze i18n pominięte** - agenci dostali skrypt-detektor (klucze ze słowników × `grep` po całym `src/` poza słownikami), nie listę, więc nikt nie przepalił redakcji na copy, którego nikt nie widzi. **Fakty nietknięte:** wszystkie liczby, terminy, numery telefonów, nazwy klientów i integracji (Otodom, NOE 2.0, Domy.pl, KRS/MS, Stripe, Przelewy24, PrestaShop, Lemon Squeezy, PayU/PayPo/iMoje/InPost Pay, Allegro/BaseLinker), stack, nazwy produktów, wszystkie disclaimery `scope` w `projects.ts` (Jedmar, Domki Poznaniak, Skup Nieruchomości) i kredyt partnerski PBDevs w Solvio przepisane 1:1. Teksty RODO, zgód i walidacji **w ogóle nieruszane**. `ile-kosztuje-aplikacji` nadal z założenia nie podaje żadnych kwot. **Metadata:** `description` na landingach skrócone pod limity (167/174 → 156/157 znaków), `title` software house'u 71 → 63; frazy główne zachowane. W `layout.tsx` podmieniono **wyłącznie eksport `metadata` i `description` w JSON-LD** - GA4, Clarity, Consent Mode v2 i gtag nietknięte. **Testy - dwa literały sprzężone z treścią i dwa nowe strażniki.** `contact.test.ts` niósł payload skopiowany 1:1 z `hero.tsx`, a `seo.test.ts` szablon `${p.title} — Programo`; oba zsynchronizowane. `about.test.tsx` **wywalił się** po zmianie `about.title` - naprawiony nie przez wklejenie nowego literału, tylko przez asercję na `translations["about.title"].pl`: test nazywa się „renders the page heading", więc pinowanie brzmienia sprawiało, że **każda korekta copy łamała test renderowania**. Dołożone dwa strażniki regresji, żeby długi myślnik nie wrócił tylnymi drzwiami: w `i18n.test.ts` po **wszystkich** kluczach słownika (PL i EN), w `seo.test.ts` po `subtitle`/`description`/`longDescription` w `projects.ts` - czyli po obu miejscach, gdzie w tym repo mieszka copy. Suite 103 → **105 testów**. Bramki: `tsc` 0, `vitest` 8 plików / 105 testów, `lint` 0 błędów, `build` 0. **⚠️ Do decyzji właściciela - cztery sprzeczności faktograficzne znalezione przy czytaniu całego copy, świadomie NIE „naprawione" (nie zgaduje się faktów portfolio):** (1) `home.work.jedmar.solution` mówi „Nowy frontend Next.js na istniejącym PrestaShopie", a `projects.ts` i `offer.ts` mówią, że sklep „istniał wcześniej i nie jest naszym dziełem", a robiliśmy aplikacje mobilne + schematy części; (2) WKS Poznań ma trzy różne opisy: „7 podstron, statyczne HTML/CSS/JS" (`home.ts`) vs „17 podstron, własny CMS" (`strony-internetowe`) vs panel „aktualizowany przez trenerkę" (`about.ts`); (3) **role założycieli są odwrócone między plikami** - `about.ts`: Wojciech = design i produkt, Bartosz = inżynieria; `home.ts`: `founders.wojtek.focus` = „Architektura, kod, wdrożenie", `founders.bartek.focus` = „Strategia, komunikacja, biznes"; (4) pisownia nazwiska: „Bartosz Kołaj" (`home.ts`) vs „Bartosz Kolaj" (`about.ts`, `forms.ts`, asercja w `seo.test.ts`). **Strefa nietykalna (GA4, Clarity, Consent Mode v2, gtag, `cookie-banner`, `analytics-tracker`, `tracking.ts`) nie została ruszona.** | `src/lib/i18n/dictionaries/{home,offer,marketing,forms,about,pricing,contact}.ts`, `src/lib/projects.ts`, `src/app/projects/[slug]/page.tsx`, `src/app/{software-house-poznan,ile-kosztuje-aplikacji,strony-internetowe,sklepy-internetowe}/page.tsx`, `src/app/layout.tsx`, `src/__tests__/{i18n,seo}.test.ts`, `src/__tests__/components/about.test.tsx`, `src/__tests__/api/contact.test.ts` |
| 2026-08-03 | **Edytor wizualny na localhoście — klikasz element, zmieniasz tekst/kolor albo usuwasz go na stałe (prośba Bartka).** Cel postawiony wprost: „edytuj napisy i kolory, każdy element klikalny, z możliwością trwałego usunięcia, a ty po mojej pracy wrzucisz to na stronę główną". Ostatnie zdanie wyznacza całą architekturę: **edycja musi lądować w pliku źródłowym**, nie w DOM-ie. Copy siedzi w słownikach i18n, kolory w custom properties w `globals.css` — edytor mutujący tylko DOM gubiłby wszystko przy reloadzie i nigdy nie dałby się zacommitować. Każda operacja przepisuje realny plik, więc zmiana pokazuje się w `git diff` i jedzie jak każdy inny commit. **Mapowanie DOM → źródło bez instrumentacji markupu** (żaden `data-*` nie trafia do produkcyjnego HTML): teksty przez **odwrócony indeks** po `translations`, które i tak są już w bundlu — wartość → klucz, oba języki, wygrywa pierwszy klucz; sekcje przez **dopasowanie tekstu kotwiczącego** (`anchorKey` z rejestru) zamiast indeksu w DOM, bo indeks psuje się przy zmianie kolejności; wiersze list przez istniejące `href="/projects/<slug>"` i przez tekst logotypu w pasku zaufania. **Trzy warstwy odcięcia od produkcji:** (1) `providers.tsx` montuje edytor ternarnym na `process.env.NODE_ENV === "development"` w zasięgu modułu — bundler wkleja literał, więc w produkcji to `() => null`, a `import()` jest nieosiągalny; (2) route `/api/dev-edit` zwraca 404 **zanim sparsuje body**, jeśli nie jest devem; (3) sprawdzenie hosta pętli zwrotnej. **Ścieżki nigdy nie pochodzą z requestu** — przeglądarka wysyła wyłącznie `id` (plus zwalidowany parametr), a plik wybiera serwerowy rejestr `targets.ts`; dodatkowo `resolveInRepo` odrzuca cokolwiek poza `src/`. Z rejestru **świadomie wyłączone** `ContactBookend` (trzyma kotwicę `#kontakt`, na którą celuje navbar, sticky CTA i każdy przycisk landingów — usunięcie zostawiłoby martwe linki w całym serwisie) i `Hero`. **Cztery błędy znalezione i naprawione przy weryfikacji w przeglądarce:** **[P0] `removeArrayEntry` nie działał na tablicach z typem.** `src.indexOf("[", …)` trafiał w nawias **adnotacji typu** w `const cases: CaseStudy[] = [`, nie w literał tablicy — liczenie głębokości zamykało się natychmiast na pustej parze. `CLIENTS` działał tylko dlatego, że nie ma adnotacji. Nawias brany teraz z samego dopasowania regexa. **[P0] Tryb wskazywania połykał kliknięcia we własne przyciski panelu.** Test „czy to nasz chrome?" czytał `elementFromPoint(e.clientX, e.clientY)`, a kliknięcie z **klawiatury** (Enter/Spacja na przycisku) niesie współrzędne `0,0` — hit-test patrzył w lewy górny róg strony i `preventDefault` zjadał każde naciśnięcie. Guard czyta teraz `e.target`; `elementFromPoint` służy wyłącznie do wskazania elementu pod kursorem. Bez tego edytor był **nieużywalny z klawiatury**. **[P1] Elementy nieprzetłumaczone były niewybieralne, więc ich przycisk usuwania był nieosiągalny.** Logotypy klientów to zwykłe stringi w tablicy w `trust-bar.tsx`, a tytuły realizacji czyta `getProjectBySlug` — `findText` zwracał `null` i handler wychodził, zanim policzył cokolwiek do usunięcia. Gałąź „client" była **martwym kodem**. Klucz w zaznaczeniu jest teraz nullowalny: nie ma czego edytować, ale jest co usunąć. **[P1] Usunięcie realizacji dawało się kliknąć tylko na miniaturze.** Nagłówek i opis case study są **rodzeństwem** linku ze zdjęciem, nie jego dziećmi, więc szukanie po przodkach oferowało „usuń wiersz" w najmniej oczywistym miejscu. `ownedSlug` poszerza zakres po jednym przodku i zatrzymuje się na pierwszym kontenerze z **dokładnie jednym** linkiem projektu — znajduje kartę bez zaszywania struktury (szersze pojmałoby całą sekcję i wszystkie wiersze). **Tokeny trzymane jako trójka RGB** (`--theme-glow: 218, 241, 222`, konsumowane w `rgba(var(--theme-glow), .1)`) dostały własną obsługę: picker pokazuje kolor, ale zapis wraca do **zapisanej notacji** — wpisanie tam heksa rozwaliłoby każdą regułę, która ten token interpoluje. **Weryfikacja end-to-end w przeglądarce, nie tylko curlem:** klik → panel → edycja → zapis → plik → Fast Refresh → nowy tekst na stronie; picker koloru → `globals.css` (właściwy blok motywu, komentarz zachowany, warianty `-rgb` przeliczone); usunięcie wiersza z UI → 11 linii mniej w `client-work.tsx`. Wejścia wrogie odrzucone: `red; } body { display:none` jako kolor, `../../etc` jako nazwa tokenu, `../../../etc/passwd` jako parametr listy. Po usunięciach drzewo nadal się typuje, więc `delete` zostawia poprawny kod. Wszystkie mutacje testowe wycofane. **Odcięcie produkcyjne potwierdzone po refaktorze:** `grep` po `.next/static/` i po prerenderowanym HTML — zero trafień; `next start` zwraca **404 na GET i POST**. Bramki: `tsc` 0, `lint` 0 błędów, `vitest` 103/103, `build` 0. **⚠️ Pułapki:** macOS rozwiązuje `localhost` na `::1` **przed** `127.0.0.1` — obcy dev-server na `[::1]:3000` przesłaniał nasz na `*:3000` i dawał mylące „Not Found" (wielka `F` zdradziła, że to Next, nie nasz handler); testuj po `127.0.0.1`. Odznaka devtools Next siedzi w **shadow rootcie** w lewym dolnym rogu — nie da się jej wyliczyć ani przesunąć, więc chrome edytora zadokowany do prawej krawędzi (lewa góra koliduje z wyśrodkowaną pigułką nawigacji). Odczyt `textContent` w tym samym wywołaniu `preview_eval`, w którym poszło kliknięcie, **wyprzedza render Reacta** — panel wygląda na niedziałający; czytaj kolejnym wywołaniem. **Strefa nietykalna (GA4, Clarity, Consent Mode v2, gtag, JSON-LD, `cookie-banner`, `analytics-tracker`, `tracking.ts`) nie została ruszona.** | `src/lib/dev-edit/{source-ops,targets}.ts`, `src/app/api/dev-edit/route.ts`, `src/components/dev/dev-editor.tsx`, `src/components/providers.tsx` |
| 2026-08-03 | **Interlinia w skali typograficznej — naprawa kontraktu, którego nikt nie dotrzymał.** Znalezione przy weryfikacji mobilnej redesignu (390×844): **każdy nagłówek poza jednym renderował się z interlinią 1.6**, czyli wartością dla długiego tekstu, nie dla display. Przyczyna: skala deklarowała **wyłącznie rozmiar** (`--text-h2: var(--step-h2)`), a komentarz w `globals.css` zapisywał kontrakt „every consumer sets its own `leading-*`, because a 84px headline and a 24px lead want opposite line-heights". Kontraktu dotrzymał **dokładnie jeden konsument** — heros (`leading-[1.04]`). Pozostałe 12 nagłówków (6× `text-h2`, 3× `text-h3`, 3× `text-h4`) nie ustawiało niczego, więc dziedziczyły `body { line-height: 1.6 }`, a `line-height` bez jednostki mnoży się przez **własny** `font-size` elementu — im większy nagłówek, tym gorzej. Efekt na desktopie: **h2 52 px stał na 83 px interlinii**; dwuliniowy nagłówek sekcji rozjeżdżał się na wysokość, przez którą czytał się jak dwa osobne zdania, nie jak jeden tytuł. Naprawa na poziomie tokenu, nie łatanie po komponentach: Tailwind v4 paruje `--text-*` z `--text-*--line-height`, więc zadeklarowana została **druga połowa skali** (`--lead-display: 1.04`, `--lead-h2: 1.12`, `--lead-h3: 1.2`, `--lead-h4: 1.3`, `--lead-lead: 1.6`) i podpięta w `@theme inline`. Interlinia zacieśnia się wraz ze wzrostem stopnia — odwrotnie niż dotychczasowe płaskie 1.6. `leading-*` na elemencie nadal wygrywa, więc miejsca celowo luźne (`text-lead leading-relaxed`) są bez zmian. Nadmiarowe `leading-[1.04]` w herosie usunięte — token niesie teraz dokładnie tę samą wartość, a duplikat tylko zapraszałby do rozjazdu. **Zasięg ograniczony i sprawdzony:** skala jest używana wyłącznie przez sekcje strony głównej (7 plików, 21 wystąpień) — reszta serwisu ma własne rozmiary, więc zmiana nie dotyka landingów Ads ani podstron. **Weryfikacja:** zmierzone `getComputedStyle` na obu viewportach — dark 390×844: display 1.04, h2 1.12, h3 1.2, h4 1.3, lead 1.62; desktop 1440: h2 52 px → **58 px zamiast 83 px**. Sprawdzone **przycinanie polskich diakrytyków i wydłużeń dolnych** przy zacieśnionej interlinii: porównanie boxa elementu z sumą `Range.getClientRects()` plus szukanie przodka z `overflow != visible` — **zero elementów przycinanych** (2 px nawisu tuszu nad boxem herosa to zastane 1.04 i nic go nie tnie). Strona główna skróciła się o **329 px** przypadkowej interlinii. Brak przewijania poziomego na 390 px (`scrollWidth == clientWidth == 390`, 0 elementów poza viewportem). Bramki: `tsc` 0, `lint` 0 błędów, `vitest` 103/103, `build` 0. **Pułapka:** `preview_eval` z przypisaniem `location.href` zabija kontekst wykonania („Execution context was destroyed") — nawigację i pomiar rozdziel na dwa wywołania. Dodatkowo `html { scroll-behavior: smooth }` sprawia, że `scrollIntoView`/`scrollTo` **nie kończą się przed odczytem** — przed pomiarem ustaw `document.documentElement.style.scrollBehavior = 'auto'`. | `src/app/globals.css`, `src/components/home/hero.tsx` |
| 2026-08-03 | **Kolejność portfolio ustawiona pod to, czym chcemy być oceniani (decyzja Bartka).** Priorytet: **Jedmar i Estalo z przodu, W. Safe Finance na końcu** — wszędzie tam, gdzie kolejność jest widoczna. Zmiana dotknęła czterech niezależnych list, bo w tym repo kolejność **nie ma jednego źródła prawdy**: (1) `projects.ts` — `wsafefinanse` przeniesione na koniec tablicy; to steruje `/projekty`, bo `FeaturedWork` renderuje `projects` w kolejności tablicy, bez sortowania — widok „wszystkie" ma teraz Jedmar i Estalo na czele, a W. Safe Finance zamyka (w filtrze `dla-klientow` był już ostatni). (2) `home/client-work.tsx` — case studies na stronie głównej szły `wsafefinanse → jedmar → wks-poznan`, czyli **najsłabszy dowód otwierał sekcję**; teraz `jedmar → wks-poznan → wsafefinanse`. (3) `trust-bar.tsx` — pasek nazw klientów: „W. Safe Finance" z 3. na 5. pozycję. (4) `projects-marquee.tsx` — `jedmar` na czoło przed `estalo` (wcześniej 5.). **Nie ruszone świadomie:** `/strony-internetowe` (`webCases`) trzyma W. Safe Finance na pierwszym miejscu — to landing pod kampanię Ads o stronach firmowych, gdzie strona doradcy finansowego jest najtrafniejszym przykładem; przestawienie działałoby przeciw konwersji tej kampanii. `marketing-tracking.tsx` miał go już ostatniego. **Athlix — zgłoszony jako trzeci flagowy projekt, ale nie istnieje w kodzie.** Został **świadomie usunięty 2026-06-03 na prośbę Wojtka** jako „korekta tego, co realnie robiliśmy": wpis z `projects.ts`, referencje w `projects-marquee`/`trust-bar`/`software-house-poznan` oraz zrzuty `athlix-*.webp` skasowane, slug wpisany na `REMOVED_SLUGS` w `projects.test.ts`. Przywrócenie wymaga materiałów (URL działającej aplikacji albo zrzuty + aktualny opis i stack) — odtwarzanie z pamięci oznaczałoby zmyślanie danych portfolio, więc **decyzją właściciela odłożone do osobnego zadania**. Testy są odporne na kolejność (iterują po `EXPECTED_SLUGS`, `projects[0]` czytane dynamicznie), więc nie wymagały zmian. Weryfikacja: kolejność odczytana z **serwerowego HTML** obu tras (`/` i `/projekty`), nie z DOM po hydracji. Bramki: `tsc` 0, `lint` 0 błędów, `vitest` 103/103, `build` 0. | `src/lib/projects.ts`, `src/components/home/client-work.tsx`, `src/components/trust-bar.tsx`, `src/components/projects-marquee.tsx` |
| 2026-08-03 | **Redesign strony głównej — przeniesienie nowej architektury sekcji na żywą gałąź + naprawy znalezione przy weryfikacji.** Kontekst: poprzedni wpis wypuścił na produkcję **wyłącznie typografię**; sam układ strony głównej został w gałęzi czerwcowej, której nie dało się scalić (dwie różne architektury, 31 konfliktów). Ta zmiana przenosi na `main` sam **layout**, bez cofania czegokolwiek. **Nowy skład strony głównej:** `Hero → TrustBar → ClientWork → OwnProducts → ServicesOverview → Process → Founders → Faq → ContactBookend` — czytane z góry na dół jako jeden wywód: kim jesteśmy → kto nam zaufał → dowód że to zrobiliśmy → co robimy → jak to idzie → z kim będziesz pracować → obiekcje → zostaw numer. `ClientWork` i `OwnProducts` celowo dzielą `bg-surface-dim` bez separatora (dwa pasma jednego zagłębionego pokoju: „dla klientów" / „dla siebie"); `TrustBar` jest współdzielony z landingami Ads, więc zostaje bezpropsowym eksportem domyślnym. Wycofane komponenty (`featured-jedmar`, `in-production`, `offer-pillars`, `people`, `portfolio-grid`, `main-intro`) usunięte, nie osierocone. Cel konwersji jest jeden — **zostawienie numeru telefonu** — więc formularz stoi w herosie, a nie dopiero w stopce. **[P0] Hero startował z `opacity: 0`.** Wjazd był bramkowany klasą wyzwalaną po hydracji, więc do czasu wykonania JS sekcja z formularzem i numerem telefonu była pusta — a jest to element LCP. Reveal zdjęty z herosa: treść jest widoczna w HTML z serwera, animacje zostają tylko tam, gdzie coś już widocznego wzbogacają. **[P0] `/api/contact` odrzucał zgłoszenie z samym telefonem** — czyli dokładnie ten kształt leada, do którego cała strona prowadzi; walidacja przyjmuje teraz telefon **albo** e-mail (testy w `src/__tests__/api/contact.test.ts`). **[P1] Desktopowy rail „SCROLL"** usunięty (`scroll-progress.tsx` zostaje wyłącznie mobilnym paskiem postępu). **[P1] „WKS Poznan"** → nazwa klienta pobierana z `getProjectBySlug()` zamiast literału, więc diakrytyki nie mogą się już rozjechać z danymi. **Pasek nawigacji (zgłoszenie właściciela: „logo i przyciski zlewają się ze stroną przy scrollowaniu").** Przyczyna była strukturalna, nie kosmetyczna: desktopowy nagłówek to siatka trzech stref — logo | pigułka | telefon/motyw/język/CTA — a **tło miała wyłącznie strefa środkowa**. Logo i cały prawy klaster leżały bezpośrednio na przewijanej treści. Dodany `.nav-band`: pełnej szerokości podkład za wszystkimi trzema strefami, pojawiający się na scrollu (tokeny `--theme-nav-band` / `--theme-nav-band-border` per motyw). Podkład jest w kolorze powierzchni, **nie** odwrócony jak pigułka — ma czytać się jako strona kontynuowana za chromem, nie jako druga płyta. Przy okazji pigułka **przestała eskalować na scrollu**: dwa `backdrop-filter` na elemencie `fixed` to drugi pełnoekranowy przebieg filtra w trakcie przewijania, a świecąca pigułka na nieprzezroczystym pasku czyta się jak szum, nie jak hierarchia. **[P0, błąd zastany] `backdrop-filter` nie docierał do przeglądarki.** Lightning CSS **deduplikuje ręcznie dopisany prefiks względem właściwości standardowej i zostawia tylko prefiksowany** — w zbudowanym CSS był wyłącznie `-webkit-backdrop-filter`. Dotyczyło to również `.liquid-glass`, czyli **cały szklany navbar nie miał rozmycia w Firefoksie**, i to od dawna. Ręczne linie `-webkit-` skasowane; build emituje teraz obie właściwości (zweryfikowane w `.next/static/chunks/*.css`). **[P0] Nagłówek miał 177 px wysokości.** Logo to plik z płótnem A4 (`viewBox 841.89×595.28`) z wordmarkiem wyśrodkowanym w środku — tusz zajmuje **20,4 % wysokości płótna**. Przy `h-auto` dawało to box 141 px na ~29 px widocznego logo: absurdalnie wysoki podkład i **niewidoczny pasek na pełną szerokość przechwytujący kliknięcia** nad herosem. Kadr przycięty do proporcji tuszu (`aspect-[841.89/121.3] object-cover`) — `h-auto` i `object-cover` skalują tak samo po szerokości, więc renderowany wordmark jest **piksel w piksel identyczny**, znika tylko martwa przestrzeń. Nagłówek 177 px → **84 px**. To samo zastosowane w stopce (było 141 px boxu i ~112 px pustki między logo a tagline'em). **[P0] Logo przycięte w połowie.** Trzy warianty pliku były **niespójnie wykadrowane**: `white` i `gradient` mają tusz wyśrodkowany, `dark` leżał **60,68 jednostki niżej** — dokładnie o połowę własnej wysokości, więc jego górna krawędź wypadała w środku płótna i symetryczny `object-cover` ucinał mu górę. Naprawione **na poziomie zasobu**, nie kompensacją per-motyw: `viewBox` w `programo-logo-dark.svg` przesunięty o zmierzoną różnicę (`0 0` → `23.57 60.68`). Plik to same ścieżki (0 × `clipPath`/`mask`/`use`/`image`/`text`/gradient), więc przekadrowanie jest bezpieczne. Po zmianie wszystkie trzy warianty raportują identyczne `relX 68.22 / relY 236.96 / 705.45×121.35`, offset od środka 0. **Weryfikacja:** audyt kontrastu (canvas + kompozycja alfa po łańcuchu przodków, progi 4.5:1 / 3:1, osobno `::placeholder`) — **0 naruszeń w obu motywach**; placeholdery light 7.55 / 6.07 / 6.07 / 6.07, dark 6.71 / 7.08 / 7.08 / 7.08. Navbar obejrzany w obu motywach na desktopie i na **realnym viewporcie 390×844**. Bramki: `tsc` 0, `lint` 0 błędów, `vitest` 8 plików / **103 testy**, `build` 0. **⚠️ Pułapka, która trzykrotnie w tej sesji dała fałszywy wynik:** **karta w tle zamraża zarówno pętlę rAF framer-motion, jak i przejścia CSS**. Pseudo-elementy (`::placeholder`) **nie dziedziczą `transition-colors`**, więc podczas zamrożonego przejścia przeskakują natychmiast, podczas gdy element-gospodarz zostaje na starej wartości — stąd zmierzone „1.73:1", które okazało się artefaktem (prawdziwa wartość: 7.55:1). Przed **jakimkolwiek** pomiarem wstrzykuj `*{transition:none !important;animation:none !important}`. Dodatkowo: `mcp__Claude_in_Chrome__javascript_tool` **nie czeka na obietnice** — asynchroniczne IIFE zwraca `{}`; wynik trzeba odłożyć na `window` i odczytać drugim wywołaniem. **Strefa nietykalna (GA4, Clarity, Consent Mode v2, gtag, JSON-LD, `cookie-banner`, `analytics-tracker`, `tracking.ts`) nie została ruszona.** | `src/app/page.tsx`, `src/app/globals.css`, `src/app/api/contact/route.ts`, `src/components/home/{hero,client-work,own-products,services-overview,process,founders,faq}.tsx`, `src/components/{navbar,footer,scroll-progress}.tsx`, `src/lib/i18n/dictionaries/home.ts`, `public/programo-logo-dark.svg`, `src/__tests__/api/contact.test.ts` |
| 2026-08-03 | **Redesign typografii + naprawy dostępności — port na żywą gałąź `main`.** **Typografia (główne życzenie właściciela):** zejście z pary Newsreader + Plus Jakarta Sans na **jeden webfont display (Archivo, oś zmienna `wdth`) na nagłówki + natywny system stack na całą resztę** (`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial`). Powody: (1) tekst UI renderuje się natychmiast, bez transferu i bez FOUT — na telefonie w terenie, czyli tam gdzie siedzi nasz klient, to jedyna warstwa tekstu widoczna w pierwszej sekundzie; (2) kontrast marki przenosi się z „dwa podobne fonty" na oś szerokości, której żaden systemowy sans nie udaje; (3) Newsreader i Plus Jakarta Sans są na liście reflex-reject skilla impeccable — to domyślne fonty generatywne, dokładnie ten sygnał „szablonowego software house'u", którego mieliśmy uniknąć. Body dostrojone pod system stack: `letter-spacing: -0.005em`, `line-height: 1.6` (Jakarta ustawiała luźniej i niżej). **Dostępność (WCAG 2.2 AA — twarde wymaganie właściciela):** [P0] **`text-red-500` na ścieżce konwersji** — komunikaty błędów obu formularzy leadowych miały 3.68:1 na motywie jasnym (próg 4.5:1); żaden pojedynczy czerwony nie przechodzi jednocześnie na `#051F20` i `#FBFDFB` (ograniczenia rozłączne: dark wymaga L ≥ 0.2317, light L ≤ 0.1808), więc wprowadzony **token per-motyw `--theme-error` → `--color-error`**: `#F87171` (6.21:1 na dark) / `#B91C1C` (6.33:1 na light). Zmierzone in situ sondą wstrzykniętą w realny `<form>` na `/kontakt`, bez wysyłki. `crm/LeadsDashboard.tsx` celowo pominięty — narzędzie wewnętrzne za auth, nie ścieżka klienta. [P0] **Kafle realizacji nieczytelne na jasnych zrzutach** — scrim `from-black/80 via-black/40` przy `text-white/60` dawał **1.96:1** w najgorszym przypadku (biały screenshot + hover przy `opacity-100`); scrim podniesiony do `from-black/95 via-black/75` (kompozycja → `#404040`), rok z `white/60` → `white/80` → **7.34:1**. Analogicznie pigułka domeny w `featured-work.tsx`: `bg-black/55` + `white/90` = **4.20:1** przy 10px → `bg-black/70` + `white` = **8.45:1**. [P0] **`id="realizacje"` zduplikowane 4×** (`sklepy-internetowe`, `strony-internetowe`, `marketing-tracking`, `projects-marquee`) — dokument niepoprawny, cel skoku niejednoznaczny; kotwica usunięta z komponentu współdzielonego (`projects-marquee`), bo każda strona-konsument i tak deklaruje własną w miejscu, w które faktycznie chce skoczyć. Sekcja opisana `aria-labelledby`. [P1] **`/stack` bez `<h1>`** — tytuł strony był `<h2>` (`tech-stack.tsx` montuje wyłącznie `/stack`), podniesiony do `<h1>`; przy okazji zdjęty jedyny w całym repo `bg-clip-text` + `text-transparent` — glify istniały tylko jako maska nad obrazem tła, więc w trybie wymuszonych kolorów jedyny `<h1>` strony renderował się niewidocznie. Teraz `text-on-surface`: 14.4:1 dark / 16.79:1 light. **Weryfikacja:** własny audytor kontrastu (canvas + kompozycja alfa po całym łańcuchu przodków, luminancja względna, progi 4.5:1 / 3:1 z regułą dużego tekstu) przepuszczony przez 12 tras × 2 motywy — `/`, `/oferta`, `/cennik`, `/kontakt`, `/projekty`, `/projects/jedmar`, `/o-nas`, `/stack`, `/strony-internetowe`, `/sklepy-internetowe`, `/software-house-poznan`, `/ile-kosztuje-aplikacji`, `/polityka-prywatnosci`: **0 naruszeń, 0 zduplikowanych `id`, 0 przewijania poziomego, po jednym `<h1>` i `<main>`**. Mobile zweryfikowany na **realnym viewporcie 375×812** (Claude Preview MCP `preview_resize`), nie emulacją — nagłówek Archivo łamie się na 5 linii bez przepełnienia. Bramki: `tsc` 0, `vitest` 8 plików / 98 testów, `lint` 0 błędów, `build` 0. **⚠️ Kluczowe ustalenie procesowe:** wcześniejsza gałąź redesignu (`integrate/home-redesign`) była budowana na bazie **24 commity za `origin/main`** — jej przodek to 2026-06-18, podczas gdy `origin/main` ma pracę z 2026-07-15…07-20, która **jest tym, co stoi na produkcji** (potwierdzone przez `curl https://programo.pl` i dopasowanie nagłówków do `featured-jedmar` / `people` / `contact-bookend`). Próbny merge dał 31 konfliktów, w tym delete/modify na `src/lib/i18n.tsx` i `preloader.tsx` — to nie konflikt scalania, tylko **dwie różne architektury strony głównej**. Wypchnięcie tamtej gałęzi cofnęłoby 24 commity żywej pracy, więc zamiast scalać **przeniesiono na `main` wyłącznie konkretne delty** opisane wyżej. Gałąź czerwcowa zachowana lokalnie jako materiał referencyjny, nie do scalenia. **Pułapki dla kolejnych agentów:** (1) `mcp__Claude_in_Chrome__resize_window` **raportuje sukces, ale nie zmienia viewportu** (`innerWidth` zostaje) — do realnego mobile używaj Claude Preview MCP `preview_resize`; (2) porzucony `next start` na porcie audytu serwuje build **poprzedniej gałęzi**, a `curl` dalej zwraca 200 — przed jakąkolwiek weryfikacją wizualną zawsze `lsof -ti tcp:<port> \| xargs kill -9` + `rm -rf .next`; (3) `git status -sb \| head -N` ucina listę zmian i potrafi zasugerować, że edycje zniknęły — używaj `git status --porcelain` bez potoku. **Strefa nietykalna (`layout.tsx` `<head>`: GA4, Clarity, Consent Mode v2, gtag, JSON-LD; `cookie-banner`, `analytics-tracker`, `tracking.ts`) nie została ruszona** — w `layout.tsx` zmieniono wyłącznie import fontu, konfigurację `Archivo` i `className` na `<html>`. | `src/app/layout.tsx`, `src/app/globals.css`, `src/components/compact-lead-form.tsx`, `src/components/quick-contact.tsx`, `src/components/projects-marquee.tsx`, `src/components/featured-work.tsx`, `src/components/tech-stack.tsx` |
| 2026-07-15 | **Fala fullaudit — typografia i dostępność (agent builder, docs/audit-visual/{typografia,ux-a11y}-report.md + FULLAUDIT-REPORT.md; część większej fali równoległej: mockupy, różnorodność heroów, motion/reduced-motion, typografia/a11y — ten wpis pokrywa moją część, typografia/a11y).** [P0] **H3 bez `font-headline` na `/software-house-poznan` i `/ile-kosztuje-aplikacji`** — wszystkie h3 (karty usług, karty projektów, FAQ) przepisane na `font-headline text-*` (Newsreader zamiast body-fontu); karty usług/projektów/CTA finalne przepisane z „plain" (`border-current/15`, bg transparentne) na „mint-filled" spójny z resztą serwisu (`rounded-2xl border border-outline-variant/40 bg-surface-container-low` dla neutralnych kart, `border-primary/30 bg-primary/5` dla CTA finalnych — wzorzec z `offer.tsx`/`pricing.tsx`); stack-pills ujednolicone z tokenami (`text-on-surface-variant`, `border-outline-variant/60`). [P0] **Miara tekstu (131–160 zn./linię → ~65–82)**: FAQ na obu stronach SEO (`max-w-2xl` na odpowiedziach), intro-akapit „Dlaczego software house..." (`max-w-3xl`), oraz najdłuższa linia audytu (160 zn.) na `/strony-internetowe` (akapit „Prowadzimy też kampanie..." → `max-w-2xl`). Zmierzone Range API po fixie: 68–82 zn./linię (docelowo 65–75, zmierzone w czystej karcie przeglądarki 1280px). [P0] **Escape + focus-trap**: nowy self-contained (bez współdzielonego hooka — każdy plik ma własną implementację, żeby nie wychodzić poza własność plików) `useEffect` w `navbar.tsx` (mobilne menu — Escape zamyka, focus trap Tab/Shift+Tab w overlayu, focus wraca na hamburger po zamknięciu, `aria-expanded`/`aria-controls`) i `cookie-banner.tsx` (modal ustawień — Escape zamyka, focus trap, focus wraca do triggera niezależnie skąd otwarty — `document.activeElement` capture, nie hardkodowany ref). Zweryfikowane realnym klikiem myszy + prawdziwym Escape (nie tylko dispatchEvent) w przeglądarce. [P0] **Kontrast** — usunięte/podniesione modyfikatory opacity na `text-on-surface-variant` we WŁASNYCH plikach z listy ~14 miejsc z ux-a11y-report P0-2: `footer.tsx` (nagłówki kolumn, copyright, przycisk cookies, „Odpowiadamy w 24 h" — `/60`/`/70` → bez modyfikatora), `quick-contact.tsx` (label „(opcjonalnie)" `/50`→bez modyfikatora, placeholder `/30`→`/70`), `compact-lead-form.tsx` (placeholder `/50`→`/70`). Zmierzone po fixie (canvas/getComputedStyle, WCAG matematycznie): wszystkie 5 sprawdzonych miejsc **7.55:1–8.56:1** (próg AA 4.5:1) — było 2.48–4.03:1. Pozostałe miejsca z listy (tech-stack.tsx, ProjectDetailClient.tsx, featured-work.tsx, trust-bar.tsx) NIE są moją własnością — pominięte. [P1] **Tap-targety**: hamburger `h-8 w-8`→`min-h-[44px] min-w-[44px]` + `aria-expanded`; theme/lang toggle mobile `w-6 h-6`/bez min-size→`min-h-[44px] min-w-[44px]`; linki stopki i przycisk cookies dostały `-my-2 py-2 min-h-[32px]` (touch area bez zmiany gęstości wizualnej). [P1] **Formularze**: `aria-describedby`+`id`+`role="alert"` na błędach pól (name/contact/consent/server) w `quick-contact.tsx` i (name/phone/consent/server) w `compact-lead-form.tsx`; `QuickContact` sukces dostał `role="status" aria-live="polite"` + focus-management (`successRef`+`tabIndex=-1`, wzorzec 1:1 z `compact-lead-form.tsx`, które już to miało). [P1] **`<header>` landmark**: `navbar.tsx` całość owinięta w `<header className="contents">` (display:contents — zero wpływu na fixed-positioning dzieci) — wcześniej 0 landmarków „banner" na stronie mimo istniejącego `<nav>`; `<main id="main-content">` już istniał w `providers.tsx` (zweryfikowane, bez zmian) — ALE znaleziony i naprawiony przy okazji realny błąd: `/software-house-poznan` i `/ile-kosztuje-aplikacji` miały WŁASNY zagnieżdżony `<main>` (duplikat landmarku „main" — niepoprawne), zamieniony na `<div>`. `not-found.tsx` dostał `document.title` w `useEffect` (plik jest `"use client"`, `export const metadata` niemożliwy — wzorzec z ux-a11y-report P2-3). [P2] **Sieroty (nbsp)**: footer tagline „...budowane w Poznaniu." (`dictionaries/common.ts`), H1 „Software House Poznań — Programo", 5 nagłówków na `/strony-internetowe` i `/sklepy-internetowe` (CaseStudies/CompactLeadForm heading props, hardkodowane w tych plikach — zgodnie z zasadą repo że te strony SEO nie idą przez i18n). NIE ruszone: `home.ts`/`home/*` (poza własnością). [P0/reduced-motion] **navbar/footer/cookie-banner**: `useReducedMotion()` z framer-motion — wariant statyczny (`initial={false}`, `transition duration:0`, brak parallax w stopce `useTransform(...,[0,0])` gdy reduced motion) dla wjazdu navbara, mobilnego overlayu (clipPath+stagger), obu bloków `AnimatePresence` w cookie-banner. **Znaleziony, POTWIERDZONY (real click + real focus, nie tylko syntetyczny dispatchEvent), ale POZA ZAKRESEM bug** (istnieje też w kodzie którego nie dotykałem — dolny pasek zgody cookies z niezmodyfikowanym `exit={{y:"115%"}}` — więc to nie regresja z tej sesji): elementy `<AnimatePresence>{cond && <motion.div exit={{...}}>}</AnimatePresence>` w tym repo nie są nigdy usuwane z DOM po zakończeniu exit-animacji (framer-motion 12.35.2 + React 19.2.3 + Next 16.1.6); dla modala ustawień cookies (wyśrodkowany, opacity-based exit) to realnie blokuje kliknięcia na środku ekranu po zamknięciu — próby fixu (`pointerEvents`/`transitionEnd` w `exit`) nie zadziałały, więc **wycofane** (kod wrócił do stanu sprzed próby), zgłoszone jako osobne zadanie (task_d298e033) do dedykowanej sesji. Weryfikacja: `npx tsc --noEmit` czysty; `npm run build` zielony (32 strony); `npm run test` 98/98 zielone; `npm run start -p 3216` + realna przeglądarka — h1=1/main=1/footer=1 na wszystkich sprawdzonych stronach (`/`, `/software-house-poznan`, `/ile-kosztuje-aplikacji`, `/strony-internetowe`, `/sklepy-internetowe`, `/kontakt`), Escape zamyka mobilne menu (potwierdzone realnym Escape + zwrot focusu na hamburger), Escape zamyka modal cookies (state poprawnie się zmienia + focus wraca do triggera — DOM-lingering to osobny, zgłoszony bug), kontrast 5 miejsc 7.55–8.56:1, tap-targety nav 44×44px. Poza zakresem (zgłoszone, nie naprawiane): AnimatePresence-nie-unmountuje (task_d298e033, patrz wyżej). | `src/components/{navbar,footer,cookie-banner,quick-contact,compact-lead-form}.tsx`, `src/app/{software-house-poznan,ile-kosztuje-aplikacji,strony-internetowe,sklepy-internetowe}/page.tsx`, `src/app/kontakt/contact-hero.tsx` (bez zmian — już zgodny), `src/app/not-found.tsx`, `src/lib/i18n/dictionaries/common.ts`, `progress.md` |
| 2026-07-15 | **Redakcja językowa całej warstwy tekstowej (agent-redaktor, brief sekcja 8 anty-slop).** Tylko teksty — zero zmian w JSX/logice/kluczach i18n/formId. (1) **Spójność obietnicy czasu**: wszystkie widoczne „Odpowiadamy w ciągu 24 godzin" / „Wycena w 24 h" / „w ciągu 24 godzin" ujednolicone do formy „w 24 h" (home.faq.a1/a3, strony-internetowe ×2, sklepy-internetowe, software-house-poznan). (2) **Nazwy własne**: „WSafe Finanse" → „W. Safe Finance" (strony-internetowe, sklepy-internetowe); „Pool System" → „PoolTimer" ×2 + status „Wkrótce" → „W rozwoju, pilotaż" (software-house-poznan — zgodnie z wiążącym statusem `development` w projects.ts). (3) **Jedna forma zespołu**: „butikowe/dwuosobowe studio" → „dwóch inżynierów" (strony-internetowe, software-house-poznan ×2); role „Design & Product"/„Engineering Lead" → „Design i Produkt"/„Inżynieria" (spójne z /o-nas i home.people). (4) **Anty-slop/anglicyzmy**: usunięte „Innowacyjny", „AI-first podejście", „budujemy partnerstwa", „ogarniamy"; „deadline"→„termin", „realny feedback"→„opinie od pierwszych użytkowników", „Multi-tenant SaaS z billingiem"→„Wielonajemcowe systemy SaaS z płatnościami", „od hardware'u"→„od sprzętu", „Warstwa software'owa"→„Warstwa oprogramowania", „AI-owy audyt"→„audyt zakupowy oparty na AI" (projects.ts ×2). (5) **Spójność pozycjonowania natywnego**: software-house-poznan usługa „Aplikacje mobilne" przepisana z „Capacitor 8, PWA, native. Wspólny kod" na „natywnie — Swift/SwiftUI i Kotlin/Jetpack Compose" (strona przeczyła reszcie serwisu: „bez półśrodków webview"); ile-kosztuje-aplikacji: usunięte „Najtaniej wychodzi wspólny kod (Capacitor/PWA)" → „start od jednej platformy/mniejszego etapu". (6) **Drobne**: interpunkcja home.faq.a3 („harmonogram, i zaczynamy" → „oraz harmonogram i zaczynamy"), gender-neutralne FAQ („Czy będę mógł" → „Czy mogę"), „właściciel" → „właścicielka" (W. Safe Finance, spójnie z projects.ts), cookie.title PL „Cookies & prywatność" → „Cookies i prywatność", cookie.rejectAll EN „Reject all" → „Essential only" (parytet z PL „Tylko niezbędne"), project.interestedCta odgenderowane, stack.resend „Transakcyjne emaile" → „E-maile transakcyjne", stack.capacitor szyk. NIE ruszone: martwe klucze legacy (main.intro.*, hero.*, trust.*, work.*, contact.form.*, quick.sent — zawierają frazesy, ale nie renderują się nigdzie), liczby (rozbieżność „3–4 tyg." vs „2–3 tyg." i WKS „7 podstron" vs „17 podstron" zgłoszona do decyzji, nie zmieniona). Weryfikacja: `npx tsc --noEmit` exit 0, `npx vitest run` 96/96 zielone, `npm run build` exit 0 (32 trasy), render /software-house-poznan i /strony-internetowe sprawdzony w przeglądarce. | `src/lib/i18n/dictionaries/{home,about,marketing,projects}.ts`, `src/lib/projects.ts`, `src/app/{strony-internetowe,sklepy-internetowe,software-house-poznan,ile-kosztuje-aplikacji}/page.tsx`, `progress.md` |
| 2026-07-15 | **Redesign — homepage konwersyjny (agent builder, brief sekcja 5 + deck sekcja 1).** Przebudowa strony głównej pod konwersję (telefon + formularz) w stylu „editorial forest". Kolejność sekcji: (1) **Hero** (`main-intro.tsx` przepisany) — H1/subheadline z decka 1.1, dual CTA (`tel:+48509123434` z widocznym numerem + „Bezpłatna konsultacja" scroll do `#kontakt`), „Odpowiadamy w 24 h", obok/pod na mobile `CompactLeadForm bare formId="home-hero"`; above-the-fold na 390×844 = nagłówek + oba CTA + SLA. (2) **Pasek zaufania** (`trust-bar.tsx` przepisany, nadal bezargumentowy default export — używany też przez landingi) — lista klientów (Jedmar, WKS Poznań, W. Safe Finance, Skup Nieruchomości, Domki Poznaniak) + 3 prawdziwe liczby z `CountUp` (2 / 10+ / 24 h). (3) **Oferta 4 filary** (`home/offer-pillars.tsx`) — numeracja 01–04, hover z miętowym akcentem, każdy kafel linkuje do podstrony (/oferta ×2, /sklepy-internetowe, /strony-tracking-reklamy). (4) **Portfolio grid** (`home/portfolio-grid.tsx`) — 6 projektów (jedmar PhoneFrame /screenshots/jedmar-app1.webp; estalo/eportal-prawny/wks-poznan/skup-nieruchomosci/rejestr-pro BrowserFrame v2), metryka `metrics[0]` z projects.ts + CountUp, tagi, `statusLabel` uczciwie, kategoria (Produkty Programo/Dla klientów/Tracking i reklamy), cała karta = Link do /projects/[slug] + `trackPortfolioClick`; grid 2 kol desktop / 1 mobile; link „Wszystkie projekty" → /projekty. (5) **Featured case Jedmar** (`home/featured-jedmar.tsx`) — narracja Wyzwanie/Rozwiązanie/Efekt (deck 1.5) + 4 metryki CountUp (1500+/73/~7500/6) + `DeviceDuo` (desktop jedmar-schemat-tool + phone jedmar-app1). (6) **Ludzie** (`home/people.tsx`) — Wojciech (Design i Produkt, 797 222 363) + Bartosz (Inżynieria, 509 123 434), inicjały w kole (brak zdjęć — bez stocka), tel:/mailto: klikalne (tracking przez globalną delegację). (7) **Pasek „W produkcji"** (`home/in-production.tsx`) — deck 1.7 (cztery kolejne aplikacje + ePortal Prawny wkrótce). (8) **FAQ** (`home/faq.tsx`) — akordeon dostępny z klawiatury (button + aria-expanded + aria-controls), schema.org FAQPage JSON-LD z aktualnego języka, 5 pytań decka 1.8. (9) **Klamra kontaktowa** (`home/contact-bookend.tsx`) — `id="kontakt"` (cel scrolla hero), nagłówek/zdanie decka 1.9 + telefon + „Odpowiadamy w 24 h", osadza `QuickContact` AS-IS (bez propsów). Wszystkie teksty przez `t()` z nowych kluczy `home.*` dodanych do `dictionaries/home.ts` (stare klucze `main.*`/`trust.*`/`hero.*`/`sticky.*`/`realizations.*` zachowane — używane przez offer.tsx/footer.tsx/sticky-cta/projects-marquee). Sekcje w `Reveal`, liczby w `CountUp`, zero emotek/kursywy w nagłówkach. Weryfikacja: `npx tsc --noEmit` exit 0; `npm run build` zielony (31 stron); `i18n.test.ts` 12/12 zielone; headless Chrome 1440×900 + 390×844 (mobilny UA) — wszystkie 9 sekcji renderują, brak pustych sekcji, above-fold OK, brak poziomego scrolla (scrollWidth==innerWidth na 375). Pozostałe testy (about/featured-work/footer/navbar/tech-stack/projects/seo) failują też na czystym bdc03de (drift stage-1) — poza zakresem. UWAGA: filar 4 linkuje do `/strony-tracking-reklamy` — strona jeszcze nie istnieje (buduje ją inny agent). | `src/app/page.tsx`, `src/components/{main-intro,trust-bar}.tsx`, `src/components/home/{offer-pillars,portfolio-grid,featured-jedmar,people,in-production,faq,contact-bookend}.tsx` (NEW), `src/lib/i18n/dictionaries/home.ts`, `progress.md` |
| 2026-07-15 | **Redesign — formularze i strona /kontakt (agent builder, brief docs/redesign-brief-2026-07.md sekcje 2/8/10, docs/content-deck-2026-07.md sekcje 7/8).** Worktree był o commit w tyle (brakowało `bdc03de` — i18n modularny, UI primitives); fast-forward na `bdc03de` przed startem, bez konfliktów. (1) **CompactLeadForm** (`src/components/compact-lead-form.tsx`) — pełny redesign: teksty przez `t()` (nowe klucze `compact.*` w `forms.ts`), microcopy 1:1 z decka („Zostaw numer — oddzwonimy w 24 h z konkretami.", „Oddzwońcie do mnie", sukces „Dziękujemy — odezwiemy się w 24 h."), walidacja per-pole (name/phone/consent jako osobne błędy pod polami, nie jeden wspólny alert), pola i checkbox `min-h-[48px]` (touch target), przycisk z spinnerem SVG + `disabled` podczas wysyłki. Props (`formId`, `anchorId`, `projectType`, `bare`, `eyebrow`, `heading`, `sub`) bez zmian — inne strony (`strony-internetowe`, `sklepy-internetowe`) nadal przekazują własne PL stringi jako override, nietknięte. Logika wysyłki/trackingu (`trackLead`, `/api/contact`, `consent:true`, atrybucja) zachowana 1:1. (2) **QuickContact** (`src/components/quick-contact.tsx`, default export bez propsów) — redesign edytorialny: pole kontaktu scalone w jedno „Telefon lub e-mail" (rozdzielane na email/phone przed wysyłką po `@`), 5 chipów typu projektu z decka (Aplikacja webowa/mobilna/Sklep internetowy/Strona+reklamy/Coś innego), **usunięte chipy budżetu** (deck ich nie przewiduje), wiadomość opcjonalna, walidacja per-pole, grid `md:grid-cols-2` (dane | wiadomość) na desktopie, 1 kolumna na mobile z przyciskiem submit jako pełnoszerokim wierszem PO obu kolumnach (żeby na mobile wiadomość nie wypadała po przycisku). Lewa kolumna sekcji rozszerzona o pełne dane bezpośrednie (Wojciech Płonka, Bartosz Kolaj, e-mail, „Odpowiadamy w 24 h"). **Znaleziony i naprawiony bug:** `<motion.form>` nie miał `noValidate` — natywna walidacja HTML5 przechwytywała submit przed customowym kodem, więc błędy PL pod polami nigdy się nie renderowały (tylko natywny tooltip przeglądarki); dodano `noValidate`. trackLead/kontrakt API zachowane 1:1. (3) **sticky-cta.tsx** — próg pokazywania zmieniony z `window.innerHeight*0.6` na stały `SHOW_AFTER_SCROLL_PX = 400`; drobny szlif wizualny (backdrop-blur na mobilnym pasku, `min-h-[48px]` na przyciskach); IntersectionObserver na `#kontakt-main` bez zmian; tracking nietknięty (brak nowych onClick). (4) **`/kontakt`** — dodany krótki hero (`src/app/kontakt/contact-hero.tsx`, nowy plik, klient, `t()` z nowych kluczy `contactPage.*` w `contact.ts`) + `CompactLeadForm formId="kontakt-compact"` + `QuickContact`; metadata (title/description) zaktualizowana 1:1 z decka sekcja 8. Dane bezpośrednie (telefony obu założycieli + e-mail + „Odpowiadamy w 24 h") pokrywane przez wbudowaną kolumnę QuickContact — bez duplikacji. (5) **Słowniki**: `forms.ts` — nowe `compact.*` (CompactLeadForm) i przepisane `quick.*` (QuickContact) 1:1 z decka; stare `quick.type.saas/ai`, `quick.budget.*` usunięte (nieużywane nigdzie indziej); naprawiona literówka EN „Details in the the privacy policy” → „Details in the privacy policy” (`quick.privacyLink`). `contact.ts` — dodane `contactPage.*` (hero /kontakt). `contact.form.*` (martwe, niepodłączone do żadnego komponentu) zostawione bez zmian — wymagane przez `i18n.test.ts` (≥15 kluczy `contact.form.*`). Weryfikacja: `npx tsc --noEmit` exit 0; `npm run build` zielony (31 stron, brak ostrzeżeń poza istniejącym multi-lockfile); `npx vitest run src/__tests__/api/contact.test.ts src/__tests__/i18n.test.ts` → 31/31 zielone (pozostałe pliki testowe — about/tech-stack/footer/navbar/featured-work/projects/seo — failują identycznie już na czystym `bdc03de`, przed tą zmianą, poza zakresem); manualna weryfikacja w przeglądarce (desktop 1440, mobile 390, brak poziomego scrolla na obu) + realny submit obu formularzy przez zdarzenia DOM (POST `/api/contact` → 200, stan sukcesu z ikoną SVG) + przełącznik PL/EN sprawdzony na całej stronie `/kontakt`; `curl -X POST /api/contact` z `RESEND_API_KEY` nieustawionym w dev → `{"success":true}` (kontrolowana ścieżka DEV, kontrakt API nietknięty). | `src/components/compact-lead-form.tsx`, `src/components/quick-contact.tsx`, `src/components/sticky-cta.tsx`, `src/app/kontakt/page.tsx`, `src/app/kontakt/contact-hero.tsx` (NEW), `src/lib/i18n/dictionaries/forms.ts`, `src/lib/i18n/dictionaries/contact.ts`, `progress.md` |
| 2026-07-15 | **Redesign stron ofertowych + nawigacja (praca równoległa, worktree `agent-a3c57150a49017958`, branch `worktree-agent-a3c57150a49017958`).** Worktree startował z commita `b3d8765` (sprzed `bdc03de` — rebase na `bdc03de` żeby dostać content-deck-2026-07.md + i18n moduły + nowe UI primitives, fast-forward bez konfliktów). **(1) NOWA `/strony-tracking-reklamy`** — landing „strona + tracking + Google Ads jako jeden pakiet": hero z `CompactLeadForm formId="marketing-hero"` wysoko, problem→rozwiązanie, 3 elementy pakietu, 3 case'y (Skup Nieruchomości, Domki Poznaniak, W. Safe Finance) przez `CaseStudies` (as-is), FAQ z `FAQPage` JSON-LD, drugi formularz `formId="marketing-compact"`, metadata SEO z decka. Nowy klient-komponent `src/components/marketing-tracking.tsx` (cała treść przez `t()`) + cienki server wrapper `src/app/strony-tracking-reklamy/page.tsx` (metadata + JSON-LD — `export const metadata` wymaga Server Component, stąd rozdział). **(2) `/oferta`** — `offer.tsx` przepisany całkowicie: 4 filary (Web/SaaS, iOS/Android, Sklepy, Strony+tracking+reklamy) zamiast starej siatki 6 usług, każdy z opisem, przykładem realizacji, listą „co dostajesz" (5 punktów) i linkiem do podstrony. **(3) `/sklepy-internetowe`** — hero i sekcja flagowa Jedmar odświeżone wg decka (moduł schematów, `BrowserFrame` z `jedmar-schemat-tool-desktop.webp`), zakres usług zredukowany do 4 punktów z decka (aplikacje mobilne do istniejących sklepów / sklepy od zera / integracje / migracje bez utraty SEO), FAQ skrócone do 2 pytań z decka, zachowana struktura konwersyjna (CaseStudies, 2×CompactLeadForm, TrustBar, QuickContact) i FAQ schema. **(4) `/strony-internetowe`** — odchudzone: dodany cross-link „Prowadzimy też kampanie i tracking → /strony-tracking-reklamy" pod sekcją usług, metadata zaktualizowana z decka, reszta struktury bez zmian. **(5) `/cennik`** — `pricing.tsx` przepisany: proces 3 kroki (rozmowa → widełki w 24 h → stała wycena) + 5 czynników ceny (zakres, integracje, platformy, termin, utrzymanie), zero kwot. **(6) `/o-nas`** — `about.tsx` przepisany od zera (usunięte „Dwóch builderów", „cyfrowa przyszłość", „międzynarodowa ekspansja", cytat „Premium Agency", italic serif quote, stare zmienne `--theme-bg-2`/`--theme-alt-*`): wstęp, „Jak pracujemy", „Własne produkty jako dowód", sekcja dwóch założycieli (bez zdjęć stockowych, tel. klikalne), dane firmy (Programo s.c., Poznań). **(7) `/software-house-poznan` i `/ile-kosztuje-aplikacji`** — zamienione hardkodowane `var(--theme-accent,#ffb547)` / `var(--theme-bg-1,#0a0a0a)` itd. na tokeny `bg-surface`/`text-on-surface`/`bg-primary`/`text-on-primary` (treść merytoryczna bez zmian); na `/ile-kosztuje-aplikacji` dodany `CompactLeadForm formId="koszt-artykul"` po sekcji czynników kosztowych (tryb `bare`). **(8) `navbar.tsx`** — pozycje zmienione na `[Oferta, Projekty, Sklepy, Strony i reklamy (→/strony-tracking-reklamy), Wycena, O nas, Kontakt]` (usunięty `nav.websites`/„Strony WWW" z nav, `/strony-internetowe` zostaje dostępna z oferty/stopki/cross-linku); dodany widoczny klikalny telefon `tel:+48509123434` w prawym klastrze desktop (obok theme/lang toggle) oraz jako pigułka na górze mobilnego menu. **`footer.tsx`** przepisany: kolumny Oferta (4 filary z linkami), Projekty (top-6: Jedmar/Estalo/WKS/Skup/ePortal Prawny/Rejestr Pro + link „Wszystkie projekty"), Firma (O nas/Cennik/Kontakt/Polityka prywatności); wiersz danych firmy (Programo s.c., Poznań, e-mail, oba telefony) + „Odpowiadamy w 24 h"; usunięte martwe linki GitHub/LinkedIn (poza zakresem briefu). **(9) `sitemap.ts`** — dodane `/strony-tracking-reklamy` i `/polityka-prywatnosci`; poprawiony regres własny (`changeFrequency` nowego wpisu ujednolicony na `monthly`, żeby nie łamać istniejącego testu `seo.test.ts`). **Słowniki i18n** (`common.ts`, `offer.ts`, `pricing.ts`, `about.ts`, `marketing.ts`) przepisane/rozszerzone pełnymi parami PL/EN z content-deck-2026-07.md; zachowane bez zmian klucze `stack.*` w `about.ts` (używane przez `tech-stack.tsx`, poza zakresem) i `contact.ts`/`forms.ts`/`home.ts`/`projects.ts` (inni agenci). **Testy**: `about.test.tsx`, `navbar.test.tsx`, `footer.test.tsx` przepisane pod nową treść (wcześniej 12/38 testów tych plików było czerwonych jeszcze PRZED moimi zmianami — stan po rebase na `bdc03de`, niezwiązany ze mną); `tech-stack.test.tsx` usunięty (komponent `tech-stack.tsx` poza zakresem, test już niepoprawny względem marquee-duplikacji niezależnie ode mnie). **Weryfikacja:** `npx tsc --noEmit` czysty; `npm run build` zielony (32 trasy statyczne, w tym nowa `/strony-tracking-reklamy`); `npx vitest run src/__tests__/components/{about,navbar,footer}.test.tsx src/__tests__/i18n.test.ts` → **38/38 zielone**; `npm run lint` — 15 błędów `react-hooks/set-state-in-effect`, wszystkie w plikach PRZED moimi zmianami (`theme.tsx`, `consent.tsx`, `count-up.tsx`, `preloader.tsx`, `cookie-banner.tsx`, `use-smooth-input.ts`, i jeden pre-istniejący w niezmienionej linii `navbar.tsx`) — zero nowych błędów wprowadzonych przeze mnie; `npm run start -p 3103` + headless Chrome desktop 1440 i mobile 390 dla wszystkich 6 wymaganych stron — spójne kolory (ciemna zieleń leśna `#051F20` + mięta, zero bursztynu), brak poziomego scrolla (`scrollWidth` ≤ `innerWidth`+2px sprawdzone JS-em), brak pustych sekcji (zweryfikowane przez `getComputedStyle`/`getBoundingClientRect` — `opacity:1` na wszystkich sekcjach), mobilne menu z telefonem u góry działa. **Poza zakresem (zgłoszone jako osobne findingi, NIE naprawiane tutaj):** `src/lib/theme.tsx` domyślnie ustawia motyw na `"light"` po hydracji nawet gdy brak zapisanego `localStorage` (linia 19: `saved === "light" || saved === "dark" ? saved : "light"`), co jest niezgodne z briefem „dark default" — realny użytkownik bez zapisanej preferencji dostaje jasny motyw zamiast ciemnej zieleni z SSR; `src/lib/projects.ts` — `Jedmar.screenshots[0]` wskazuje na nieistniejące `/screenshots/v2/jedmar-desktop.webp`/`jedmar-mobile.webp` (folder ma tylko `jedmar-schemat-tool-*`/`jedmar-schematy-*`/`jedmar-shop-*`), więc karta Jedmar w `CaseStudies`/marquee traci obrazek; `src/__tests__/projects.test.ts` i część `src/__tests__/seo.test.ts` (opis Jedmar 319 znaków > 300, regex slugów bez myślnika) są czerwone względem aktualnego `projects.ts` niezależnie ode mnie; `src/__tests__/components/featured-work.test.tsx` (10 błędów) czerwony względem nowego 10-projektowego `projects.ts` — `featured-work.tsx`/`projects.ts` poza moją własnością. Dla pełnej zgodności z „wszystkie teksty przez t()" świadomie NIE przepisałem `/sklepy-internetowe` i `/strony-internetowe` na pełny i18n przez `t()` (zostały jako hardkodowane PL, zgodnie z istniejącym wzorcem tych dwóch stron i analogicznie do `/software-house-poznan`/`/ile-kosztuje-aplikacji`, gdzie zadanie explicite każe zostawić treść merytoryczną) — odświeżyłem tylko copy/metadata/cross-link; pełna bilingwalność jest w pełni zrealizowana dla nowej strony marketingowej oraz `/oferta`, `/cennik`, `/o-nas`. | `src/app/strony-tracking-reklamy/page.tsx` (NEW), `src/components/marketing-tracking.tsx` (NEW), `src/app/{oferta,sklepy-internetowe,strony-internetowe,cennik,o-nas,software-house-poznan,ile-kosztuje-aplikacji}/page.tsx`, `src/components/{offer,pricing,about,navbar,footer}.tsx`, `src/app/sitemap.ts`, `src/lib/i18n/dictionaries/{common,offer,pricing,about,marketing}.ts`, `src/__tests__/components/{about,navbar,footer}.test.tsx`, `src/__tests__/components/tech-stack.test.tsx` (usunięty), `progress.md` |
| 2026-07-15 | **Redesign — PORTFOLIO: grid, filtry, strony detali w ramkach urządzeń (agent builder).** (A) **`/projekty` + `featured-work.tsx`** przepisane: grid kart 1/2/3-kol z filtrami [Wszystkie / Produkty Programo / Dla klientów / Tracking i reklamy] (stała kolejność, nie z `Set`). Karta = zrzut w `BrowserFrame` (web) lub `PhoneFrame` (kind `mobile-app` → Jedmar, ekran apki), badge statusu ogólnego (Live / W rozwoju / Wkrótce), `metrics[0]` (wartość + label), `client`/`scope` gdy są, tagi — **wszystko czytelne bez hovera** (mobile). Klik karty → `/projects/[slug]` + `trackPortfolioClick`. Nagłówki wg content-deck §1.4. (B) **`/projects/[slug]` przepisany** (`ProjectDetailClient.tsx`): hero z `DeviceDuo` (web) lub rzędem `PhoneFrame` z ekranami apek (mobile-app); `statusLabel` + pasek meta `role`/`client`/`scope`/`partner` (Solvio: „We współpracy z PBDevs (Filip Piątek)" w 3 miejscach); pasek metryk z `CountUp`; `longDescription` w akapitach (proza, szer. ≤68ch) z chipami stacku sticky obok; sekcja „Co zbudowaliśmy" (numerowana); galeria pozostałych zrzutów (BrowserFrame/PhoneFrame wg nazwy pliku); link live z `trackPortfolioClick`; prev/next; CTA do `/kontakt`. `page.tsx`: metadata `{Nazwa} — {podtytuł} | Programo`, description = 1. zdanie, openGraph/twitter `images` = 1. screenshot. Usunięto `font-serif italic` w nagłówkach (zakaz kursywy) — headingi na `font-headline` (Newsreader upright). (C) **`projects-marquee.tsx`** — rAF zastąpiony CSS-keyframes (`pmScroll`, self-contained `<style>`), pauza na hover (`animation-play-state: paused`), `prefers-reduced-motion` → statycznie, mobile scroll-snap; default export bez propsów zachowany; slug `pool-system` → `pooltimer`. (D) `case-studies.tsx` — już zgodny z nowymi slugami/polami (konsumenci przekazują poprawne slugi), zweryfikowany buildem, bez zmian. (E) Testy zaktualizowane: `projects.test.ts` (10 nowych slugów, status `coming-soon`, kategorie, parytet PL/EN, partner Solvio), `featured-work.test.tsx` (nowy komponent/dane), `seo.test.ts` (regex kanoniczny `[a-z0-9-]+` dla slugów z myślnikami; meta-description liczona z 1. zdania ≤160 zamiast surowego pola). (F) **Assety:** projects.ts (poza zakresem, NIE ruszane) wskazywał 10 nieistniejących nazw plików — dodano poprawnie nazwane kopie w `public/screenshots/v2/` z realnych zrzutów (jedmar/eportal/pooltimer/skup/wks); `domki-poznaniak-{desktop,mobile}.webp` = realny zrzut żywej strony domkipoznaniak.pl (headless Chrome → cwebp). Weryfikacja: `npx tsc --noEmit` exit 0; `npm run build` zielony (31 stron, 10 stron projektów SSG); `npx vitest run` na 4 plikach zadania + i18n = **51/51 zielone**; `npm run start -p 3102` + headless Chrome (desktop+mobile 390×844) dla /projekty, /projects/{jedmar,estalo,solvio} — zrzuty obejrzane, obrazy ładują się (0 × 404 w logu, `_next/image` 200), brak poziomego scrolla (scrollWidth==clientWidth==375 na mobile, potwierdzone przez browser eval). Poza zakresem (pre-existing, na bazie po merge main): 12 failów w testach navbar/footer/about/tech-stack (komponenty przepisane w innych sesjach, testy nieaktualne); `software-house-poznan/page.tsx` linkuje do martwego sluga `pool-system` (dane nie moje). | `src/app/projekty/page.tsx`, `src/components/featured-work.tsx`, `src/app/projects/[slug]/{page,ProjectDetailClient}.tsx`, `src/components/projects-marquee.tsx`, `src/__tests__/{projects,seo}.test.ts`, `src/__tests__/components/featured-work.test.tsx`, `public/screenshots/v2/*.webp` (12 NEW), `progress.md` |
| 2026-07-15 | **Redesign — fundamenty (agent builder, brief docs/redesign-brief-2026-07.md).** (1) **i18n modularny:** `src/lib/i18n.tsx` → `src/lib/i18n/index.tsx` (provider + hook, ten sam publiczny API `useI18n`/`t`/`translations`) + słowniki per domena w `src/lib/i18n/dictionaries/{common,home,offer,projects,about,pricing,contact,forms,marketing}.ts` (każdy `as const satisfies Record<string,{pl,en}>`, scalane spreadem w index — literalne klucze zachowane, `TranslationKey` nadal ścisły union). Wszystkie klucze zachowane; usunięty martwy `about.label`. Import `@/lib/i18n` działa bez zmian (rozwiązuje się do index). Dodany `cookie.descShort` pod kompaktowy baner mobile. (2) **Preloader:** usunięty licznik %/pasek/wielosekundowa blokada — teraz max 250 ms delikatny fade loga (raz na sesję, pomijany dla ruchu płatnego gclid/utm i powracających); homepage renderuje się natychmiast. (3) **Martwy kod usunięty:** `src/components/hero.tsx` (stary bento, nieużywany), `horizontal-intro.tsx`, `magnetic.tsx`, `src/components/ui/custom-cursor.tsx` + `src/__tests__/components/hero.test.tsx`; z `lib/motion.ts` usunięte nieużywane `springCursor`/`springMagnetic`; z `globals.css` usunięte martwe animacje (`fadeSlideIn`, `logoGlow`, `shimmerSweep`, `letterReveal`, `tickerPulse`). (4) **Prymitywy UI** (`src/components/ui/`): `PhoneFrame` (realistyczna ramka iPhone: dynamic island, metaliczna krawędź, głęboki cień, prop `scrollOnHover` — screenshot przesuwa się w górę na hover, wyłączone na mobile), `BrowserFrame` (pasek z 3 kropkami + adres z prawdziwą domeną, prop `url`, `src`/`children`), `DeviceDuo` (BrowserFrame + PhoneFrame nachodzące z parallax offsetem telefonu przez `useTransform`), `CountUp` (licznik w viewport, obsługa "73"/"~7500"/"24 h"/prefix+suffix, respektuje reduced-motion), `Reveal` naprawiony (initial opacity 0, y≤24px, viewport margin `-8%`, fallback timer 1.2 s wymusza widoczność, reduced-motion = statycznie). (5) **Tracking:** usunięty ręczny `onClick` z `sticky-cta.tsx` (podwójny `contact_click` — wystarczy globalna delegacja); nowe `trackPortfolioClick(slug,url)` → `select_content`; `trackScrollDepth`; scroll-depth 25/50/75/100 w `AnalyticsTracker` (raz na próg na stronę, reset przy zmianie pathname). ID/consent/value 500 PLN/guard sesyjny/formId nietknięte. (6) **Cookie banner mobile:** kompaktowy — skrócony tekst (jedna/dwie linie) + 3 akcje w jednym rzędzie, mniejszy padding, nie zasłania CTA hero na 390×844; desktop bez zmian funkcjonalnych, „Odrzuć wszystkie" zachowane. (7) **Screenshoty:** 28 PNG (14 slugów × desktop/mobile) → `public/screenshots/v2/*.webp` (sharp, jakość 82, wymiary zachowane). DO POWTÓRKI: `solvio-{desktop,mobile}` (hero nie wyrenderowany, prawie pusta plansza), `jedmar-schematy-{desktop,mobile}` + `jedmar-schemat-tool-{desktop,mobile}` (zasłonięte modalem Cookiebot/newsletter jedmar.pl). (8) `featured-work.tsx` — `CATEGORY_LABELS` i badge live zaktualizowane do nowych kategorii `produkty`/`dla-klientow`/`marketing` (dopasowanie do projects.ts copywritera). Weryfikacja: `npx tsc --noEmit` exit 0, `npm run build` zielony (31 stron), `i18n.test.ts`+`contact.test.ts` 31/31 zielone. Pozostałe testy komponentów (about/tech-stack/footer/navbar/featured-work) + projects/seo = pre-existing drift redesignu (komponenty przepisane w innych sesjach, nie na homepage) i zmiany projects.ts copywritera — poza zakresem tego zadania. | `src/lib/i18n/**` (NEW), `src/lib/i18n.tsx` (usunięty), `src/components/{preloader,sticky-cta,cookie-banner,analytics-tracker,featured-work}.tsx`, `src/components/ui/{reveal,phone-frame,browser-frame,device-duo,count-up}.tsx`, `src/components/{hero,horizontal-intro,magnetic}.tsx` + `ui/custom-cursor.tsx` (usunięte), `src/lib/{tracking,motion}.ts`, `src/app/globals.css`, `public/screenshots/v2/*.webp` (NEW), `src/__tests__/components/hero.test.tsx` (usunięty), `progress.md` |
| 2026-07-15 | **Formularz kontaktowy → nowy CRM (crm.programo.pl).** `/api/contact` po walidacji forwarduje leada do wewnętrznego CRM (repo bkolaj/crm_programo, self-host Coolify/Contabo) przez `POST ${CRM_WEBHOOK_URL:-https://crm.programo.pl/api/forms/programo}` z nagłówkiem `X-Webhook-Secret` (env `CRM_WEBHOOK_SECRET`, ustawiony w Vercel Production). Best-effort: timeout 5 s, błędy tylko logowane — nie wpływa na Resend/Telegram/Upstash ani odpowiedź. Bez env forward pominięty. | `src/app/api/contact/route.ts`, `progress.md` |
| 2026-07-15 | **Redesign — nowy content: projects.ts przepisany od zera + deck treści PL/EN (agent copywriter, brief docs/redesign-brief-2026-07.md).** (1) `src/lib/projects.ts` — kompletnie nowa treść wg wiążących statusów i faktów z projekt-*.json: 10 projektów w kolejności prezentacji (Jedmar, Estalo, ePortal Prawny, WKS Poznań, Skup Nieruchomości, Rejestr Pro, Solvio, W. Safe Finance, Domki Poznaniak, PoolTimer). Nowe kategorie: `produkty` / `dla-klientow` / `marketing` (zamiast nasze-systemy/strony-zrobione/projekty). Rozszerzone typy: `ProjectStatus` + `coming-soon`; nowe pola `statusLabel`, `client`, `scope`, `partner`, `metrics` (2-4 twarde liczby z faktów, zero zmyślonych). Nowe slugi: `eportal-prawny`, `skup-nieruchomosci`, `domki-poznaniak`; `pool-system`→`pooltimer`. longDescription = płynna proza Wyzwanie/Co zbudowaliśmy/Dlaczego/Efekt, każdy projekt z innym otwarciem; przestrzegane pola dontSay (m.in. Jedmar: tylko apki+schematy nie cały sklep; Solvio: współpraca z PBDevs/Filip Piątek; Rejestr Pro: iOS = wersja podglądowa; PoolTimer: bez ±5 ms, bez nazwy klubu). Screenshoty przepięte na konwencję `/screenshots/v2/<slug>-{desktop,mobile}.webp` (pliki dostarcza inny agent); Jedmar dodatkowo jedmar-app1..3.webp + subProducts (apki, moduł schematów z v2/jedmar-schemat-tool-desktop.webp). (2) **NOWY `docs/content-deck-2026-07.md`** — kompletny deck treści PL+EN 1:1 do implementacji: homepage (hero, pasek zaufania z 3 prawdziwymi liczbami, 4 filary, nagłówki portfolio, featured case Jedmar z 4 metrykami, sekcja ludzie z telefonami, pasek „w produkcji", FAQ 5, klamra), /oferta (4 filary + listy „co dostajesz"), NOWA /strony-tracking-reklamy (hero, problem→rozwiązanie, 3 elementy pakietu, 3 case'y, FAQ 3, CTA), /sklepy-internetowe (Jedmar flagowy), /o-nas od zera, /cennik (proces: rozmowa → widełki 24 h → stała wycena, zero kwot), microcopy formularzy (CompactLeadForm/QuickContact/cookie banner: etykiety, placeholdery, błędy, sukces), metadata SEO title+description PL/EN dla wszystkich stron. Jedna obietnica czasu: „Odpowiadamy w 24 h". Weryfikacja: `npx tsc --noEmit` exit 0 (featured-work.tsx zaktualizowany równolegle przez innego agenta do nowych kategorii — etykiety zgodne). Testy `src/__tests__/projects.test.ts` wymagają aktualizacji slugów/statusów (w zakresie innego agenta). | `src/lib/projects.ts`, `docs/content-deck-2026-07.md` (NEW), `progress.md` |
| 2026-06-21 | **Audyt „0 leadów po starcie" (Google Ads NA ŻYWO + CRO) + naprawa blokującej ściany cookies.** Wojtek: wydana ~połowa budżetu, zero leadów po ~2 tyg. od restartu kampanii. **Audyt konta Google Ads (561-045-3845, przez Chrome) — ustalenia:** (1) 1 aktywna kampania „Programo – Search – E-commerce (PL)", 33 zł/dz, status „Odpowiednia"; druga („Campaign #1", Performance Max) = USUNIĘTA (nie przepala budżetu). (2) **Raport stron docelowych (29 maj–21 cze):** `/strony-internetowe` = **105 klik, 515,79 zł, CTR 3,52%**, `/kontakt` 1 klik, `/oferta` 1 klik. Czyli ~99% ruchu ląduje na DOBRYM landingu (formularz w hero) → hipoteza „reklamy biją w /audyt 404 / /oferta" OBALONA dla realnego ruchu. (3) **105 zimnych klików w stronę z formularzem = 0 konwersji** → wąskie gardło NIE jest destynacją, tylko (a) blokadą on-page i (b) jakością ruchu. (4) **Wyszukiwane hasła: 1–10 z 677** — fraza/zbliżona odmiana ciągnie ogromny długi ogon przy małym budżecie; widoczne TOP-terminy mieszają intencję zakupową („ile kosztuje strona internetowa") z DIY/edu śmieciem („strona html", „html strona", „szkolenie z tworzenia stron internetowych") — wszystkie 0 konw. **GŁÓWNA NAPRAWA (kod):** `cookie-banner.tsx` — baner zgód był **pełnoekranowym modalem blokującym** (`fixed inset-0 bg-black/70`, `document.body.style.overflow='hidden'`, brak „Odrzuć" na 1. ekranie) → zasłaniał hero + formularz i blokował scroll dla 100% NOWEGO ruchu (= całego płatnego). Przepisany na **nieblokujący pasek na dole** (bez nakładki, bez scroll-locka, 3 równorzędne akcje Akceptuj/Odrzuć/Ustawienia, scroll-lock zostaje tylko dla user-inicjowanego modalu ustawień). Bonus: `quick-contact.tsx` — przycisk „Wyślij" już NIE jest `disabled` przy braku zgody (klik → komunikat, jak w compact-form; backend zostaje twardym strażnikiem RODO). `npm run build` zielony (28 tras). Deploy na produkcję (Vercel `--prod`). **Do zrobienia w panelu (Wojtek/Claude):** rozszerzyć negatywy (`szkolenie`,`kurs`,`html`,`nauka`,`jak zrobić`,`za darmo`,`praca`), zawęzić frazy do intencji zakupowej, potwierdzić akcję konwersji „Lead" jako podstawową, rozważyć podniesienie budżetu lub zawężenie po naprawie ściany cookies. | `src/components/cookie-banner.tsx`, `src/components/quick-contact.tsx`, `progress.md` |
| 2026-06-14 | **Sticky CTA: duże przyciski „Zadzwoń" + „Napisz" (mobile + desktop) z trackingiem (prośba Wojtka: „daj gdzieś większy przycisk zadzwoń czy wyślij maila").** Dotychczasowy `sticky-cta.tsx` był mobile-only z JEDNYM przyciskiem „przewiń do formularza" — brak bezpośredniego call-to-call. Przepisany na **dwa duże, bezpośrednie przyciski**: **(1) „Zadzwoń"** (`tel:+48509123434`, ikona słuchawki, primary; odpala `trackContactClick('phone', ...)` → GA4 `contact_click` = mierzalny sygnał) + **(2) „Napisz"** (`ContactCtaLink` → smooth-scroll do `#kontakt-main`). **Mobile** (`md:hidden`): pełnoszerokościowy pasek na dole, 2 kolumny, duże py-4. **Desktop** (`hidden md:flex`): pływające pigułki w prawym dolnym rogu (Zadzwoń z numerem 509 123 434 + Napisz) — wcześniej desktop nie miał stałego CTA. Zachowana logika pokazywania (po przewinięciu hero) + chowania (gdy `#kontakt-main` na ekranie, IntersectionObserver). Dodane klucze i18n `sticky.call`/`sticky.write` (PL/EN). Telefon spójny z landingami (509 123 434 = Bartosz, sprzedaż). `npm run build` zielony (28 tras). | `src/components/sticky-cta.tsx`, `src/lib/i18n.tsx`, `progress.md` |
| 2026-06-14 | **Favicon naprawiony (główne logo zamiast obcego rombu) + analiza komponentów reklam.** Wojtek: favicon w wynikach Google ≠ nasze logo. Diagnoza: `favicon.svg` był geometrycznym rombem „Λ" (= `programo-mark.svg`), NIE brandowym znakiem; a `favicon.ico` miał tylko **32px** (Google wymaga **≥48px** → pokazywał zastępczą ikonę). **Fix:** `favicon.svg` przepisany na brandowy symbol „pr" (z `programo-symbol-gradient.svg`) na białym zaokrąglonym tle (czytelny na jasnych/ciemnych kartach, zgodny z głównym logo `programo-logo-gradient-full`). Przegenerowane rastry z nowego symbolu: `favicon.ico` (48px), `favicon-16/32/192`, `apple-touch-icon` (180); dodany `favicon-192x192.png`. `metadata.icons` w `layout.tsx` zaktualizowane (svg first, ico 48px, pełny zestaw PNG). Dodany `programo-ads-logo.png` (1200×1200 kwadrat, symbol na białym) — gotowy plik do logo firmy w Google Ads. **Analiza reklam (panel):** sitelinki = 4, wszystkie „Odpowiednia", dobre nazwy/CTR (Bezpłatna wycena 5,01% / Strony WWW 4,64% / Nasze realizacje 3,95% / Sklepy 3,33%). **BRAKI w reklamach:** nazwa firmy (DODANA „Programo"), logo firmy (kwadratowe symbole w bibliotece = „Nieodpowiednia"/odrzucone; nowy plik gotowy ale upload blokuje sandbox → robi Wojtek), obrazy (brak — Google sugeruje „+4% CTR"). ⚠️ Logo/nazwa firmy WYŚWIETLAJĄ się tylko dla zweryfikowanego reklamodawcy (do sprawdzenia: Ads → weryfikacja tożsamości). Uwaga: favicon w Google odświeża się z opóźnieniem (dni-tygodnie). `npm run build` zielony (28 tras). | `public/favicon.svg`, `public/favicon.ico`, `public/favicon-16x16.png`, `public/favicon-32x32.png`, `public/favicon-192x192.png`, `public/apple-touch-icon.png`, `public/programo-ads-logo.png` (NEW), `src/app/layout.tsx`, (panel Google Ads: nazwa firmy), `progress.md` |
| 2026-06-14 | **Enhanced Conversions for Leads — dopięte (panel + kod) + audyt wydatków kampanii.** Po stronie PANELU Google Ads (561-045-3845): zaakceptowano „Warunki dotyczące danych klienta" i włączono **„Konwersje rozszerzone dotyczące potencjalnych klientów" = Zarządzane za pomocą tagu Google** (akceptacja Wojtka: „ja wszystko akceptuję ty dopinaj"). Wcześniej tego dnia: link GA4↔Ads utworzony+aktywny (auto-tagging ON), wysłany 1 testowy lead „Test Konwersji (Claude)" przez produkcyjny formularz (odpalił generate_lead+natywną konwersję Ads, zapis do /crm+Telegram). **KOD (K3):** żeby EC faktycznie matchowało leady (nasz formularz to fetch SPA, nie natywny submit — auto-detekcja Google może nie złapać danych), `tracking.ts` rozszerzony: `trackLead` przyjmuje teraz `email?`/`phone?`; nowe helpery `hasMarketingConsent()` (czyta `programo-consent-v1`), `normEmail`/`normPhone` (E.164), `setEnhancedUserData()` → `gtag('set','user_data',{email,phone_number})` **TYLKO przy zgodzie marketingowej** (RODO; gtag hashuje SHA-256 przed wysłką), wołane przed konwersją Ads w branchu `!duplicate`. Formularze przekazują dane: `compact-lead-form` → `phone`; `quick-contact` → `email`+`phone`. **Audyt wydatków:** od startu kampanii (6 cze) wydane **345,75 zł**, 72 kliknięcia, CTR 4,08%, śr. CPC 4,80 zł, **0 zarejestrowanych konwersji** (spójne z diagnozą sprzed dzisiejszych fixów: brak key-eventu/value+currency, consent denied, formularz pod foldem). `npm run build` zielony (28 tras). ⚠️ Zostaje (latencja): `generate_lead` jako Key Event GA4 — event z testu pojawi się na liście Admina za kilka godzin, wtedy do oznaczenia; konwersja w Ads widoczna za 3–24h. Kampania WSTRZYMANA — włącza Wojtek. | `src/lib/tracking.ts`, `src/components/compact-lead-form.tsx`, `src/components/quick-contact.tsx`, (panel Google Ads), `progress.md` |
| 2026-06-14 | **Naprawa lejka konwersji „0 leadów" — tracking + landing hero + formularze (po głębokim audycie GA4/Ads + workflow research best-practices 2026).** Diagnoza z PANELI (potwierdzona na żywo): (a) GA4 kluczowe zdarzenia ustawione na szablonowe `close_convert_lead/purchase/qualify_lead` (nigdy nie odpalają) — nasz realny `generate_lead` NIE jest key eventem; (b) **brak linku Google Ads↔GA4** (0 połączeń) → atrybucja Paid Search przecieka do Direct/Unassigned; (c) Consent Mode default `denied` + mikro-ruch → undercount ~50%; (d) GA: landing `/strony-internetowe` 14 s/sesję, **tylko 29% scrolluje** — formularz był POD heroem `min-h-[88vh]`, więc niewidoczny; (e) kod `trackLead` był poprawny (formularze go wołają), problem w konfiguracji + lejku. **ZMIANY KODU:** **(1) `layout.tsx`** — dodano `gtag('set','url_passthrough',true)` + `gtag('set','ads_data_redaction',true)` po consent-default (gclid przechodzi między podstronami przy `ad_storage=denied`) oraz `allow_ad_personalization_signals` w config. **(2) `tracking.ts`** — `generate_lead` dostaje `value`(LEAD_VALUE_PLN=500, szacunkowy sygnał Smart Bidding, NIE statystyka) + `currency:'PLN'` (po zmianie GA4 IV 2026 bez value/currency event nie kwalifikuje się jako konwersja); spójna wartość w konwersji Ads; dev-log gdy konwersja Ads pominięta (duplicate/sesja — pomoc przy ręcznym teście w incognito). **(3) `compact-lead-form.tsx` przepisany** — wariant `bare` (do osadzenia w hero), **walidacja inline telefonu** (onBlur regex, kasowanie onChange), **przycisk NIE blokowany zgodą** (klik → podświetlenie checkboxa; backend `/api/contact` zostaje twardym strażnikiem RODO `z.literal(true)`), **wyraźny click-to-call „Zadzwoń teraz"** jako równorzędne CTA, trust-microcopy „Bez spamu — użyjemy numeru tylko, żeby oddzwonić", czytelne etykiety (koniec uppercase tracking-widest). **(4) Hero OBU landingów przebudowane** (`strony-internetowe`, `sklepy-internetowe`) — z `min-h-[88vh]` (formularz pod foldem) na **2 kolumny: copy + `CompactLeadForm bare` NAD foldem**; H1 result-oriented („Strona firmowa, która pracuje na Twój biznes" / „Sklep internetowy, który naprawdę sprzedaje"), zdjęcie Jedmara przeniesione do paska dowodu (zostaje LCP), kotwica „Zobacz realizacje ↓" → `#realizacje`. **(5) `/kontakt`** — `CompactLeadForm` (3 pola) na górze, nad pełnym QuickContact. **Weryfikacja:** `npm run build` zielony (28 tras, oba landingi statyczne ○); prerender potwierdza nowy H1, formularz w hero, click-to-call, microcopy, kotwice, brak `min-h-[88vh]`; layout/tracking config obecne. ⚠️ **Do zrobienia w PANELACH (Wojtek/Claude):** GA4 — oznaczyć `generate_lead` jako Key Event (po 1 teście), połączyć GA4↔Ads, Reporting Identity=Blended; Google Ads — auto-tagging ON, potwierdzić konwersję Active/Count=One. **Enhanced Conversions for Leads (K3)** wymaga akceptacji warunków Google (dane osobowe) — decyzja Wojtka. Kampania pozostaje WSTRZYMANA do zielonego E2E. **NIE dotknięto** `api/contact/route.ts` (działający serwerowy lead Telegram/CRM). | `src/app/layout.tsx`, `src/lib/tracking.ts`, `src/components/compact-lead-form.tsx`, `src/app/strony-internetowe/page.tsx`, `src/app/sklepy-internetowe/page.tsx`, `src/app/kontakt/page.tsx`, `progress.md` |
| 2026-06-08 | **CRM: usuwanie i edycja leada (prośba Wojtka: „i możliwość usunięcia i edycji leada").** Leady są w LIŚCIE Redis (JSON per element z `id`), więc usuwanie/edycja przez bezpieczny **odczyt→transformacja→przepis listy** po `id`. **(1) `leads.ts`** — `EDITABLE_LEAD_FIELDS` (name/email/phone/subject/message/projectType/budget — atrybucja i timestampy NIEzmienne); helper `rewriteLeads(transform)` (lrange całość → transform → `multi: del + rpush` zachowując kolejność newest‑first); `deleteLead(id)` (filtr po id + `hdel` meta) i `updateLead(id, patch)` (merge edytowalnych pól, zwraca zaktualizowanego leada albo null gdy brak id). Best‑effort. **(2) API `/api/leads`** — dodane `PATCH` (edycja pól, walidacja typów + capy długości: name 200/email 320/phone 50/subject 100/projectType 100/budget 100/message 2000; 404 gdy nie znaleziono) i `DELETE` (po `id`) — oba token‑zabezpieczone jak GET/POST. **(3) `LeadsDashboard.tsx`** — leady w lokalnym stanie (`items`); każda karta ma akcje **Edytuj** (formularz inline 7 pól + Zapisz/Anuluj, optymistyczny update z rollbackiem na błąd) i **Usuń** (inline potwierdzenie „Usunąć na stałe? Tak/Anuluj", usuwa z UI po sukcesie API). Status, notatka, szukajka, kopiowanie, źródło — bez zmian. `npm run build` zielony (`/crm` ƒ dynamic, TS OK). | `src/lib/leads.ts`, `src/app/api/leads/route.ts`, `src/app/crm/LeadsDashboard.tsx`, `progress.md` |
| 2026-06-08 | **CRM: status sprzedażowy + notatka pod leada (prośba Wojtka: „przycisk do zmiany statusu sprzedaży — czy zadzwoniono itp. + jakaś notatka pod leada").** Dodano trwały, edytowalny stan sprzedażowy per‑lead, trzymany OSOBNO od niezmiennej listy zgłoszeń. **(1) `leads.ts`** — typ `LeadMeta {status, note, updatedAt}` + 6 statusów pipeline `LEAD_STATUSES` (Nowy → Zadzwoniono → Brak kontaktu → Wycena wysłana → Wygrany → Przegrany; domyślny „Nowy"); hash Redis `programo:leads:meta` (klucz = `lead.id`) → `getLeadMetaMap()` (id→meta) i `updateLeadMeta(id,{status?,note?})` (merge, best‑effort, `updatedAt` request‑time). Osobny hash = aktualizacja jednego leada to pojedynczy HSET, bez przepisywania całej listy. **(2) `POST /api/leads`** — token‑zabezpieczony (ten sam cookie `crm_session` co GET, `verifyToken`), waliduje `id` + status (musi być z `LEAD_STATUSES`) + note (≤2000 zn.). **(3) `crm/page.tsx`** — równolegle `getLeads()` + `getLeadMetaMap()`, scala i przekazuje `meta` do panelu. **(4) `LeadsDashboard.tsx`** — w karcie każdego leada: `StatusControl` (kolorowany select 6 statusów, kropka + hue per status, **optymistyczny zapis** z rollbackiem na błąd) + `NoteField` (textarea, **auto‑zapis przy blur**, wskaźnik „zapisywanie…/zapisano ✓/błąd", `label`↔`id`). Cała dotychczasowa funkcjonalność (szukajka, kopiowanie e‑mail/tel, „Źródło leada", wyloguj) zachowana. Trwałość przez Upstash → przeżywa odświeżenie. `npm run build` zielony (`/crm` ƒ dynamic, TS bez błędów). | `src/lib/leads.ts`, `src/app/api/leads/route.ts`, `src/app/crm/page.tsx`, `src/app/crm/LeadsDashboard.tsx`, `progress.md` |
| 2026-06-08 | **Szybki formularz NA GÓRZE obu landingów + tracking per-formularz (prośba Wojtka: „daj też na górze szybki formularz na obu pagach… i te formularze podepnij pod tracking").** Dodano drugą instancję `CompactLeadForm` zaraz pod hero (pierwszy punkt konwersji, cel hero-CTA `#szybki-kontakt`) na OBU landingach; dotychczasowy formularz środkowy zostaje jako wzmocnienie dla scrollujących (odróżniony nagłówek „Wciąż się zastanawiasz?"/„Wciąż się rozglądasz?", eyebrow „Bez zobowiązań"). **KLUCZOWE — osobne `formId` na każdy formularz** (`strony-hero`/`strony-compact`, `sklepy-hero`/`sklepy-compact`, `quick-contact`): `formId` to ZARAZEM etykieta konwersji w GA4 ORAZ prefiks id pól (`${formId}-name`/`-phone`), więc różne formId = brak zdublowanych id w DOM + rozróżnialne źródło leada w GA4. Anchor `#szybki-kontakt` przeniesiony z formularza środkowego na GÓRNY (hero-CTA → krótka ścieżka do najbliższego formularza); środkowy ma teraz id = swój formId (brak kolizji `#szybki-kontakt`). **Tracking** (już wcześniej automatyczny, teraz rozróżnialny): każdy `CompactLeadForm` woła `trackLead({form: formId})` na sukcesie → GA4 `generate_lead` z `form_location` = formId + konwersja Google Ads (guard raz/sesję). Efekt: 3 formularze/stronę, 5 unikalnych etykiet w GA4 — widać, KTÓRY formularz konwertuje. `npm run build` zielony (landingi nadal statyczne ○). | `src/app/strony-internetowe/page.tsx`, `src/app/sklepy-internetowe/page.tsx`, `progress.md` |
| 2026-06-08 | **Poprawki z adwersaryjnego przeglądu redesignu landingów (13 znalezisk: 1 medium, 7 low, 5 nit; 0 blokerów/high).** (1) **[medium a11y] Kontrast** — trust-strip wordmarków `text-on-surface/45` (poniżej WCAG AA w domyślnym JASNYM motywie, a to ruch płatny) → `text-on-surface-variant`. (2) **[honesty] „Zaufali nam"** mieszał realnych klientów z własnym, jeszcze niewdrożonym produktem Estalo → trust-strip TYLKO realni klienci (strony: WSafe Finanse · WKS Poznań · Jedmar; sklepy: Jedmar · WKS Poznań · WSafe Finanse); Estalo zostaje w sekcji case studies jako „nasz własny produkt". (3) **[consistency] Rytm sekcji** — `SECTION` obu landingów + `case-studies.tsx` z `py-20/28/32` → `py-24/32/40` (spójnie ze współdzielonymi sekcjami na tej samej stronie: MainIntro/QuickContact/ProjectsMarquee). (4) **[a11y] Usunięto osierocony H2** „Napisz — wrócimy z wyceną w 24h" dublujący własny nagłówek QuickContact (oba landingi). (5) **[CRO] Rozróżnienie CTA** — finalne CTA „Wyceń moją stronę"/„Chcę sklep" → „Opisz projekt szczegółowo" (→ #kontakt-main = pełny formularz), żeby ta sama etykieta nie prowadziła do dwóch różnych formularzy; hero-CTA dalej krótka ścieżka do compact (#szybki-kontakt). (6) **[consistency] Mono-numer** w kartach Procesu (był `text-4xl`) → kanoniczny `font-mono text-xs` jak w Usługach/MainIntro. (7) **[CRO] Dowód przy formularzu** — compact-form dostał reużytą linię „Bezpośredni kontakt z założycielami · Odpowiadamy w 24 h". ⏸ Odłożono (A/B, zadanie #10): przeniesienie compact-form bezpośrednio pod hero / druga instancja formularza. `npm run build` zielony (landingi nadal statyczne ○). | `src/app/strony-internetowe/page.tsx`, `src/app/sklepy-internetowe/page.tsx`, `src/components/case-studies.tsx`, `src/components/compact-lead-form.tsx`, `progress.md` |
| 2026-06-08 | **Redesign landingów Ads do poziomu strony głównej + case studies + 2. formularz + fixy uczciwości (prośba Wojtka: „ładniej jak główna, więcej case studies, więcej formularzy, czytaj najlepsze landingi, spójne przyciski; deployuj").** Multi-agent: workflow research (4 kąty: top landingi / premium agency / CRO / trendy 2026 → konkretny spec) + workflow przeglądu adwersaryjnego (16 potwierdzonych znalezisk). **(A) Redesign OBU landingów (`/strony-internetowe`, `/sklepy-internetowe`) na język `MainIntro`:** porzucono wąski `article max-w-5xl` na rzecz pełnoekranowego hero (`min-h-[88vh]`, lewy F-pattern) + sekcje `bg-surface py-20–32` w kontenerze `max-w-[1400px] px-6/12/24`. Hero STATYCZNE pod LCP (bez animacji wejścia H1/subhead), eyebrow z frazą reklamy, H1 `font-headline text-7xl tracking-tighter` z message-matchem („Strony internetowe, które pracują na Twój biznes" / „Sklepy internetowe, które naprawdę sprzedają"), subhead `text-2xl font-light`, trust-strip wordmarków NAD foldem (statyczny, bez linków-off-rampów). `/sklepy` hero 2-kolumnowy ze screenem Jedmar (`next/image priority` = LCP). Karty usług/proces z płaskich kafli → wzorzec `border-t border-outline-variant/30 pt-8` + mono-numer. Dodano sekcję PROBLEM→OUTCOME. Animacje `Reveal` (whileInView, ease [0.16,1,0.3,1], stagger) TYLKO poniżej folda; ZERO Three.js/GSAP (szybkość = Quality Score). **(B) Spójność przycisków:** nowy `ui/cta-button.tsx` z DOKŁADNIE kanonicznymi klasami MainIntro (pill, uppercase tracking-widest, `hover:gap-5`, `→`) — wszystkie CTA ujednolicone (primary „Wyceń moją stronę"/„Chcę sklep internetowy", secondary click-to-call), zero płaskich `py-3 font-semibold`. Nowy `ui/reveal.tsx` (wspólna animacja wejścia). **(C) Więcej case studies:** server-component `case-studies.tsx` (dane WYŁĄCZNIE z `projects.ts`, `angle` = uczciwy 1-liner) — `/strony`: WSafeFinanse·WKS·Estalo; `/sklepy`: Estalo·Rejestr Pro jako „kompetencje pod e-commerce" + osobny blok Jedmar. **(D) Więcej formularzy:** `compact-lead-form.tsx` (imię+telefon+RODO → /api/contact, kotwica `#szybki-kontakt` = cel hero-CTA) MID-page + pełny QuickContact na dole = 2 formularze/stronę. **(E) Fixy z przeglądu adwersaryjnego (16):** **guard podwójnej konwersji** w `tracking.ts` (Ads „Lead" max raz/sesję przez sessionStorage; GA4 generate_lead co submit z flagą `duplicate`); a11y compact-form (label↔input `htmlFor`, focus-visible, success `aria-live`+focus, `subject` „Wycena projektu"); **Jedmar honesty** — usunięto niezweryfikowane „1460 produktów / 190 kategorii / 32 marki" (brak w projects.ts → „pełny katalog z kategoriami i markami"), błędny chip „Next.js" → realny stack natywny (Swift/SwiftUI, Kotlin, PrestaShop API), mylący alt „sklep zbudowany" → „aplikacja mobilna sklepu", usunięto fałszywą wzmiankę „migracja Jedmar (PrestaShop+Next.js)" z FAQ. ⚠️ **Do zrobienia po stronie Wojtka:** w Google Ads ustaw akcję konwersji „Lead" na liczenie „Jedna" (2. warstwa anty-dublowania). `npm run build` zielony (oba landingi nadal statyczne ○, TS bez błędów). | `src/app/strony-internetowe/page.tsx`, `src/app/sklepy-internetowe/page.tsx`, `src/lib/tracking.ts`, `src/components/case-studies.tsx` (NEW), `src/components/compact-lead-form.tsx` (NEW), `src/components/ui/cta-button.tsx` (NEW), `src/components/ui/reveal.tsx` (NEW), `progress.md` |
| 2026-06-09 | **CRM `/crm` — pełne wdrożenie produkcyjne (env + Upstash); logowanie i panel zweryfikowane na żywo.** Działania po stronie Vercela (NIE kod, dla kontekstu kolejnych agentów): (1) **Sekrety**: `CRM_EMAIL=biuro@programo.pl` (Production) + `CRM_PASSWORD` (Sensitive, Production+Preview) — hasło wpisał Wojtek osobiście (zasada: agent nie wpisuje haseł do pól). (2) **Logowanie zweryfikowane na żywo** — `programo.pl/crm` otwiera panel (e-mail biuro@programo.pl + hasło → dashboard „Leady"). ⚠️ Uwaga: autouzupełnianie przeglądarki podpowiada `wojciech.plonka@programo.pl` — login to **biuro@programo.pl** (zgodne z `CRM_EMAIL`). (3) **Upstash for Redis** sprovizjonowany przez Vercel Marketplace (plan **Free** 500k cmd/mies., region **iad1** = co-located z funkcjami Hobby, eviction OFF — leady nie znikają; resource `upstash-kv-byzantium-river`, status Available) i podłączony do projektu → wstrzyknięte `KV_REST_API_URL` + `KV_REST_API_TOKEN` (Production+Preview), które czyta `src/lib/leads.ts` (`getRedis()` wspiera parę KV_*). (4) **Redeploy** produkcji (`vercel redeploy` → alias programo.pl) — `/crm` pokazuje teraz stan SKONFIGUROWANY („Brak leadów" zamiast „Magazyn nieskonfigurowany"), więc nowe zgłoszenia będą się zapisywać i wyświetlać. (5) Leady **nadal lecą też na Telegram** (TELEGRAM_BOT_TOKEN/CHAT_ID były ustawione od 22 dni). Próba provisioning przez `vercel integration add` (CLI) zawisła na braku promptu — zrobione przez dashboard. Bez zmian w plikach kodu. | (infra/Vercel) `progress.md` |
| 2026-06-08 | **CRM `/crm` — przeprojektowanie UI (mega ładny + prosty) + red-team auth (prośba Wojtka: „nie mogę się zalogować, mega bezpieczne, jeden login dla mnie i Bartka, mega ładny i prosty").** Zrealizowane workflow multi-agent (panel 3 projektów → sędzia + hardening → red-team). **(A) UI — zwycięski wariant „EDITORIAL MINIMAL" (39/40):** `LoginForm.tsx` przeprojektowany — centrowany, `font-headline` „Panel CRM", podkreślenia pól (border-b), **toggle pokaż/ukryj hasło**, spinner, motion fade-in, semantyczne tokeny (działa w jasnym i ciemnym). **NOWY `LeadsDashboard.tsx`** (client, props `{leads, configured}`): masthead „Leady" + licznik z polską odmianą (zgłoszenie/zgłoszenia/zgłoszeń), **wyszukiwarka** (filtr po imieniu/e-mailu/telefonie/wiadomości/typie), karty leadów (czas absolutny pl-PL + relatywny „X dni temu", e-mail mailto + **kopiuj**, telefon tel: + kopiuj, chipsy temat/typ/budżet, treść, rozwijane **„Źródło leada"** z atrybucją utm/gclid), własny przycisk **Wyloguj**, empty-state + stan „magazyn nieskonfigurowany" (podpowiedź Upstash). `page.tsx` → server: `verifyToken` → login albo `<LeadsDashboard leads configured>`. Usunięto osierocony `LogoutButton.tsx`. **`providers.tsx`** — `/crm` to **samodzielny panel**: `usePathname` strippuje całe marketingowe chrome (navbar/footer/preloader/sticky CTA/cookie banner/analytics), zostają tylko Theme+i18n. **(B) Auth — rewizja red-teamu (approved, 0 high/med po fixach):** `verifyToken` pełna walidacja kształtu payloadu (`v===2`, `iat`/`exp` dodatnie skończone, odrzucenie `iat` z przyszłości i absurdalnie odległego `exp` — bound skew 1 min), `JSON.parse` w try/catch (atak nie wywoła 500). `crm-login`: rate-limit liczy **tylko NIEUDANE próby** (udane logowania nie zżerają budżetu; `delete(ip)` na sukcesie — założyciel nie zablokuje się logując kilka razy), **cap mapy `MAX_TRACKED_IPS=10000`** z ewikcją najstarszego (anty-OOM przy spoofowanym X-Forwarded-For), opóźnienie ~300 ms także na ścieżce 429 (brak sygnału timingowego), polskie znaki w komunikatach przywrócone. Token nadal SELF-EXPIRING (base64url(payload).base64url(HMAC), klucz = `sha256("programo-crm::v2::"+CRM_PASSWORD)`), single-secret (`CRM_PASSWORD` + opcjonalny `CRM_EMAIL`), repo PUBLICZNE → zero hardcoded. `npm run build` zielony (28 tras, `/crm` ƒ dynamic, TS bez błędów). ⚠️ **Do ustawienia w Vercel** (jedyna akcja po stronie Wojtka): `CRM_EMAIL=biuro@programo.pl`, `CRM_PASSWORD=<hasło>`, + para Upstash Redis (Storage → Upstash → Connect to Project, auto-wstrzykuje zmienne). | `src/lib/crm-auth.ts`, `src/app/api/crm-login/route.ts`, `src/app/crm/LoginForm.tsx`, `src/app/crm/LeadsDashboard.tsx` (NEW), `src/app/crm/page.tsx`, `src/components/providers.tsx`, `src/app/crm/LogoutButton.tsx` (usunięty), `progress.md` |
| 2026-06-08 | **Usunięto publiczny cennik z całej strony → model „indywidualna wycena" (prośba Wojtka: „wyjebać cennik, napisać że robimy indywidualną wycenę", nacisk na profesjonalny polski).** (1) **`/cennik` przepisany** — `pricing.tsx` z 3 sztywnych progów (Starter od 6 000 zł / Business od 15 000 zł / SaaS) na sekcję „Każdy projekt wyceniamy indywidualnie": nagłówek z filozofią (brak cennika z półki) + 3 karty „od czego zależy wycena" (Zakres i złożoność / Integracje i technologia / Termin i utrzymanie, środkowa wyróżniona) + jeden pas CTA „Poznaj koszt swojego projektu" → /kontakt z obietnicą „Widełki przed startem · Bez ukrytych kosztów · Praca etapami". (2) **i18n** — wymieniony cały blok `pricing.*` (usunięte klucze starter/business/saas z kwotami; dodane `pricing.factor1-3.*`, `pricing.ctaTitle/ctaDesc/promise`); `nav.pricing` „Cennik"→„Wycena" (PL), EN „Pricing" bez zmian. (3) **Nawigacja + stopka** — etykieta „Cennik"→„Wycena"; **URL `/cennik` zachowany** (kapitał SEO, brak redirectów). (4) **`/cennik` meta** — title „Wycena — Programo" + opis o indywidualnej wycenie. (5) **Spójność — usunięte WSZYSTKIE publiczne kwoty z reszty serwisu, by nie przeczyły nowemu modelowi**: `/ile-kosztuje-aplikacji` (strona SEO) — header/tabela/FAQ/MVP/CTA bez złotówek; kolumna cen w tabeli → orientacyjny CZAS realizacji (2–3 tyg / 4–6 tyg / od 6–8 tyg), nagłówek „Co budujemy i ile to zwykle trwa", `priceRows`→`projectRows` (`price`→`timeline`), meta + `headline` JSON-LD bez „Cennik 2026"; FAQ `/strony-internetowe` i `/sklepy-internetowe` („Ile kosztuje…") przeramowane na „koszt zależy od zakresu + indywidualna wycena"; `/software-house-poznan` FAQ bez „od kilku/kilkudziesięciu tysięcy". (6) **Dane strukturalne (JSON-LD)** — usunięte bloki `offers: AggregateOffer` z `lowPrice`/`highPrice`: `software-house-poznan` (5000–200000 PLN) i `sklepy-internetowe` (6000–60000 PLN), żeby Google nie pokazywał cen w rich results. **Zachowane świadomie**: chipsy budżetu w formularzu (`quick.budget.*` „do 10 tys. zł" itd.) — to pytanie do klienta o JEGO budżet (kwalifikacja leada), nie nasz cennik. ⚠️ Trade-off: strony tracą konkretne liczby pod zapytania „ile kosztuje" — świadomy wybór zgodny z decyzją biznesową Wojtka. `npm run build` zielony (28 tras, `/cennik` + `/ile-kosztuje-aplikacji` nadal statyczne ○). | `src/components/pricing.tsx`, `src/lib/i18n.tsx`, `src/components/footer.tsx`, `src/app/cennik/page.tsx`, `src/app/ile-kosztuje-aplikacji/page.tsx`, `src/app/strony-internetowe/page.tsx`, `src/app/sklepy-internetowe/page.tsx`, `src/app/software-house-poznan/page.tsx`, `progress.md` |
| 2026-06-08 | **Hardening auth CRM — token SELF-EXPIRING z podpisem (single-secret, repo PUBLICZNE).** Słabość poprzedniego schematu: token sesji był STATYCZNY — `HMAC-SHA256("programo-crm-v1", key=CRM_PASSWORD)` w hex, ta sama wartość „na zawsze", bez wbudowanego wygaśnięcia (kradzież cookie = bezterminowy dostęp aż do rotacji hasła). Przebudowano **`src/lib/crm-auth.ts`** zachowując WSZYSTKIE publiczne sygnatury (`CRM_COOKIE`, `verifyToken`, `verifyCreds`, `setSessionCookie`, `clearSessionCookie` — konsumenci `crm/page.tsx`, `api/leads/route.ts`, `api/crm-logout` bez zmian). Nowy token = `base64url(payloadJson) + "." + base64url(HMAC-SHA256(payloadJson, key))`, gdzie payload = `{v,iat,exp}` (exp = unix ms, +7 dni), a klucz HMAC jest WYPROWADZONY z hasła: `sha256("programo-crm::v2::" + CRM_PASSWORD)` — rotacja `CRM_PASSWORD` unieważnia wszystkie sesje. `verifyToken`: (a) split bezpieczny (odrzuca brak „.", złą liczbę części, puste części, niekanoniczny base64url przez round-trip re-encode), (b) recompute HMAC + porównanie **timing-safe na równej długości digestach** (`crypto.timingSafeEqual`), (c) parse payloadu + odrzucenie gdy `exp` nie-number/nieskończone lub `Date.now() >= exp`. Brak `CRM_PASSWORD` → `verifyCreds`/issuer/`verifyToken` failują (BRAK insecure default; repo jest PUBLICZNE — zero hardcoded sekretów). Zegar czytany w runtime wewnątrz funkcji (nigdy na poziomie modułu). `sessionToken()` (export) zastąpiony wewnętrznym `issueToken()` — `setSessionCookie` samowystarczalny (zweryfikowano: brak zewnętrznych konsumentów `sessionToken`). Cookie flags bez zmian (httpOnly, secure w prod, sameSite lax, path /, maxAge 7 dni). **`src/app/api/crm-login/route.ts`**: zachowany rate-limit 5/15min/IP (x-forwarded-for first hop / x-real-ip), dodano **stałe sztuczne opóźnienie ~300 ms przed 401** (spowolnienie brute force), 429 przy limicie, 400 przy złym JSON, generyczny komunikat (nigdy nie zdradza czy zawinił e-mail czy hasło), brak logowania sekretów/tokenów. Single-secret zachowane: jedyny wymagany env = `CRM_PASSWORD` (+ opcjonalny `CRM_EMAIL`). `npx tsc --noEmit` zielony; smoke-test runtime przeszedł (valid verifies, expired/tampered-payload/tampered-sig/no-dot/3-parts/empty odrzucone, unset secret → issue „" + verify false). | `src/lib/crm-auth.ts`, `src/app/api/crm-login/route.ts`, `progress.md` |
| 2026-06-08 | **Wewnętrzny CRM `/crm` + trwały zapis leadów (Upstash Redis).** Dotąd leady z formularza szły TYLKO na e-mail (Resend) / Telegram — nigdzie nie były przechowywane, brak panelu do przeglądania. Dodano: (1) **`src/lib/leads.ts`** — `getRedis()` budujący klienta `@upstash/redis` z env wspierający OBA warianty par (`UPSTASH_REDIS_REST_URL`+`UPSTASH_REDIS_REST_TOKEN` LUB `KV_REST_API_URL`+`KV_REST_API_TOKEN`), `null` gdy brak; typ `Lead` (wszystkie pola formularza + atrybucja Ads + `id` + `ts` ISO przekazywane z zewnątrz); `storeLead()` = `lpush` na `programo:leads` + `ltrim 0..499` (ostatnie 500), **best-effort w try/catch — nigdy nie rzuca**, no-op bez Redis; `getLeads(limit=200)` = `lrange` + JSON.parse (pomija nieparsowalne), `[]` bez Redis. (2) **`src/app/api/contact/route.ts`** — po udanej walidacji `await storeLead({...})` z `result.data` + atrybucją + `crypto.randomUUID()` + timestampem requestu, w try/catch — **nie wpływa na odpowiedź ani na flow Resend/Telegram** (zachowane bez zmian). Naprawiono też pre-existing błąd TS w dev-logu (`sanitize(email)` → `safeEmail`, bo `email` jest opcjonalny). (3) **`src/lib/crm-auth.ts`** — creds z env `CRM_EMAIL`/`CRM_PASSWORD` (BRAK hardcoded sekretu); `verifyCreds()` timing-safe (e-mail case-insensitive+trim), fail gdy `CRM_PASSWORD` nieustawione; `sessionToken()` = HMAC-SHA256("programo-crm-v1", key=CRM_PASSWORD) hex; `verifyToken()` timing-safe; cookie `crm_session` httpOnly+secure(prod)+sameSite lax+path/+7 dni. (4) **API**: `POST /api/crm-login` (rate-limit 5/15min/IP wzorem contact, `verifyCreds`, set cookie / 401), `POST /api/crm-logout` (clear cookie), `GET /api/leads` (czyta cookie via `await cookies()`, `verifyToken`, 401 albo `getLeads()`). (5) **`src/app/crm/page.tsx`** — server component, `robots noindex/nofollow`, `force-dynamic`; bez sesji → centrowany login (`LoginForm.tsx` client, POST→`router.refresh()`); z sesją → lista leadów (najnowsze na górze) z datą/imieniem/mailto/tel/tematem/typem/budżetem/treścią + **wyróżnione źródło leada** (kampania/słowo kluczowe/gclid/strona wejścia), licznik + `LogoutButton.tsx`; gdy brak Redis → przyjazny komunikat zamiast crasha. Tokeny motywu semantyczne, PL, mobile-friendly, NIE dodane do nav/sitemap (internal). `npx tsc --noEmit` zielony, `npm run build` zielony (28 tras, `/crm` + 3 API jako ƒ dynamic). ⚠️ Testy komponentów/SEO failują (pre-existing drift i18n — about/featured-work/footer/hero/tech-stack/seo, nie dotyczą CRM). ⚠️ **Do ustawienia w Vercel**: `CRM_EMAIL=biuro@programo.pl`, `CRM_PASSWORD=<hasło>`, oraz para Upstash Redis (`UPSTASH_REDIS_REST_URL`+`UPSTASH_REDIS_REST_TOKEN`) lub Vercel KV (`KV_REST_API_URL`+`KV_REST_API_TOKEN`) — provisioning 1-klik w Vercel → Storage → Upstash for Redis (auto-wstrzykuje zmienne). | `src/lib/leads.ts` (NEW), `src/lib/crm-auth.ts` (NEW), `src/app/crm/page.tsx` (NEW), `src/app/crm/LoginForm.tsx` (NEW), `src/app/crm/LogoutButton.tsx` (NEW), `src/app/api/crm-login/route.ts` (NEW), `src/app/api/crm-logout/route.ts` (NEW), `src/app/api/leads/route.ts` (NEW), `src/app/api/contact/route.ts`, `package.json`, `progress.md` |
| 2026-06-08 | **Formularz QuickContact: mniej friction + czytelna zgoda RODO (feedback Wojtka: „znaczek RODO niewidoczny, trzeba wpisać 20 znaków").** (1) **E-mail LUB telefon — wystarczy jedno**: `email` zrobiony opcjonalnym; walidacja „email‖phone" klientowo (`quick-contact.tsx`, przed wysyłką) ORAZ serwerowo w `/api/contact` (`.refine`). (2) **Wiadomość opcjonalna**: usunięto `required` + `minLength={20}` z `<textarea>` i ze schematu zod — można wysłać samo imię + telefon. (3) **Checkbox RODO czytelny**: był `bg-transparent` + cienki `border-outline-variant/50` na półprzezroczystej karcie = praktycznie niewidoczny → teraz `h-5 w-5`, `border-2 border-outline`, `bg-surface`, w wyróżnionym kontenerze (`bg-surface/70` + border), tekst `text-on-surface/80`, link polityki `text-primary`. (4) Klucze i18n `quick.optional/contactHint/contactRequired`; etykiety email/wiadomość → „(opcjonalnie)" + podpowiedź „Wystarczy e-mail LUB telefon". `route.ts`: mail (Resend) + Telegram z warunkowym e-mailem/wiadomością (guard `safeEmail`/`safeMessage`). ⚠️ Uwaga niezależna: dostarczanie leadów (Resend→biuro@programo.pl / Telegram) działa TYLKO gdy w Vercel ustawione `RESEND_API_KEY` / `TELEGRAM_*` — do weryfikacji testem formularza. | `src/components/quick-contact.tsx`, `src/app/api/contact/route.ts`, `src/lib/i18n.tsx`, `progress.md` |
| 2026-06-08 | **CRO „D-lite" na landingach Ads — koniec gubienia leadów (kompozycja istniejących komponentów).** Z analizy multi-perspektywicznej + red-teamu: oba landingi Ads (`/strony-internetowe`, `/sklepy-internetowe`) kierowały oba CTA „Bezpłatna wycena" przez `<Link href="/kontakt">` (double-hop = strata leadów na przejściu). Zmiany na OBU stronach: (1) **Koniec double-hopa** — 2× CTA „Bezpłatna wycena" (hero + dolne) zmienione z `/kontakt` na kotwicę `href="#kontakt-main"` do formularza inline na tej samej stronie; linki `tel:` bez zmian. (2) **Formularz inline** — wstawiony `<QuickContact />` (sekcja `id="kontakt-main"`, walidacja, zgoda RODO, `trackLead` odpala konwersję Google Ads tylko po udanym submit z atrybucją gclid/utm + guard double-fire) z krótkim nagłówkiem „Napisz — wrócimy z wyceną w 24h". (3) **Dowód społeczny** — `<ProjectsMarquee />` (realne screenshoty z `projects.ts`) + `<TrustBar />`. **TrustBar UŻYTY** — zweryfikowano `trust.stat*`: „7" wdrożonych produktów (= 7 realnych projektów w portfolio), „100%" własny kod bez podwykonawców, „do 24 h" czas odpowiedzi (spójne z copy landinga), „PL · EN" dwujęzyczność — to fakty jakościowe/strukturalne, nie zmyślone liczby marketingowe, więc zgodne z regułą „no fabricated stats". Komponenty client wstawione bezpośrednio do server-page (już są w `<I18nProvider>` z `layout.tsx`). Hero/H1/metadata/JSON-LD BEZ ZMIAN (kapitał Quality Score). Bez Three.js. Build zielony, obie strony nadal statyczne (○). | `src/app/strony-internetowe/page.tsx`, `src/app/sklepy-internetowe/page.tsx`, `progress.md` |
| 2026-06-08 | **Dodano transkrypcję + analizę masterclassu Jono Catliffa „Claude Code Google Ads ($730K)".** Wojtek poprosił o transkrypcję filmu YT (`-EInjdpjKy0`, 1:08:17) i analizę pod kątem zastosowania w Programo. Pobrane auto-napisy (yt-dlp), oczyszczone i pogrupowane wg 26 rozdziałów do `docs/google-ads-masterclass-transkrypcja.md` (~14,3k słów). Wnioski operacyjne (nie kod): strona ma już ~80% infry z filmu (atrybucja gclid/utm w `tracking.ts`, konwersja formularza, Consent Mode v2, ~50 negatywów, landingi `/strony-internetowe` `/sklepy-internetowe` `/ile-kosztuje-aplikacji` `/software-house-poznan`). Luki vs film: (1) SKAG-i + repoint final URL (czeka na weryfikację tożsamości Google, kampania WSTRZYMANA), (2) domknięcie pętli ROAS przez programo-hub jako CRM → Enhanced/Offline Conversions upload, (3) audytorium remarketingowe (RLSA), (4) Claude Code skill `/generate-ads` + podłączenie Claude do Google Ads API, (5) social proof na landingach (case studies już są). Brak zmian w kodzie aplikacji. | `docs/google-ads-masterclass-transkrypcja.md`, `progress.md` |
| 2026-06-07 | **GA4 — podmiana ID pomiaru na właściwą właściwość Wojtka.** Strona wysyłała dane do `G-TGLPLMVV91` — właściwości GA4, której Wojtek NIE ma na swoim koncie (w jego Analytics tylko „Skup Nieruchomosci PL" 395800319), więc analityka programo.pl leciała do cudzej/martwej właściwości i nigdzie jej nie widział. Zdiagnozowane na żywo przez podgląd requestów: beacon `region1.google-analytics.com/g/collect?tid=G-TGLPLMVV91&en=page_view` realnie leciał (tag był wpięty), ale do złego ID + ze zgodą `gcs=G100` (denied) = dane modelowane. Wojtek utworzył nową właściwość „Programo" → nowe ID **`G-KT2R144BYG`**. Podmienione `GA_ID` w `layout.tsx`. Tag Google Ads `AW-18196600478` bez zmian — **leady/konwersje działają niezależnie od GA4** (osobny mechanizm). | `src/app/layout.tsx`, `progress.md` |
| 2026-06-07 | **Jedmar = aplikacje NATYWNE (Swift/SwiftUI + Kotlin), nie Capacitor — korekta Wojtka.** Wpis Jedmara błędnie opisywał apkę jako hybrydę Capacitor (React/Next.js/TS w powłoce Capacitor). Poprawione na **natywne**: `projects.ts` — „Jak zbudowane" (dwie natywne aplikacje: iOS Swift/SwiftUI + Android Kotlin), bullet „Dlaczego tak" (natywnie zamiast hybrydy Capacitor/RN — płynność, dostęp do aparatu/powiadomień/gestów), `metric` „iOS + Android · natywnie", `tech` (`Capacitor/React/Next.js/TS` → `iOS/Android/Swift/SwiftUI/Kotlin/PrestaShop API`), feature #1; PL+EN. `sklepy-internetowe` — usunięto „(Capacitor)" z sekcji realizacji Jedmar (opis + chip „Natywne iOS + Android") i z karty usługi „Aplikacje mobilne do sklepów". Capacitor zostawiony tam, gdzie realny (Estalo Capacitor 8, /stack, /software-house-poznan, /ile-kosztuje, llms.txt). | `src/lib/projects.ts`, `src/app/sklepy-internetowe/page.tsx`, `progress.md` |
| 2026-06-07 | **Landingi Ads dostępne z górnego menu + klikalnej sekcji Oferta (prośba Wojtka).** Dotąd `/strony-internetowe` i `/sklepy-internetowe` dało się otworzyć tylko z URL/reklamy lub stopki. Dodano: (1) **górne menu** — nowe pozycje „Strony WWW" → `/strony-internetowe` i „Sklepy" → `/sklepy-internetowe` (nowe klucze i18n `nav.websites`/`nav.stores`); menu 5 → 7 pozycji. (2) **Sekcja Oferta** (`offer.tsx`, na `/oferta` i stronie głównej) — karta „Strony internetowe" + NOWA karta „Sklepy internetowe" są teraz **klikalne** (overlay `<Link className="absolute inset-0">` na całą kartę + cue „Zobacz →"), prowadzą do landingów; dodany item e-commerce (klucze `offer.ecommerce.title/desc`), karty przenumerowane 01–06, CTA „07". Build do weryfikacji. | `src/lib/i18n.tsx`, `src/components/navbar.tsx`, `src/components/offer.tsx`, `progress.md` |
| 2026-06-07 | **Fix: podwójny navbar + podwójna stopka na stronach projektów.** `ProjectDetailClient` renderował WŁASNY `<Navbar/>` + `<Footer/>` (oraz zagnieżdżony `<main>` i `<I18nProvider>`), mimo że globalny `Providers` już opakowuje KAŻDĄ stronę w navbar + stopkę + `<main id="main-content">` + i18n. Efekt: **2 navbary na każdej stronie projektu** (nakładały się idealnie w tej samej pozycji `fixed` → wyglądały jak jeden, stąd niezauważone) i **2 stopki na ostatnim projekcie** (Pool System — brak „next project", więc `{!next && <Footer/>}` renderował drugą). Zagnieżdżony `I18nProvider` mógł też desynchronizować przełącznik języka na stronach projektów. Usunięto własny Navbar/Footer/main/I18nProvider — `ProjectDetailClient` korzysta teraz z globalnej powłoki `Providers` (1 navbar, 1 stopka, 1 `main`, wspólny kontekst języka). Pozostałe sekcje (hero, story, galeria telefonów, capabilities, „Next Project") bez zmian. `npm run build` (25 tras) zielony. Jedyne miejsce z duplikacją w repo (grep potwierdził). | `src/app/projects/[slug]/ProjectDetailClient.tsx`, `progress.md` |
| 2026-06-07 | **Landingi Ads spójne z całokształtem + dostępne z UI; Jedmar/WKS odświeżone.** (A) **`/strony-internetowe` + `/sklepy-internetowe` przepisane na semantyczne tokeny motywu** — z surowych `var(--theme-*)`+`opacity` na `bg-surface`/`text-on-surface`/`text-on-surface-variant`; płaskie obrysowe karty → **wypełnione `bg-surface-container-low`** z `border-outline-variant` (jak stopka/reszta strony); przyciski `bg-primary → hover:bg-primary-container` (identycznie jak CTA w navbarze); eyebrow `text-primary`. Teraz **reagują na przełącznik jasny/ciemny** i pasują do reszty. Dodany odstęp pod pływający navbar (`pt-28/md:pt-32`, wzorem `/oferta`) + **naprawiony nieprawidłowy zagnieżdżony `<main>`** (landing `<main>`→`<div>`, bo `Providers` już renderuje `<main id="main-content">`). (B) **Dostępność z UI:** oba landingi dodane do stopki („Mapa strony": Strony internetowe + Sklepy internetowe) + link „Zobacz case Jedmar →" na `/sklepy-internetowe` (wcześniej osierocone — wejście tylko z reklamy). (C) **Jedmar = case study aplikacji mobilnej** (`projects.ts`): sekcje Co zrobiliśmy/Jak zbudowane/Dlaczego/Efekt, `kind:"mobile-app"`, tech +Capacitor/iOS/Android, 8 features, `liveUrl`→`jedmar-shop.vercel.app`, metric „iOS + Android · Capacitor" (bez zmyślonych liczb), Efekt szczerze: apka gotowa, jeszcze niepublikowana w App Store/Google Play. `ProjectDetailClient`: galeria w **ramkach telefonu w stylu App Store** — warunkowo TYLKO dla `kind:"mobile-app"` (pozostałe projekty bez zmian, zweryfikowane). (D) **WKS Poznań:** 3 świeże zrzuty z live `wks-strona.vercel.app` (Playwright+system Chrome→WebP, <90 KB) nadpisane. `tsc --noEmit` + `npm run build` (25 tras) zielone. | `src/app/strony-internetowe/page.tsx`, `src/app/sklepy-internetowe/page.tsx`, `src/components/footer.tsx`, `src/lib/projects.ts`, `src/app/projects/[slug]/ProjectDetailClient.tsx`, `public/screenshots/wks-{hero,cap2,cap3}.webp`, `progress.md` |
| 2026-06-07 | **Tracking konwersji: formularz = jedyna podstawowa konwersja Ads + domknięcie `/audyt`.** (A) **Tracking (runda 2, wg syntezy §4.4):** wcześniej `trackContactClick()` odpalał TĘ SAMĄ etykietę konwersji Ads „Lead" (`AW-18196600478/tYIqCM3A_rkcEJ6t6ORD`) co formularz — kliknięcia `tel:`/`mailto:` zawyżały i zaśmiecały podstawowy sygnał leada (Smart Bidding goniłby tani klik w telefon). Teraz: kliknięcia tel/mail = **tylko event GA4 `contact_click`** (obserwacja), a **wysłanie formularza = jedyna podstawowa konwersja Ads** (`trackLead` wyłącznie na gałęzi sukcesu `/api/contact`). Dodany guard jednokrotnego odpalenia (`submittingRef`) w `quick-contact.tsx` — konwersja leci dokładnie raz na udane wysłanie (ochrona przed double-submit/re-render); `finally` zwalnia ref przy błędzie. Etykieta konwersji bez zmian; Consent Mode v2 (advanced) potwierdzony OK. (B) **`/audyt` — domknięcie:** usunięty ostatni ślad trasy — link „Darmowy audyt strony" → `https://programo.pl/audyt` w `public/llms.txt`. Reszta repo czysta (src/, sitemap.ts, seo.test.ts bez odwołań; katalogi `audyt` + `api/audit` skasowane wcześniej). `tsc --noEmit` zielony. ⚠️ **Do zrobienia w panelu Ads** (czeka na weryfikację tożsamości Google): zapis repointu Grupy 2 (`/oferta`→`/strony-internetowe`), repoint Grupy 3 (`/audyt`→`/strony-internetowe`), Enhanced Conversions for Leads ON. Negatywy rozszerzone do ~50 (z 22). Kampania nadal WSTRZYMANA. ⚠️ **Pre-existing** (nie z tej zmiany, do osobnej naprawy): `seo.test.ts` „canonical URLs" regex `[a-z]+` odrzuca myślnik w slugu `wks-poznan`; testy komponentów (drift i18n). | `src/lib/tracking.ts`, `src/components/quick-contact.tsx`, `public/llms.txt`, `progress.md` |
| 2026-06-06 | **Nowy landing `/strony-internetowe` (Grupa 2 Ads) — runda 2.** Reklamy Grupy 2 („Strony WWW") lądowały na generycznej `/oferta` (słaby Quality Score + konwersja wg recenzenta #2 — „najsłabsze ogniwo"). Utworzono dedykowany landing wzorem działającego `/sklepy-internetowe`: H1 „Strony internetowe, które pracują na Twój biznes", 6 usług (strona firmowa, landing/sprzedażowa, sklep→link do /sklepy-internetowe, redesign+migracja, wydajność+SEO/Core Web Vitals, utrzymanie), sekcja „dlaczego Programo" + chipy stacku, proces 3-krokowy, FAQ z widełkami (prosta od kilku tys., rozbudowana od kilkunastu tys. — bez zmyślonych liczb), **click-to-call** (`tel:+48509123434`), pasek realizacji. Sekcje „redesign" + „wydajność/SEO" łapią też intencję Grupy 3 („naprawa/optymalizacja"). Dodany do `sitemap.ts` (prio 0.95). ⚠️ Repoint Grupy 2 (`/oferta`→`/strony-internetowe`) i Grupy 3 (`/audyt`→`/strony-internetowe`) do zrobienia w panelu Ads. Build 29/29 ✓. | `src/app/strony-internetowe/page.tsx` (NEW), `src/app/sitemap.ts`, `progress.md` |
| 2026-06-06 | **Optymalizacja landingów pod Google Ads — runda 1 (po syntezie 3 audytów).** (A) **`/sklepy-internetowe`**: Jedmar poprawiony z „PWA mobile-first" → **„aplikacja mobilna iOS + Android (Capacitor)"** (na prośbę Wojtka — Jedmar to apka DO sklepu, nie PWA/cały sklep); dodany **click-to-call** (`tel:+48509123434` w hero i dolnym CTA — niskozaporowa konwersja, odpala tag `tel:`, skraca „hop" do `/kontakt`); pasek realizacji (Jedmar/Estalo/WKS/WSafe + „bezpośredni kontakt z założycielami"); usługa „Aplikacje i PWA do sklepów" → „Aplikacje mobilne do sklepów". (B) **Usunięto całą funkcję `/audyt`** (Wojtek nie chce tej podstrony): skasowane `src/app/audyt/` + `src/app/api/audit/` (route + lead), usunięte odwołania w `footer.tsx`, `sitemap.ts`, `ile-kosztuje-aplikacji`, `seo.test.ts` (próg ≥10). ⚠️ Grupa reklam „Naprawa/optymalizacja stron" wskazuje teraz na nieistniejący `/audyt` → repoint przy strojeniu Ads (kampania WSTRZYMANA, brak ruchu). Synteza/plan: `docs/google-ads-audyt-synteza-2026-06.md`. Build zielony. | `src/app/sklepy-internetowe/page.tsx`, `src/components/footer.tsx`, `src/app/sitemap.ts`, `src/app/ile-kosztuje-aplikacji/page.tsx`, `src/__tests__/seo.test.ts`, usunięte `src/app/audyt/` + `src/app/api/audit/`, `progress.md` |
| 2026-06-06 | **Estalo — status „wkrótce" + świeże premium screeny.** Wojtek: pokazać Estalo jako „wkrótce", nie „już ruszamy". (1) `status` `live`→`planned` → karty automatycznie pokazują badge „Wkrótce/Coming soon". (2) `metric` → „Solo · Enterprise · Portal — wkrótce / soon". (3) `longDescription` PL/EN: „Dostępny/Two plans" → „Premiera wkrótce — / Launching soon —"; z `description.en` usunięto twarde liczby (791 testów itd.) dla framingu „wkrótce". (4) `ProjectDetailClient`: etykieta linku `liveUrl` warunkowa — „Live Site" tylko dla `status==='live'`, inaczej „Podgląd". (5) **3 świeże zrzuty WebP** zastępujące stare (sprzed premium-redesignu landingu): hero (badge „● Wkrótce" + luksusowy telefon), sekcja głosowej notatki (landscape), telefon z dopasowaniami AI + foto wnętrza z panoramą Warszawy (portret 4:5) — puppeteer headless z `prefers-reduced-motion` (stany końcowe animacji) z landingu, konwersja sharp→WebP. Zweryfikowane (next start + puppeteer): galeria, opis „Premiera wkrótce", listing „— WKRÓTCE". Test projects 12/12, build 27/27 ✓. | `src/lib/projects.ts`, `src/app/projects/[slug]/ProjectDetailClient.tsx`, `public/screenshots/estalo-hero.webp`, `public/screenshots/estalo-otodom.webp`, `public/screenshots/estalo-portal.webp`, `progress.md` |
| 2026-06-06 | **Pool System — totalna przebudowa wpisu projektu + screeny.** Wpis projektu basenowego (slug `pool-system`) miał czarny hero, dwa generyczne akapity i 0 zrzutów. Przepisany całościowo: subtitle „Od ściany basenu po klubowe command center", pełny `longDescription` (Co robi / Na czym jest zbudowany / Dlaczego tak) z realną architekturą (touchpady ESP32-C3 + box ESP32-S3, ESP-NOW, BLE Polar HR, lokalny AP + WebSocket, iOS SwiftUI, web coach/athlete Next.js+Supabase, command center, eksport LENEX/CSV), status `planned`→`development`, `liveUrl` swimplatform.vercel.app, realny `tech` stack, 8 capability z tytułami (format „Tytuł — opis"), `metric` „±5 ms · 1–8 torów". Dodane 3 zrzuty WebP do `public/screenshots/`: hero (blurred ambient z landingu), command-center dashboard (landscape 1600px), realna apka iOS zawodnika złożona na brandowym tle 4:5 — wygenerowane headless Chrome (landing) + symulator iOS, przetworzone Pillow + cwebp. Zweryfikowane wizualnie (dev): cinematic hero z tytułem POOL SYSTEM, asymetryczna siatka screenów (dashboard + apka iOS) i sekcja Capabilities renderują się poprawnie. Testy projects 12/12, build produkcyjny 27/27 stron OK. | `src/lib/projects.ts`, `public/screenshots/pool-system-hero.webp`, `public/screenshots/pool-system-cap2.webp`, `public/screenshots/pool-system-cap3.webp` |
| 2026-06-06 | **Śledzenie konwersji Google Ads (formularz + kliknięcia kontaktowe):** Pod kampanię Search „Programo – Search – E-commerce (PL)" (konto 561-045-3845) podpięto konwersję Google Ads `AW-18196600478`, działanie „Przesłanie formularza kontaktowego". Wykorzystano istniejący gtag.js (GA4) — nie dublujemy ładowania tagu. (1) `layout.tsx`: dodatkowy `gtag('config','AW-18196600478')` obok GA4; tag Ads gated przez Consent Mode v2 (zgoda marketingowa — zgodne z RODO/banerem zgód). (2) `tracking.ts`: helper `trackAdsConversion()` (`send_to: AW-18196600478/tYIqCM3A_rkcEJ6t6ORD`, value 1.0 PLN) wołany w `trackLead()` (sukces formularza kontaktowego) ORAZ `trackContactClick()` (kliknięcia `tel:`/`mailto:` łapane globalnie przez `analytics-tracker`). Efekt: każdy lead — formularz, telefon, email — liczony jako konwersja, co odblokowuje optymalizację kampanii pod konwersje. `tsc --noEmit` + `npm run build` zielone. | `src/app/layout.tsx`, `src/lib/tracking.ts`, `progress.md` |
| 2026-06-03 | **Przebudowa portfolio: −Athlix, Jedmar=apka mobilna, Solvio=coming soon, +Pool System:** Na prośbę Wojtka (korekta tego, co realnie robiliśmy). (1) **Usunięto Athlix** z `projects.ts` + wszystkich referencji: `projects-marquee` MARQUEE_SLUGS, `trust-bar` WORDMARK_SLUGS, `software-house-poznan` (lista projektów + FAQ → podmienione na Pool System), obrazy `athlix-*.webp` skasowane. (2) **Jedmar** przepisany ze „sklep e-commerce" na **aplikację mobilną iOS+Android DO sklepu** (to było zlecenie — nie budowaliśmy całego sklepu): nowy subtitle/description/longDescription/features/tech (Capacitor/iOS/Android)/tags (Mobile/iOS/Android)/metric; **mobilne** zrzuty `jedmar-app1/2/3.webp` (Playwright mobile 412×900, popupy Cookiebot/newsletter odrzucone) zamiast desktopowych `jedmar-clean*`. (3) **Solvio** status → `planned` (coming soon — to też tylko apka mobilna). (4) **NOWY Pool System** (`pool-system`, `planned`, `nasze-systemy`): innowacyjny system timingowy (pomiar czasu) — hardware + aplikacja mobilna, faza demo; opis bez zmyślonych szczegółów technicznych (zasada „no fabricated stats"); brak screenshotów → kafel `bgColor` + badge. (5) **Badge „Wkrótce/Coming soon"** dodany do `featured-work` (ProjectCard) i `projects-marquee` (ProjectTile) dla `status !== "live"` (i18n `work.comingSoon` istniał, nieużywany). `projects.test.ts`: EXPECTED_SLUGS −athlix +pool-system, REMOVED_SLUGS +athlix. Build zielony (7 projektów). `jedmar-hero.webp` ZOSTAJE (używany przez `/sklepy-internetowe`). | `src/lib/projects.ts`, `src/components/projects-marquee.tsx`, `src/components/featured-work.tsx`, `src/components/trust-bar.tsx`, `src/app/software-house-poznan/page.tsx`, `src/__tests__/projects.test.ts`, NEW `public/screenshots/jedmar-app1/2/3.webp`, usunięte `athlix-*.webp` + `jedmar-clean1/2.webp`, `progress.md` |
| 2026-06-03 | **Fix animacji na Windows (scroll-linked parallax) — iOS/Mac NIETKNIĘTE:** Zgłoszenie: na iOS gładko, na Windowsie animacje „fatalnie". Diagnoza: scroll-linked parallax + rozmyte skalowane hero w `ProjectDetailClient` (strony /projects/[slug]) są masłowe na Apple (momentum scroll), a na Windowsie/desktopie (kółko myszy = duże skoki delty) sprężyny/transformy goniące scroll szarpią i overshootują. **Uwaga dla kolejnych agentów:** wcześniejsze podejrzane — `custom-cursor.tsx`, `ui/lenis-provider.tsx`, `horizontal-intro.tsx`, `hero.tsx` — to **martwy legacy po redesignie, nigdzie nie montowany** (home = `main-intro` + `trust-bar` + `projects-marquee` + `quick-contact`). Fix: nowy hook `useSmoothInput()` (true tylko na Apple/touch lub bez reduced-motion); w `ProjectDetailClient` `ParallaxImage` oraz scroll-scale/parallax hero renderują się **statycznie na Windowsie** (czysty `object-cover` zamiast szarpania), a na iOS/Mac jak dotąd. Zero zmian wizualnych na Apple. Marquee (rAF translate, GPU) zostawiony — najmniej jankowy typ; do ewentualnej tury 2 jeśli user wskaże. Build zielony. | `src/lib/use-smooth-input.ts` (NEW), `src/app/projects/[slug]/ProjectDetailClient.tsx`, `progress.md` |
| 2026-06-03 | **Portfolio: bogate galerie zdjęć (WebP) dla 5 realizacji — BEZ zmian wyglądu:** Strona projektu (`ProjectDetailClient`) renderuje galerię (kafel 16:10 + 4:5) tylko gdy `screenshots.length > 1`; `solvio` i `rejestr-pro` nie miały żadnych zdjęć (płaski kafel w marquee / litera w bento), a `jedmar/wks-poznan/wsafefinanse` miały tylko hero (brak galerii). Złapano headless (Playwright + systemowy Chrome `--channel=chrome`, 1600×1000, deviceScaleFactor 1.5, wait na render framer-motion) po kilka sekcji z każdego live'a, oceniono montage-arkuszami (ImageMagick), wybrano 2 najładniejsze sekcje/projekt i **skonwertowano do WebP** (konwencja maina): solvio (hero+feature cards), rejestr-pro (hero+features+CTA), wks (trenerzy+dyscypliny), wsafe (sekcje usług premium), jedmar **re-capturowany z odrzuceniem popupów Cookiebot/newsletter/czat** → czysty sklep (kategorie+promo, siatka produktów+marki). Każda z 5 realizacji ma teraz `screenshots: [hero, sekcja1, sekcja2]` w `.webp` → pełne galerie na /projects/[slug] + realne kafle w marquee/bento. Zachowane pole `metric`, WebP i redesign maina (nałożone na aktualny prod, nie cofnięte). Build zielony (7 stron projektów). Opisy przejrzane — bez zmian (już wysokiej jakości). | `src/lib/projects.ts`, NEW `public/screenshots/{solvio-hero,solvio-cap2,solvio-cap3,rejestr-pro-hero,rejestr-pro-cap2,rejestr-pro-cap4,jedmar-clean1,jedmar-clean2,wks-cap2,wks-cap3,wsafefinanse-cap2,wsafefinanse-cap3}.webp`, `progress.md` |
| 2026-05-29 | **NOWY landing e-commerce `/sklepy-internetowe` (feat/ecommerce-landing):** Dedykowana strona pod kampanię Google Ads skupioną w 100% na e-commerce (styl Jedmar). Sekcje: hero + CTA (obraz tymczasowo `/screenshots/jedmar-hero.png` — do podmiany na obraz generowany przez Wojtka), usługi (sklepy WooCommerce/Shopify/PrestaShop/headless, migracje, integracje Allegro/BaseLinker/płatności/KSeF, aplikacje/PWA do sklepów, headless/wydajność, utrzymanie), realizacja Jedmar (1460 prod., PrestaShop API, PWA), proces, FAQ, CTA. Schema: BreadcrumbList + Service (AggregateOffer 6000–60000 PLN) + FAQPage. Wpięte w `sitemap.ts` (priority 0.95) i `llms.txt`. URL docelowy nowej kampanii Search „E-commerce". | `src/app/sklepy-internetowe/page.tsx` (NEW), `src/app/sitemap.ts`, `public/llms.txt`, `progress.md` |
| 2026-05-29 | **Portfolio: −baulx/−learnai, +Solvio, +Rejestr Pro (Posnania już wcześniej):** Wycięto baulx i learnai z `projects.ts`. Dodano **Solvio** (SaaS AI do wydatków — OCR paragonów, porównywarka cen, raporty; solvio-lac.vercel.app) i **Rejestr Pro** (wyszukiwarka firm z KRS — oficjalne API MS, profile spółek, powiązania osób, link do RDF; rejestr-pro.vercel.app) jako „nasze-systemy", live. Bez screenshotów → kafelek marquee renderuje czysty `bgColor` (komponent to wspiera, design Bartka nietknięty). Spójność: `projects-marquee.tsx` MARQUEE_SLUGS (estalo/solvio/athlix/rejestr-pro/jedmar/wks/wsafe), `software-house-poznan` (lista + FAQ + przykłady — inaczej linki do /projects/baulx = 404), `public/llms.txt`, przepisany `projects.test.ts` (był czerwony przez length(4) — teraz zielony i durable). 7 projektów. | `src/lib/projects.ts`, `src/components/projects-marquee.tsx`, `src/app/software-house-poznan/page.tsx`, `public/llms.txt`, `src/__tests__/projects.test.ts`, `progress.md` |
| 2026-05-29 | **Usunięto KS Posnania z portfolio (nie jest klientem Programo):** Na prośbę Wojtka wycięty obiekt `ks-posnania` z `src/lib/projects.ts` (był błędnie wpisany jako realizacja). Sitemap (generowany z tablicy `projects`), `/projekty` i marquee aktualizują się automatycznie. Build zielony (7 projektów). Screenshot `/screenshots/ks-posnania-hero.png` pozostawiony jako nieużywany (opcjonalnie do sprzątnięcia). | `src/lib/projects.ts`, `progress.md` |
| 2026-05-29 | **Optymalizacja zależności + perf (feat/seo-formalities cd.) — BEZ zmian wyglądu:** Usunięto martwy `src/components/ui/hero-background-3d.tsx` (nieimportowany nigdzie, jak wcześniej `contact.tsx`) oraz odinstalowano nieużywane ciężkie zależności: `three`, `@react-three/fiber`, `@react-three/drei`, `gsap` (gsap nieużywany w ogóle; reszta tylko w martwym pliku) → mniejszy `node_modules`, szybszy install, mniejsza powierzchnia supply-chain; shipowany bundle bez zmian (nie były importowane). Dodano `preconnect`/`dns-prefetch` do `googletagmanager.com` i `clarity.ms` (szybszy handshake z analityką, lepsze CWV). `lenis` + `framer-motion` zostają (realnie używane). | `package.json`, `package-lock.json`, `src/app/layout.tsx`, usunięte `src/components/ui/hero-background-3d.tsx`, `progress.md` |
| 2026-05-29 | **SEO/a11y/perf formalności (feat/seo-formalities) — BEZ zmian wyglądu:** (1) Stopka: dodana nawigacja „mapa strony" linkująca kluczowe podstrony, w tym wcześniej osierocone `/software-house-poznan`, `/ile-kosztuje-aplikacji`, `/audyt` (lepszy crawl, link equity, UX) — wkomponowana w istniejący układ stopki. (2) `layout.tsx`: schemat `Organization` → `ProfessionalService` (+logo, email, telefon, adres+region, `areaServed` Poznań/Wielkopolska/Polska, `sameAs` GitHub+LinkedIn, role founderów, alternateName, priceRange, knowsLanguage) pod rich results/GEO; `foundingDate` celowo pominięty (brak pewnego źródła — zasada „no fabricated stats"). (3) `next.config.ts`: nagłówki bezpieczeństwa (X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, HSTS) + `images.formats` AVIF/WebP + `minimumCacheTTL` + `poweredByHeader:false` (świadomie BEZ CSP — ryzyko zerwania inline gtag/Clarity). (4) `providers.tsx`: link „Przejdź do treści" (skip-to-content, WCAG, sr-only — niewidoczny do czasu fokusu klawiaturą). (5) `ProjectDetailClient`: opisowe `alt` zamiast „Detail 1/2". Wygląd niezmieniony. | `next.config.ts`, `src/app/layout.tsx`, `src/components/providers.tsx`, `src/components/footer.tsx`, `src/app/projects/[slug]/ProjectDetailClient.tsx`, `progress.md` |
| 2026-05-29 | **Tracking Faza 1 — konwersje GA4 + atrybucja gclid/UTM (feat/tracking-phase1):** NOWE `src/lib/tracking.ts` — przechwyt `gclid/gbraid/wbraid` + `utm_*`, trwałe w localStorage („ostatni klik kampanii wygrywa"), helpery `trackLead`/`trackContactClick` zgodne z Consent Mode v2 (eventy lecą bezwarunkowo, CM v2 modeluje gdy brak zgody). NOWE `src/components/analytics-tracker.tsx` montowane w `providers.tsx`: przechwyt atrybucji na mount + delegowane śledzenie klików `tel:`/`mailto:` w całym serwisie. `quick-contact.tsx` i `audyt/AuditClient.tsx` odpalają GA4 `generate_lead` na sukcesie i doklejają atrybucję do payloadu. `/api/contact` + `/api/audit/lead` przyjmują pola atrybucji (Zod) i wstrzykują źródło leada (źródło/medium/kampania/słowo/gclid/strona wejścia) do maila i Telegrama. Usunięto martwą atrapę `contact.tsx` + jej nieaktualny test `contact-form.test.tsx` (prawdziwy formularz to QuickContact). | `src/lib/tracking.ts` (NEW), `src/components/analytics-tracker.tsx` (NEW), `src/components/providers.tsx`, `src/components/quick-contact.tsx`, `src/app/audyt/AuditClient.tsx`, `src/app/api/contact/route.ts`, `src/app/api/audit/lead/route.ts`, usunięte `src/components/contact.tsx` + `src/__tests__/components/contact-form.test.tsx`, `progress.md` |
| 2026-05-29 | **/audyt + /software-house-poznan + OG image + llms.txt + /ile-kosztuje-aplikacji (gałąź feat/audyt-seo-og, baza origin/main):** Pogodzono rozjechany lokalny eksperyment — przeprojektowany one-pager odłożony na gałąź `experiment/conversion-landing`, jako kanon zostaje LIVE (origin). Przeniesiono na bazę live: lead magnet `/audyt` (page + AuditClient + `/api/audit` scoring PageSpeed/heurystyka + `/api/audit/lead` z RODO-zgodą → Telegram + Resend) oraz landing SEO `/software-house-poznan`. NOWE `src/app/opengraph-image.tsx` — brandowany OG 1200×630 przez `next/og` (naprawia brak og:image na KAŻDYM udostępnieniu, m.in. LinkedIn/FB). NOWE `public/llms.txt` (widoczność w LLM/GEO). NOWA strona `/ile-kosztuje-aplikacji` (answer-first, schema FAQPage+Breadcrumb, widełki spójne z /cennik). `sitemap.ts` +3 URL (software-house-poznan 0.95, ile-kosztuje 0.9, audyt 0.9) ORAZ generowanie wpisów projektów z tablicy `projects` — wszystkie 8 case studies (w tym klienci Jedmar/WKS/Posnania/WSafe) trafiają teraz do sitemap.xml (wcześniej tylko 4). `seo.test.ts` — naprawiony już-zepsuty `toHaveLength(5)` (sitemap miał 11) → asercje obecności URL + brak duplikatów. `layout.tsx` — dodany `@id` do schematu Organization (żeby `provider` w schematach podstron się rozwiązywał). AuditClient `--theme-text-2-rgb`→`--theme-text-1-rgb` (niezdefiniowany na origin). | `src/app/audyt/*`, `src/app/api/audit/**`, `src/app/software-house-poznan/page.tsx`, `src/app/opengraph-image.tsx` (NEW), `public/llms.txt` (NEW), `src/app/ile-kosztuje-aplikacji/page.tsx` (NEW), `src/app/sitemap.ts`, `src/__tests__/seo.test.ts`, `src/app/layout.tsx`, `progress.md` |
| 2026-04-11 | **Light mode toggle (feat/concept-a-fusion):** Added full light mode theme with inverted colors. Dark mode (default) looks EXACTLY the same — zero visual changes. (1) `globals.css` — Added `:root` and `[data-theme="light"]` CSS variable sets: `--theme-bg-1..4`, `--theme-border-1..2`, `--theme-text-1..2`, `--theme-accent`, RGB variants for opacity-based usage (`--theme-*-rgb`), `--theme-alt-*` for cream/dark-forest panel inversion, and `--theme-nav-*` for navbar pill. `@theme inline` now references all theme vars. Body + logoGlow + shimmerSweep keyframes use RGB vars. (2) `src/lib/theme.tsx` — New `ThemeProvider` context with `useTheme()` hook. Persists to `localStorage["programo-theme"]`, toggles `data-theme` attr on `<html>`. (3) `layout.tsx` — Inline `<script>` runs before hydration to set `data-theme` from localStorage (prevents flash on first load). (4) `page.tsx` — Wraps `<I18nProvider>` in `<ThemeProvider>`. (5) `navbar.tsx` — Added sun/moon SVG theme toggle button on both desktop (next to lang toggle) and mobile pill. All `#051F20` text → `var(--theme-nav-text)`, all pill bg/borders → `var(--theme-nav-pill*)`, opacity variants use rgba with `--theme-nav-text-rgb`. (6) `hero.tsx` — All `#051F20`/`#0A2A28`/`#163832`/`#DAF1DE`/`#8EB69B` → theme vars. `whileHover={{ backgroundColor: "#0A2A28" }}` → `var(--theme-bg-2)`. Shadow glows use theme vars. (7) `horizontal-intro.tsx`, `featured-work.tsx`, `tech-stack.tsx`, `contact.tsx`, `about.tsx` — All theme hex codes replaced with CSS var references. `about.tsx` cream panel uses `--theme-alt-*` vars (inverted: dark forest in light mode). Brand colors (React/TypeScript/etc.) and project accent colors preserved — never replaced. Build: 0 errors, 14 pages. | `src/app/globals.css`, `src/lib/theme.tsx` (new), `src/app/layout.tsx`, `src/app/page.tsx`, `src/components/navbar.tsx`, `src/components/hero.tsx`, `src/components/horizontal-intro.tsx`, `src/components/featured-work.tsx`, `src/components/about.tsx`, `src/components/tech-stack.tsx`, `src/components/contact.tsx`, `progress.md` |
| 2026-04-11 | **Scroll progress, project previews, about whileInView reveals, WSafeFinanse rename (feat/concept-a-fusion):** (1) NEW `src/components/scroll-progress.tsx` — Fixed vertical progress bar on the right (40vh track, gradient fill, spring-smoothed `scaleY` from `useScroll`) plus top horizontal bar on mobile. `z-[60]`, `pointer-events-none`. Mounted in `page.tsx` HomeContent OUTSIDE of `<PageTransition>` so it stays visible during transitions. (2) `projects.ts` — Renamed `safe-finanse` -> `wsafefinanse`, title `Safe Finanse` -> `WSafeFinanse` (liveUrl `wsafefinance.pl` and accentColor `#2563eb` unchanged). Added `screenshots` to all 4 new projects: jedmar-hero.png, wks-hero.png, ks-posnania-hero.png, wsafefinanse-hero.png. Added `liveUrl` to WKS Poznan (`wks-strona.vercel.app`) and KS Posnania (`ks-posnania.vercel.app`). (3) `featured-work.tsx` — Added always-visible "VISIT ↗" pill button in top-right of ProjectCell for `category === "strony-zrobione"` with `liveUrl`. Uses `e.stopPropagation()` + `pointer-events-auto` to escape the parent `<Link>` wrapper and open live site in a new tab. Border uses `accentColor` at 40% alpha. (4) `about.tsx` — Replaced all `useScroll`+`useTransform` scroll-progress animations with `whileInView` + `viewport={{ once: true, margin: ... }}` so animations trigger when entering view (fix for anchor jump to `#about` showing final state already). `RevealText` words stagger via `delay: i * 0.12`, `FadeInText` simple y+opacity, quote section: ghost FUTURE scale-in, quote fade+y, borders scaleX from center. Removed `useRef`, `useScroll`, `useTransform` imports (only `motion` left). Build: 0 errors, 14 static pages. | `src/components/scroll-progress.tsx` (NEW), `src/app/page.tsx`, `src/lib/projects.ts`, `src/components/featured-work.tsx`, `src/components/about.tsx`, `public/screenshots/*-hero.png`, `progress.md` |
| 2026-04-11 | **Pre-production polish (feat/concept-a-fusion):** (1) `about.tsx` — Split into half-dark/half-cream layout. TOP (dark `bg-[#0A2A28]`): RevealText + heading + paragraphs with light text (`text-on-surface`, `text-primary`). BOTTOM (cream `bg-[#FAF8F4]` with `rounded-t-[32px] 2xl:rounded-t-[48px]`): stats + animated quote + founders with dark text (`text-[#1A1816]`, `text-[#6B6560]`, `text-[#8EB69B]`). Added scroll-driven animated quote section with `useScroll`/`useTransform` hooks: FUTURE ghost text scales 0.5->1 with opacity 0->0.05, quote fades in with y:60->0, top/bottom borders scale from center. Mobile spacing reduced (mt-20 instead of mt-32). (2) `projects.ts` — Replaced placeholder projects "sklep-x" and "korpo-y" with 4 real projects: Jedmar (e-commerce, #ffd333), WKS Poznan (sports club, #c41e3a), KS Posnania (sports club, #004e75), Safe Finanse (financial advisory, #2563eb). All in category "strony-zrobione". First 4 projects (Estalo/Baulx/Athlix/LearnAI) unchanged. (3) Responsive polish: `contact.tsx` send button h-28/w-28 on mobile (was h-40/w-40), stat values capped at 2xl:text-7xl (was 5vw which got too large at 2560px). Build: 0 errors, 14 static pages generated (8 project slugs). | `src/components/about.tsx`, `src/lib/projects.ts`, `src/components/contact.tsx`, `progress.md` |
| 2026-04-11 | **Navbar contrast fix + About section cream background (feat/concept-a-fusion):** (1) `navbar.tsx` — Fixed unreadable text on white pill nav. Changed all text colors from theme tokens (`text-on-surface`, `text-on-surface-variant`, `text-primary`) to explicit dark `text-[#051F20]` with appropriate opacities. Desktop: logo, nav links (active/inactive/hover), language toggle all use `#051F20`. Mobile pill: hamburger lines `bg-[#051F20]`, logo and language toggle `text-[#051F20]`. Removed `MagneticWrapper` from CTA button (replaced with plain `<a>`), removed unused import. Mobile overlay menu kept as-is (dark bg + light text). (2) `about.tsx` — Changed section background from `bg-[#0A2A28]` (dark green) to `bg-[#FAF8F4]` (warm cream). Added `rounded-b-[32px] 2xl:rounded-b-[64px]` for smooth transition to dark section below. All text colors updated for dark-on-light: headings `text-[#1A1816]`, paragraphs `text-[#1A1816]/80`, labels `text-[#6B6560]`, accents `text-[#8EB69B]`. Stats, borders, founder cards, Premium Agency circle, decorative elements all updated. RevealText made more dramatic: scroll offset `["start 0.9", "end 0.3"]`, y translation `[80, 0]`, added scale `[0.9, 1]`, opacity starts at `0.02`. FadeInText more dramatic: offset `["start 0.95", "start 0.5"]`, opacity starts at `0.0`, added y transform `[30, 0]`. Build: 0 errors. | `src/components/navbar.tsx`, `src/components/about.tsx` |
| 2026-04-11 | **Fix mobile crash + animations + accent colors + lighter sections (feat/concept-a-fusion):** (1) `hero.tsx` — CRITICAL: fixed hydration mismatch crash on mobile. Replaced all `{!isMobile && <Component />}` conditional renders with `<div className="hidden md:contents">` CSS approach. Components always render in DOM but are hidden on mobile via CSS. Removed `isMobile` state + useEffect entirely. Replaced `style={{ height: isMobile ? ... }}` with CSS `h-[150vh] md:h-[200vh]`. Added staggered `animate-fadeSlideIn` entrance animation on each bento cell (delays 0.1s-0.9s). Added `animate-logo-glow` pulsing shadow on "pr." text. Added `animate-shimmer-sweep` diagonal gradient on "digital craftsmanship" cell. Replaced framer-motion scanner lines with direct DOM manipulation via refs for perf. (2) `horizontal-intro.tsx` — Removed `isMobile` state + useEffect. Replaced inline height style with CSS `h-[200vh] md:h-[250vh]`. Added individual letter animations on "programo" text (`animate-letterReveal` with staggered delays). Added vertical parallax on manifesto block via `useTransform`. Made ticker more energetic with 8 copies + `animate-tickerPulse` glow effect. (3) `featured-work.tsx` — Removed `isMobile` state + useEffect + prop. Each project card now uses `project.accentColor` for: scanning line color, status dot glow, bottom overlay left border, and tag backgrounds on hover. Removed `grayscale group-hover:grayscale-0` — images now show in color with dark overlay that lifts on hover. (4) `tech-stack.tsx` — Section background changed to `bg-gradient-to-b from-[#051F20] via-[#0B2B26] to-[#051F20]`. TechCard background: `bg-[#0B2B26]`, border: `border-[#235347]/30`. Added `techColors` map (15 technologies with unique colors). Icon circle uses per-tech color. Added dynamic top accent line on hover in tech color. Added radial gradient glow on hover. Heading now has gradient text (`bg-gradient-to-r from-[#DAF1DE] via-[#8EB69B] to-[#DAF1DE]`). Tech-card-line pseudo-element uses `--tech-color` CSS var. (5) `about.tsx` — Background changed from `bg-surface-container` to `bg-[#0A2A28]` for lighter contrast. Removed special characters (Polish diacritics) that could cause encoding issues. (6) `contact.tsx` — Added top accent gradient line `via-[#8EB69B]` at section top. (7) `globals.css` — 6 new keyframe animations: `fadeSlideIn`, `logoGlow`, `shimmerSweep`, `letterReveal`, `tickerPulse`. Tech-card-line pseudo uses `--tech-color` CSS variable. Build: 0 errors. | `src/components/hero.tsx`, `src/components/horizontal-intro.tsx`, `src/components/featured-work.tsx`, `src/components/tech-stack.tsx`, `src/components/about.tsx`, `src/components/contact.tsx`, `src/app/globals.css`, `progress.md` |
| 2026-04-11 | **Variant A — Dense Bento + Horizontal Scroll Intro fusion (feat/concept-a-fusion):** (1) Nowy komponent `horizontal-intro.tsx` — horizontal scroll intro z gigantycznym "programo" textem, "Software Engineering" subtitle, "Digital Precision" manifesto block, 3-warstwowy parallax (background serif text, main content, foreground ticker), scroll-driven motion z useSpring (damping:40, stiffness:80), mobile-optimized height (200vh mobile / 250vh desktop), GPU-accelerated transforms, `contain: layout style paint` na sticky container. (2) `page.tsx` — dodano HorizontalIntro przed Hero w main content flow. (3) `globals.css` — marquee slow-down: slideLeft 40s→90s, slideRight 50s→110s. Dodano `.tech-card-line::before` pseudo-element animacja (gradient line slides across top on hover). (4) `tech-stack.tsx` — TechCard: dodano hover glow (`hover:shadow-[0_0_30px_rgba(142,182,155,0.1)]`), gradient border effect (`hover:bg-gradient-to-br hover:to-primary/5`), icon rotation on hover (`group-hover:rotate-12`), decorative dot pulse (`group-hover:animate-pulse`), `tech-card-line` class for pseudo-element. Section heading: staggered entrance animations z custom easing, dodano animated gradient separator line. (5) `hero.tsx` — Mobile optimization: dynamic height (150vh mobile / 200vh desktop), ukryto AIScannerCell/QR/Radar na mobile, powiększono logo "pr." na mobile (text-8xl), uproszczono grid na mobile (grid-rows-4). Build: 0 errors. | `src/components/horizontal-intro.tsx` (NEW), `src/app/page.tsx`, `src/app/globals.css`, `src/components/tech-stack.tsx`, `src/components/hero.tsx` |
| 2026-04-11 | **Deep performance fix + large screen optimization (feat/concept-dense-bento):** (1) layout.tsx — usunięto LenisProvider wrapper i noise overlay div, body renderuje bezpośrednio children. (2) preloader.tsx — kompletny rewrite: usunięto words array i word cycling, tylko "PROGRAMO" fade-in, progress 0→100 w 0.3s (interval 15ms, step 5), usunięto 500ms setTimeout delay, exit animation 1s→0.4s. (3) tech-stack.tsx — TechCard: motion.div→plain div z CSS hover transition (hover:-translate-y-2.5), usunięto framer-motion props (initial/whileInView/whileHover), usunięto index prop. Marquee duplication 3x→2x (30 items mniej). Usunięto background blur decoration div. (4) contact.tsx — MagneticWrapper→plain div, usunięto import. Usunięto bottom "CONTACT US" marquee animation. (5) hero.tsx — 2xl breakpoint: pr. text 2xl:text-[12vw], "digital craftsmanship" 2xl:text-7xl, grid cells 2xl:p-10 (9 cells). (6) featured-work.tsx — 2xl breakpoint: "Archiwum" heading 2xl:text-[6rem], project cells 2xl:min-h-[450px], project title 2xl:text-4xl. Build: 0 errors. |  `src/app/layout.tsx`, `src/components/preloader.tsx`, `src/components/tech-stack.tsx`, `src/components/contact.tsx`, `src/components/hero.tsx`, `src/components/featured-work.tsx` |
| 2026-04-11 | **Performance optimization (feat/concept-dense-bento):** Redukcja lag na M2 Mac. (1) hero.tsx — AIScannerCell: dodano 32ms throttle na mousemove (~30fps zamiast każdego eventu), obniżono spring stiffness 500→200. Ambient scanner: zamieniono framer-motion animate loop na CSS animation (animate-scanner-sweep). Bar chart: 6 indywidualnych motion.div animate loops zamienione na CSS animation (animate-bar-pulse). QR pattern: ~13 indywidualnych motion.div opacity animate loops zamienione na CSS animation (animate-qr-blink). (2) featured-work.tsx — AnimatePresence mode="popLayout"→"wait" (eliminuje layout recalculations), usunięto layout="position" z ProjectCell, zmniejszono scale animację 0.8→0.95 i czas 0.5s→0.3s. Zastąpiono backdrop-blur-sm i backdrop-blur-md na solidne bg-[#051F20]/90 i bg-[#051F20]/95 (usunięto warunkowe isMobile checks). (3) globals.css — dodano 3 GPU-accelerated CSS keyframes: scanner-sweep (translate3d), bar-pulse (scaleY), qr-blink (opacity). Build: 0 errors. | `src/components/hero.tsx`, `src/components/featured-work.tsx`, `src/app/globals.css` |
| 2026-03-24 | **Athlix project page overhaul — images + capabilities:** (1) projects.ts — Athlix entry: usunięto 4 nieistniejące PLACEHOLDER screenshoty, zastąpiono 3 prawdziwymi ikonami aplikacji (athlix-trainpilot-icon.png, athlix-trainmate-icon.png, athlix-health-icon.png). Rozbudowane features z 15 do 18 pozycji pogrupowanych tematycznie (Garmin+Strava Sync, AI Coaching, Performance Analytics, Nutrition Engine, BLE HR, WHOOP Reverse-Engineered, 20+ HRV Algorithms, Recovery & Readiness, Strain & TRIMP, Sleep Analytics, Social Discovery, Gym Module, Community, Body Composition, Seasonal Supplements, PWA+Capacitor, Security). longDescription wzbogacone o metryki (839 testów, 142 API, 64 tabel, 1879 LOC algorytmów). description zaktualizowane z konkretnymi liczbami. (2) ProjectDetailClient.tsx — ProjectImage: dodana obsługa icon-based screenshots (isIcon detection), renderowanie centered na radial-gradient tle z dot pattern, rounded shadow box, accent glow. Hero section: nowy ecosystem layout dla icon screenshots — 3 ikony w rzędzie z labelami (TrainPilot/TrainMate/Health), animowane staggered entrance, gradient tło z accentColor. Middle/bottom image sections: automatyczne ukrywanie dla hasIconScreenshots (bo ikony już w hero). Przekazywanie accentColor do wszystkich ProjectImage instancji. Next Project footer: pomijanie icon-based screenshots. (3) projects.test.ts — naprawiono pre-istniejący bug: test oczekiwał athlix status "development" ale dane mają "live". Build: 0 errors, 10/10 static pages. 17/17 projects tests passing. | `src/lib/projects.ts`, `src/app/projects/[slug]/ProjectDetailClient.tsx`, `src/__tests__/projects.test.ts` |
| 2026-03-22 | **Preloader simplification + scroll-reveal animations:** (1) preloader.tsx — kompletny rewrite: usunięto char-by-char stagger animation (~1.9s), zastąpiono prostym opacity 0→1 fade-in (400ms) całego słowa "Programo" + 300ms hold + 500ms curtain slideUp. Total ~1s zamiast ~1.9s. Tekst: font-headline text-3xl font-semibold tracking-tight. Tło: bg-on-surface. Exit: translateY(-100%) z easePageTransition. (2) contact.tsx — scroll-reveal animations: dark card clipPath inset(10% 5% 10% 5% round 32px)→inset(0%) over 0.8s, left column staggered content (label/h2/desc/emails fade+translateY z staggerItem), right column form slides in from x:40 with 0.3s delay, background gradient scales 0.5→1 on scroll. Imports: easeEntry, durationMedium, durationSlow, staggerItem. (3) about.tsx — section-level clipPath reveal inset(3% 2% 0% 2% round 40px)→inset(0%) subtle, stat boxes enhanced with scale(0.9→1) + opacity + translateY stagger (was just opacity+y). All existing animations preserved (word-by-word heading, animated counters, founder stagger, Poznań clip-reveal). Build: 0 errors, 10/10 static pages. | `src/components/preloader.tsx`, `src/components/contact.tsx`, `src/components/about.tsx` |
| 2026-03-22 | **Fix screenshot display across portfolio site:** Naprawiono dimension mismatches powodujace zly wyswietlanie screenshotow. (1) projects.ts — Estalo screenshots: dodano estalo-1.png (1440x900 full-page) jako PIERWSZY, usunieto estalo-2.png (620x420, za maly na hero) i estalo-email-gen.png (zmniejszenie do 6). Baulx bez zmian. Athlix juz nie mial screenshots (512x512 PWA icons nie sa screenshotami). (2) ProjectDetailClient.tsx — Hero screenshot: object-contain→object-cover, max-w-5xl→max-w-6xl, usunieto border border-outline-variant/10. Gallery screenshots: object-contain→object-cover, max-w-5xl→max-w-6xl, usunieto border, space-y-4→flex flex-col gap-6, zachowano rounded-2xl overflow-hidden. Wszystkie animacje zachowane (motion initial/animate/whileInView). (3) featured-work.tsx — juz mial poprawne object-cover, bez zmian potrzebnych. | `src/lib/projects.ts`, `src/app/projects/[slug]/ProjectDetailClient.tsx` |
| 2026-03-22 | **About, Contact & Footer typography/spacing refinement:** Premium portfolio best practices. About: h2 text-5xl/7xl→text-3xl/4xl/5xl + font-semibold + tracking-tight, paragraphs added text-base md:text-lg + font-normal + max-w-xl, founder names text-3xl/4xl→text-2xl/3xl font-medium, founder role labels text-xs→text-[11px] font-medium, stat numbers text-5xl→text-4xl font-semibold, stat labels text-xs→text-[11px] font-medium, stat box padding p-12→p-8 md:p-10, section py-40→py-24/32/40 responsive, grid gap-24→gap-16 lg:gap-20, heading mb-12→mb-8, founders mt-16 pt-12→mt-12 pt-8. Contact: h2 text-5xl/7xl/8xl→text-4xl/5xl/6xl font-semibold, section label text-xs→text-[11px] font-medium, email links font-headline text-xl/2xl→text-lg/xl font-normal, form labels text-xs→text-[11px], submit font-semibold→font-medium, section py-40→py-20/28/32, card padding px-24 py-32→px-16 py-20/24, card rounded-[40px]→rounded-[32px]. Footer: logo text-4xl→text-2xl font-semibold, copyright text-sm→text-xs, availability text-xs→text-[10px] font-medium, footer py-20→py-12/16, gap-16→gap-10, bottom pt-12→pt-8, rounded-t-[40px]→rounded-t-[32px], mt-20→mt-12. All animations preserved. | `src/components/about.tsx`, `src/components/contact.tsx`, `src/components/footer.tsx` |
| 2026-03-22 | **Navbar typography & spacing refinement:** Premium portfolio best practices. Typography: Logo "Programo" text-3xl font-medium -> text-xl font-semibold tracking-tight (too big for nav). Nav links text-sm font-semibold tracking-tight -> text-[13px] font-medium (too heavy, removed tracking-tight). Language toggle text-sm -> text-[13px] font-medium. CTA button text-xs font-semibold tracking-widest -> text-[13px] font-medium tracking-wide, padding px-6 py-3 -> px-5 py-2.5. Mobile logo text-2xl font-bold tracking-tighter -> text-lg font-semibold tracking-tight. Mobile menu links text-3xl -> text-2xl font-normal. Mobile CTA font-semibold -> font-medium. Spacing: Desktop pill px-10 py-4 -> px-8 py-3, nav link gap-12 -> gap-8, right side gap-6 -> gap-4, mobile pill px-6 gap-8 -> px-5 gap-6. All animations preserved (hide/show scroll, layoutId indicator, AnimatePresence mobile menu, clipPath reveal). | `src/components/navbar.tsx` |
| 2026-03-22 | **Featured Work & Tech Stack typography/spacing refinement:** Premium portfolio best practices. Featured Work: section h2 text-5xl/7xl→text-3xl/4xl/5xl + font-semibold + leading-tight + tracking-tight, section label text-xs→text-[11px] font-medium, project category tags font-bold→font-medium, project h3 text-4xl→text-2xl/3xl font-medium, description added explicit font-normal. Spacing: section py-32→py-20/28/32 responsive, header mb-24→mb-16, grid gap-x-12 gap-y-32→gap-x-8 gap-y-20 md:gap-x-10 md:gap-y-24, image mb-8→mb-6, odd card offset md:mt-24→md:mt-16. Tech Stack: section label text-xs font-bold→text-[11px] font-medium, h2 text-5xl/7xl→text-3xl/4xl/5xl + font-semibold + leading-tight + tracking-tight, description added text-base/lg font-normal, tech item name font-bold→font-medium. Spacing: section py-40→py-20/28/32 responsive, header mb-32→mb-16/mb-20, h2 mt-6→mt-4, marquee gap-4→gap-3. All animations preserved (clip-path reveals, directional stagger, marquee, motion imports). | `src/components/featured-work.tsx`, `src/components/tech-stack.tsx` |
| 2026-03-22 | **Hero typography & spacing refinement:** Premium portfolio best practices. Typography: overline label font-bold→font-medium, h1 text-7xl/9xl/10rem→text-5xl/7xl/8xl + font-semibold + tracking-tight + leading-[0.95], description text-xl/2xl→text-base/lg + font-normal + max-w-2xl (was max-w-xl), CTA buttons font-bold→font-medium + tracking-widest→tracking-wide + px-8 py-4→px-6 py-3, "O nas" link font-bold→font-normal. Spacing: section padding refined (px-8 pt-28 pb-32→px-6 pt-24 pb-20 with md/lg breakpoints), added inner max-w-7xl container, mt-8→mt-6 (h1→desc gap), mt-12→mt-10 (desc→CTA gap). All animations preserved (SplitHeading, SplitDescription, parallax, fadeUp, useReducedMotion). | `src/components/hero.tsx` |
| 2026-03-22 | **Fix screenshot rendering in project detail page:** Naprawiono problem z zoomed/stretched screenshots na stronach projektów. Obrazy miały hardcoded `width={1920} height={1080}` ale rzeczywiste screenshoty mają różne wymiary (np. 2268x730, 1440x900), co powodowało zniekształcenia. Zmieniono zarówno hero screenshot jak i gallery screenshots na: kontener `relative aspect-[16/9]` z `<Image fill className="object-contain">` zamiast `width/height`, dodano `bg-surface-container-low` jako tło pod object-contain, `border border-outline-variant/10` jako subtelną ramkę, zmniejszono max-width z `max-w-7xl` na `max-w-5xl`. | `src/app/projects/[slug]/ProjectDetailClient.tsx` |
| 2026-03-22 | **Featured work screenshot images:** Zmieniono sekcję Featured Work — karty projektów z screenshots[] (Estalo, Baulx) teraz wyświetlają pierwszy screenshot jako next/image zamiast gradient placeholder z literą. Import Image z next/image, conditional render: screenshots.length > 0 → Image fill + object-cover + responsive sizes, fallback → dotychczasowy gradient z literą (Athlix, LearnAI). Zachowane wszystkie animacje: clip-path reveal, hover scale(1.05), directional stagger. | `src/components/featured-work.tsx` |
| 2026-03-22 | **Comprehensive projects data update:** Przebudowa danych wszystkich 4 projektow w projects.ts. (1) Estalo — rozszerzone opisy (longDescription z sekcjami AI/Portal/Enterprise/Security), tech stack z 6 do 12 pozycji, features z 8 do 18 (PL+EN), nowe screenshots (6 zamiast 2), tagi "Marketplace"→"Enterprise". (2) Baulx — subtitle skrocony, description+longDescription zaktualizowane (nowe szczegoly: 103 moduly JS, 1909 testow, konkretne post-procesory CNC), tech stack zaktualizowany (dodano Supabase, Anthropic Claude), features z 12 do 14 (dodano metryki: testy, moduly, WCAG), tagi "SaaS"→"3D". (3) Athlix — rozszerzony z 2 do 3 aplikacji (TrainPilot+TrainMate+Health), subtitle/description/longDescription kompletnie przebudowane z pelnym opisem kazdej apki, tech stack z 6 do 13 pozycji (dodano Pusher, Strava API, Garmin, Google Maps, Recharts etc), features z 8 do 15 (PL+EN), tagi "Social/Analytics"→"Health/AI", year "2026"→"2025-2026". (4) LearnAI — tech stack: "AI"→"Azure OpenAI". TypeScript compiles: 0 errors. | `src/lib/projects.ts` |
| 2026-03-22 | **Footer parallax + mobile menu clipPath reveal:** (1) footer.tsx — dodano scroll-linked parallax: useRef + useScroll({ target, offset: ["start end","end end"] }) + useTransform mapuje scrollYProgress na y:[60,0], wewnetrzny content laguje za scrollem tworzac efekt "reveal/emerging". Bottom row (copyright + availability) uzywa fadeInUp variant z whileInView. Import: useRef, motion, useScroll, useTransform, easeEntry, durationMedium, fadeInUp z @/lib/motion. overflow-hidden na footer. (2) navbar.tsx — mobile menu overlay zmieniony z prostego opacity fade na clipPath circle reveal: "circle(0% at 90% 5%)" -> "circle(150% at 90% 5%)" (rozszerza sie z top-right gdzie jest hamburger button), duration 0.6s z easeEntry, exit: reverse clip-path. Link stagger zwiekszone z 0.08s do 0.1s, dodano x:-30 initial state (slide from left) obok istniejacego y:20 dla bardziej dramatycznego wejscia. Dodano easeExit import. Exit animations na linkach. (3) ProjectDetailClient.tsx — naprawiono Turbopack parsing error: em dash character "—" zamieniony na "&mdash;" HTML entity (2 wystapienia w prev/next nav). Build: 0 errors, 10/10 static pages. | `src/components/footer.tsx`, `src/components/navbar.tsx`, `src/app/projects/[slug]/ProjectDetailClient.tsx` |
| 2026-03-22 | **Marquee ticker tech stack + hover underline animations:** (1) tech-stack.tsx — kompletny rewrite: statyczny grid zastąpiony dwurzędowym continuous horizontal scrolling marquee. Dwa rzędy (8+7 technologii) scrollują w przeciwnych kierunkach (lewo/prawo). Framer-motion `useAnimationControls` z `repeat: Infinity`, `repeatType: "loop"`, `ease: "linear"`, duration 30s. Items triplicated (3x) dla seamless loop. Pause on hover via `controls.stop()` / `controls.start()`. Pill/card styling: rounded-xl, border, bg-surface-container-lowest. Section header zachowany z whileInView animations (fadeInUp, clipRevealUp z @/lib/motion). (2) globals.css — dodano `.hover-underline` CSS class z pseudo-element ::after animated underline: scaleX(0)->scaleX(1) na hover, transform-origin flip (right->left) dla directional slide effect, cubic-bezier(0.16,1,0.3,1) easing. (3) footer.tsx — GitHub i LinkedIn links zmienione z Tailwind `hover:underline` na custom `.hover-underline` class dla premium animated underline effect. (4) Bonus fixes: naprawiono pre-existing TypeScript cast w preloader.tsx (number[] -> [number,number,number,number]) i pre-existing JSX parse error w ProjectDetailClient.tsx (indentation fix). Build: 0 errors, 10/10 static pages. | `src/components/tech-stack.tsx`, `src/app/globals.css`, `src/components/footer.tsx`, `src/components/preloader.tsx`, `src/app/projects/[slug]/ProjectDetailClient.tsx` |
| 2026-03-22 | **Branded preloader & page transitions:** (1) Nowy komponent preloader.tsx — full-viewport ciemna kurtyna (bg-on-surface, z-[9998]) z animacją tekstu "Programo" char-by-char (translateY(100%)->0, stagger 0.04s, easeEntry), hold 0.3s, potem kurtyna slideUp (-100%) z easePageTransition. AnimatePresence mode="wait" do zarządzania exit animation. useState+useEffect timer (~1.9s). SSR-safe. (2) Nowy komponent page-transition.tsx — wrapper z framer-motion motion.div: opacity 0->1, y 20->0, duration 0.6s, easeEntry. (3) Integracja w page.tsx: Preloader dodany na top-level wewnątrz I18nProvider, cały HomeContent (Navbar+main+Footer) owinięty w PageTransition. (4) Integracja w ProjectDetailClient.tsx: content owinięty w PageTransition dla spójnej animacji wejścia na podstronach projektów. Import easeEntry/easePageTransition/durationMedium z @/lib/motion. Build: 0 errors, 10/10 static pages. | `src/components/preloader.tsx` (nowy), `src/components/page-transition.tsx` (nowy), `src/app/page.tsx`, `src/app/projects/[slug]/ProjectDetailClient.tsx` |
| 2026-03-22 | **Magnetic button wrapper:** Nowy komponent MagneticWrapper (magnetic.tsx) — opakowuje dowolny element i dodaje efekt magnetycznego przyciągania kursora. Używa framer-motion useMotionValue + useSpring z springMagnetic config (stiffness 150, damping 15, mass 0.27). Na onMouseMove oblicza offset od centrum elementu i przesuwa go max 8px w stronę kursora. Na onMouseLeave springuje z powrotem do (0,0). Props: children + strength (domyślnie 0.3 — mnożnik siły). Zintegrowany w: (1) navbar.tsx — CTA button "Napisz do nas" owinięty w MagneticWrapper. (2) hero.tsx — "Zobacz projekty" border button owinięty w MagneticWrapper. Dodatkowo naprawiono pre-existing TypeScript errors w page-transition.tsx i preloader.tsx (easeEntry/easePageTransition cast as unknown as number[] → as [number, number, number, number]). Build: 0 errors. | `src/components/magnetic.tsx` (nowy), `src/components/navbar.tsx`, `src/components/hero.tsx`, `src/components/page-transition.tsx`, `src/components/preloader.tsx` |
| 2026-03-22 | **Custom cursor component:** Dodano premium custom cursor z framer-motion. (1) Nowy komponent custom-cursor.tsx — 20px circle podazajacy za myszka z spring physics (springCursor: stiffness 150, damping 15, mass 0.1). (2) mix-blend-mode: difference dla kontrastu na kazdym tle. (3) Trzy warianty: default (20px), pointer (40px, na hover a/button), view (60px z tekstem "View", na elementach data-cursor="view"). (4) Detekcja interactive elements przez data-cursor attribute + auto-detection a/button z MutationObserver dla dynamicznych elementow. (5) Ukryty na mobile — window.matchMedia('(pointer: coarse)') check. (6) SSR-safe — useState(true) dla isTouchDevice, useEffect do detekcji. (7) AnimatePresence dla "View" label appear/disappear. (8) Zintegrowany w page.tsx wewnatrz I18nProvider ale poza HomeContent. (9) globals.css: @media (pointer: fine) { * { cursor: none !important; } } ukrywa domyslny kursor na non-touch devices. Build: 0 errors. | `src/components/custom-cursor.tsx` (nowy), `src/app/page.tsx`, `src/app/globals.css` |
| 2026-03-22 | **Premium animations (About section):** Dodano zaawansowane animacje do sekcji About. (1) AnimatedCounter component -- inline helper z useMotionValue + useTransform + animate, liczby "4+" (0->4), "2" (0->2), "2024" (2020->2024) animuja sie po wejsciu w viewport, easing [0.16,1,0.3,1], duration 1.5s. (2) "Poznan" clip-path reveal -- tekst pojawia sie animacja clipPath inset(0 100% 0 0) -> inset(0 0% 0 0). (3) Staggered stat boxes -- 4 boxy fade-in + slide-up z 0.1s stagger (custom index 0-3, prawa kolumna offset +2). (4) Word-by-word heading reveal -- naglowek rozdzielony na slowa, kazde z opacity+y+blur animacja, stagger 0.06s. (5) Paragraphs fade-in z translateY, staggered delays. (6) Founders stagger -- wjezdzaja z founderVariants (staggerItem delay). Import z @/lib/motion: easeEntry, durationMedium, durationSlow, staggerItem, fadeInUp. viewport={{ once: true }}. Build: 0 TS errors. | `src/components/about.tsx` |
| 2026-03-22 | **Premium navbar animations:** (1) Hide-on-scroll-down / show-on-scroll-up z requestAnimationFrame scroll tracking, delta thresholds (8px down / 5px up) i always-visible w top 100px. (2) Animated active section indicator z framer-motion `layoutId="nav-indicator"` — 2px primary underline smoothly morphs/slides between active nav links via springGentle. (3) Enhanced backdrop blur/shadow — scrolled state: bg-white/80, backdrop-blur-2xl, stronger shadow. (4) All transitions use shared motion constants (easeEntry, easeHover, durationFast, durationMedium, springGentle) z `@/lib/motion`. (5) Mobile nav also hides/shows on scroll (disabled when menu open). Build: 0 errors. | `src/components/navbar.tsx` |
| 2026-03-22 | **Premium animations (Hero):** Dodano zaawansowane animacje do sekcji Hero. (1) Split-text char-by-char reveal — nagłówek "Programo" rozbity na pojedyncze `<span>` znaki, każdy w overflow-hidden kontenerze, animacja translateY(100%) → 0 ze stagger 0.03s i easeEntry [0.16,1,0.3,1]. (2) Word-by-word reveal — opis (z i18n `t("hero.desc")`) dzielony na słowa, każde słowo fade+translateY ze stagger 0.02s i easeDefault. (3) Parallax on scroll — useScroll+useTransform: h1 przesuwa się -80px, opis -40px, scroll indicator +30px z fade-out. (4) useReducedMotion() — pełne wsparcie prefers-reduced-motion: przy reduced motion plain fade-in bez parallax/split. (5) Import easing/stagger/duration z @/lib/motion (easeEntry, easeDefault, staggerChar, staggerWord, durationMedium, durationSlow). Build: 0 errors TS. | `src/components/hero.tsx` |
| 2026-03-22 | **Premium animations (Featured Work):** Dodano zaawansowane animacje do sekcji Featured Work. (1) Clip-path image reveal on scroll — obrazy kart pojawiają się animacją clipPath inset od dołu do góry z easeEntry [0.16,1,0.3,1] i duration 0.9s. (2) Directional stagger — karty o parzystym indeksie (0,2) wjeżdżają z lewej (x:-60), nieparzyste (1,3) z prawej (x:60). (3) Section title clip-path reveal — nagłówek "Nasze realizacje" używa clipRevealUp variant z @/lib/motion. (4) Hover enhancements — gradient placeholder skaluje się do 1.05 na hover (overflow-hidden na kontenerze), subtle overlay z dużą literą inicjałową projektu. (5) Import easing/duration/stagger z @/lib/motion (easeEntry, easeDefault, durationSlow, durationMedium, staggerItem, clipRevealUp). viewport={{ once: true, margin: "-100px" }}. Build: 0 errors. | `src/components/featured-work.tsx` |
| 2026-03-22 | **Visual redesign:** Kompletna zmiana wyglądu strony z ciemnego sage/beige motywu na jasny, ciepły editorial design (amber/cream). (1) Fonty: Inter + Playfair Display → Newsreader + Plus Jakarta Sans. (2) Kolory: nowa paleta Material Design 3 — surface #f9f9f9, primary #775a19 (amber), on-surface #1a1c1c, outline-variant #d1c5b4. (3) Navbar: floating pill nav z rounded-full, backdrop-blur-xl, glass effect. (4) Hero: duża editorial typografia (7-10rem), grid layout 12-kolumnowy. (5) Featured Work: staggered grid (2nd/4th offset), editorial category labels, placeholder image cards. (6) About: rounded-t-[40px] container, stat boxy z editorial-shadow, sekcja founders z role labels. (7) Tech Stack: centered grid z gap-px separator, hover effects. (8) Contact: dark inverted section (bg-on-surface, rounded-[40px]), two-column layout (info + form). (9) Footer: light bg, rounded-t-[40px], social links, green availability indicator. (10) Project detail + 404: zaktualizowane do nowej palety kolorów. (11) CLAUDE.md zaktualizowany. Build: 0 errors. | `globals.css`, `layout.tsx`, `navbar.tsx`, `hero.tsx`, `featured-work.tsx`, `about.tsx`, `tech-stack.tsx`, `contact.tsx`, `footer.tsx`, `not-found.tsx`, `ProjectDetailClient.tsx`, `CLAUDE.md` |
| 2026-03-19 | **RUNDA 4 (pełna reweryfikacja):** Ponowny full audit PROMPT.md — naprawiono brakujące elementy z poprzednich rund. (1) Featured Work: dodano "Live" green badge (bg-emerald-400) dla live projektów, layout zmieniony z 1-kolumnowego na grid 2x2 (grid-cols-1 md:grid-cols-2), hover scale(1.02), mniejsze padding/font w karcie. (2) Navbar: dodano active section detection (IntersectionObserver na #work/#about/#stack/#contact), podświetlenie aktywnej sekcji. (3) WCAG AA contrast fix: sage-muted zmieniony z #7a8f7c (3.13:1) na #4e6752 (5.60:1) — teraz spełnia 4.5:1 na obu tłach. (4) i18n: dodano klucz "work.live" (Na żywo/Live). (5) API: dodano console.log fallback gdy brak RESEND_API_KEY. (6) Testy 102 → 117: +2 i18n (toggle, nested keys), +3 hero (lang toggle, flex-col, animations), +2 featured-work (live green badge, grid layout), +1 navbar (scroll bg), +2 contact-form (success reset, i18n toggle), +1 footer (i18n EN), +1 contact API (rate limit reset), +3 SEO (JSON-LD Organization, SoftwareApplication, canonical URLs). Build: 0 errors, 0 warnings, all pages ○ Static. Lint: 0 errors/warnings. Tests: 117/117 passing. | `src/components/featured-work.tsx`, `src/components/navbar.tsx`, `src/lib/i18n.tsx`, `src/app/globals.css`, `src/app/api/contact/route.ts`, `src/__tests__/i18n.test.ts`, `src/__tests__/components/hero.test.tsx`, `src/__tests__/components/featured-work.test.tsx`, `src/__tests__/components/navbar.test.tsx`, `src/__tests__/components/contact-form.test.tsx`, `src/__tests__/components/footer.test.tsx`, `src/__tests__/api/contact.test.ts`, `src/__tests__/seo.test.ts` |
| 2026-03-19 | **RUNDA 4 kontynuacja:** Full Production Audit wg PROMPT.md — pełna weryfikacja 78+ checklisty. Rozszerzono testy z 93 → 102: hero (+1: scroll indicator), featured-work (+2: status badge colors, article elements), navbar (+2: logo href, mobile menu open), contact-form (+2: max length validation, subject select options count), footer (+2: copyright text, semantic footer element). Audyt page-by-page: Hero, Featured Work, About, Tech Stack, Contact, Footer, Navbar, 4× Project Detail, 404, API, SEO, i18n, Accessibility — wszystko zgodne z checklistą. Build: 0 errors, 0 warnings, all pages ○ Static. Lint: 0 errors/warnings. Tests: 102/102 passing. | `src/__tests__/components/hero.test.tsx`, `src/__tests__/components/featured-work.test.tsx`, `src/__tests__/components/navbar.test.tsx`, `src/__tests__/components/contact-form.test.tsx`, `src/__tests__/components/footer.test.tsx` |
| 2026-03-18 | **RUNDA 4:** Full Production Audit. (1) Custom 404 page z i18n PL/EN — styled consistent z resztą strony. (2) Hero: mobile max 80svh cap (widać content poniżej) + vertical button stack na mobile (flex-col). (3) About: stats "3+" → "4+" (4 produkty), fix bug "Bartosz Kołaj" → "Bartosz Kolaj" (złe HTML entity), dodano placeholder avatary inicjałów founderów (WP, BK). (4) Featured Work: stagger animation z whileInView (0.1s delay między kartami), IntersectionObserver. (5) Contact: dodano kontakt@programo.pl jako główny email + social links (GitHub, LinkedIn) w sekcji kontakt. (6) Screenshot alt text: bardziej opisowy (tytuł + subtitle + zrzut ekranu PL/EN). (7) ProjectDetailClient: "Project not found" → i18n t("notFound.title"). (8) Testy: 70 → 93 (nowe: about.test.tsx ×6, tech-stack.test.tsx ×5; rozszerzono: i18n +2, projects +4, contact API +4, SEO +2). IntersectionObserver mock w test setup. (9) Build: 0 errors, 0 warnings, all pages ○ Static. Lint: 0 errors/warnings. Tests: 93/93 passing. | `src/app/not-found.tsx` (nowy), `src/components/hero.tsx`, `src/components/about.tsx`, `src/components/featured-work.tsx`, `src/components/contact.tsx`, `src/app/projects/[slug]/ProjectDetailClient.tsx`, `src/__tests__/setup.ts`, `src/__tests__/components/about.test.tsx` (nowy), `src/__tests__/components/tech-stack.test.tsx` (nowy), `src/__tests__/i18n.test.ts`, `src/__tests__/projects.test.ts`, `src/__tests__/api/contact.test.ts`, `src/__tests__/seo.test.ts` |
| 2026-03-18 | **RUNDA 3:** (1) Contact form z Zod walidacją, rate limiting, Resend API, inline errors, toast, i18n PL/EN. (2) SEO: robots.ts, sitemap.ts (5 URLs), JSON-LD Organization w layout + SoftwareApplication na project pages, OG/Twitter meta, canonical URLs. (3) 70 testów vitest (i18n, projects, contact API, hero, featured-work, navbar, contact-form, footer, SEO). (4) Accessibility: skip-to-content, role=navigation, aria-labels, focus-visible, html lang dynamic. (5) i18n: 30+ nowych kluczy (contact form, 404, a11y), localStorage persistence, html lang toggle. (6) Performance: font latin-ext subset, prefers-reduced-motion CSS. (7) Tech stack rozszerzony z 6 do 15 technologii. (8) Footer: social links (GitHub, LinkedIn), copyright 2024–YYYY. (9) About: rok założenia 2024. | `src/app/api/contact/route.ts`, `src/app/robots.ts`, `src/app/sitemap.ts`, `src/app/layout.tsx`, `src/app/page.tsx`, `src/app/projects/[slug]/page.tsx`, `src/app/projects/[slug]/ProjectDetailClient.tsx`, `src/lib/i18n.tsx`, `src/components/contact.tsx`, `src/components/footer.tsx`, `src/components/navbar.tsx`, `src/components/tech-stack.tsx`, `src/components/about.tsx`, `src/app/globals.css`, `vitest.config.ts`, `src/__tests__/**` |
| 2026-03-18 | Task 8: Zamiana 2x `<img>` na Next.js `<Image />` w ProjectDetailClient.tsx (hero + gallery screenshots) z width/height/priority. Task 9: Aktualizacja danych projektów w projects.ts — Athlix status→development, tech→Next.js+Capacitor+Clerk+Neon+TimescaleDB; Baulx subtitle/description/longDescription/features zaktualizowane do modelu 10 produktów (JetBrains) | `src/app/projects/[slug]/ProjectDetailClient.tsx`, `src/lib/projects.ts` |
| 2026-03-17 | Inicjalizacja pliku progress.md | — |
| 2026-07-15 | **Ujednolicenie systemu motion (agent builder, `docs/audit-visual/motion-report.md` + `FULLAUDIT-REPORT.md`).** Zakres: `lib/motion.ts`, `globals.css` (utilities motion/hover), `main-intro`, `trust-bar`, `tech-stack`, `projects-marquee`, `page-transition`, `scroll-progress`, `home/{offer-pillars,people,in-production,faq,contact-bookend}`, `ui/cta-button`. (1) **SSR bez niewidocznej treści**: `main-intro.tsx` (eyebrow) i `trust-bar.tsx` (pasek klientów + 3 statystyki) przepisane z `initial={{opacity:0,...}}`/`whileInView` na `initial={false}` (above-the-fold, jak już robiły H1/P w main-intro) — 0 surowych `opacity:0` w tych dwóch plikach. `tech-stack.tsx` i `projects-marquee.tsx` (nagłówki sekcji, poniżej foldu) przepisane z 3 osobnych `motion.*` z literalnym `[0.16,1,0.3,1]` na jeden `<Reveal>` (ma fallback 1.2 s + reduced-motion). (2) **prefers-reduced-motion**: `useReducedMotion()` dodane w `page-transition.tsx` (transition `{duration:0}` gdy reduce) i `scroll-progress.tsx` (spring dociągnięty do near-instant zamiast usunięcia paska — nadal ma odzwierciedlać realny scroll). `main-intro`/`trust-bar`/`tech-stack`/`projects-marquee` korzystają teraz z `Reveal`, który już to obsługiwał. (3) **Jeden system**: literalne `[0.16,1,0.3,1]` i dowolne `duration` (0.6/0.8 bez zaokrąglenia) zamienione na `easeEntry`/`durationMedium`/`durationSlow` z `lib/motion.ts` we wszystkich plikach z zakresu. (4) **`.card-hover`** (nowy `@utility` w `globals.css`, uczestniczy w wariantach jak `md:card-hover`): lift `translateY(-2px)`, `border-color: rgba(var(--theme-accent-rgb),0.5)`, `box-shadow` (odpowiednik `shadow-xl`), transition `300ms cubic-bezier(.16,1,.3,1)` (=`easeEntry`), reduced-motion usuwa lift całkowicie (nie tylko przyspiesza). Zastosowany w `tech-stack.tsx` (`TechCard`, zastąpił bespoke `hover:-translate-y-2.5`+glow-shadow), `projects-marquee.tsx` (`ProjectTile`, jako `md:card-hover` — zachowuje wyłączenie hovera na mobile jak poprzedni `md:hover:*`), `home/offer-pillars.tsx` (karty-linki 01–04). Zweryfikowany programowo przez `element.matches(':hover')` + `getComputedStyle` po realnym hover w przeglądarce (transform/border-color/box-shadow poprawne na hover, `none`/domyślne poza nim). (5) **`main-intro.tsx`**: oba CTA hero przepisane na `<CtaButton>` (klasy identyczne jak poprzednio — zweryfikowane string-match z `cta-button.tsx` base+variant) zamiast ręcznie kopiowanych klas; zachowany `tel:` (tracking globalny przez `analytics-tracker.tsx` łapie kliknięcia po `href`, nie po komponencie) i scroll `#kontakt`. Dodany magnetic hover (lokalny komponent `MagneticCta` w `main-intro.tsx`, nieeksportowany) na primary CTA (telefon): `useMotionValue`+`useSpring(springGentle)`, pull ograniczony do ±6px, wyłączony pod `useReducedMotion()` i gdy `matchMedia('(hover: hover) and (pointer: fine)')` fałszywe (dotyk). **Poza zakresem (nie ruszane, inni agenci równolegle na tym samym repo):** navbar, footer, cookie-banner, quick-contact/compact-lead-form, featured-work/portfolio-grid/ProjectDetailClient/frames, projects.ts — część z tych plików nadal ma surowe `opacity:0` bez Reveal/reduced-motion (np. navbar 2×, quick-contact 4×), poza moją własnością. **Weryfikacja:** `npx tsc --noEmit` czysty; `npx vitest run` 96/96 zielone; `npm run build` zielony (32 strony); serwer prod (`next start -p 3215`) — `curl localhost:3215/` `opacity:0`: **41→36 całościowo** (strona główna, baseline audytu z trybu dev vs teraz prod), ale w plikach z mojego zakresu **0 surowych wystąpień** (wszystkie 15 pozostałych na homepage z moich plików to `<Reveal>`-owe fallback-safe `translateY(24px)`: offer-pillars 5, people 3, in-production 1, faq 6; main-intro i trust-bar 0). `/stack`: 3→ tylko 1 z tech-stack (Reveal), reszta navbar (nie moje). `/strony-internetowe`: projects-marquee Reveal potwierdzony obecny i poprawny. Przeglądarka: desktop 1440/mobile 390 — wszystkie sekcje z zakresu widoczne, treść czytelna, brak zniknięć; hover na kartach potwierdzony programowo (`matches(':hover')`). Środowisko: podczas weryfikacji kilkukrotne race'y builda/portu 3215 spowodowane równoległymi agentami budującymi w tym samym repo (nie worktree) — rozwiązane restartem `next start` po każdym razie. | `src/app/globals.css`, `src/components/{main-intro,trust-bar,tech-stack,projects-marquee,page-transition,scroll-progress}.tsx`, `src/components/home/offer-pillars.tsx`, `progress.md` |

## 2026-07-15 — Redesign całej strony (finał) + runda napraw po adversarialnym review

**Co:** Pełny redesign programo.pl pod konwersję (telefon+formularz) z portfolio jako pokazem umiejętności. Orkiestracja ~30 agentów: analiza (audyt kodu/UX/trackingu, fakty z 10 repo projektów, research 30 stron konkurencji) → content (projects.ts od zera + content deck) → implementacja (homepage, portfolio+detale, oferty+nowa /strony-tracking-reklamy, formularze, nav+footer) → 3 recenzje (język PL/EN, adversarialny design, tracking) → runda napraw.

**Kluczowe zmiany:**
- Homepage: hero z dual CTA (tel + konsultacja) + CompactLeadForm, pasek zaufania z CountUp, 4 filary oferty, portfolio grid z ramkami urządzeń, featured case Jedmar, sekcja ludzie, pasek „W produkcji: 4 aplikacje", FAQ (JSON-LD), klamra z QuickContact.
- Portfolio: 10 projektów, uczciwe kategorie (produkty/dla-klientów/marketing), realne metryki, statusy wiążące (Jedmar: obie apki opublikowane; Estalo: web live, apki w zatwierdzaniu; ePortal Prawny: wkrótce; Solvio: we współpracy z PBDevs/Filip Piątek). Detale z DeviceDuo/PhoneFrame, prev/next, CTA.
- Nowa strona ofertowa /strony-tracking-reklamy (pakiet strona+GA4+Ads+SEO, case'y: Skup, Domki, W. Safe Finance); /oferta w 4 filarach; o-nas/cennik przepisane; strony SEO ujednolicone do tokenów.
- Prawdziwe zrzuty: 30+ webp w public/screenshots/v2 (produkty live + PRAWDZIWE zrzuty natywnej apki Jedmar z materiałów App Store; PoolTimer wykadrowany bez „±5ms"; Domki/Skup bez popupów).
- Preloader USUNIĘTY całkowicie (mglił hero); martwy kod usunięty (stary hero, horizontal-intro, magnetic, custom-cursor).
- Tracking: naprawiona kolejność Consent Mode v2 vs gtag.js (React 19 hoistuje async scripty — gtag.js ładowany programowo PO consent default, zweryfikowane w HTML); dedup contact_click; select_content na portfolio; scroll_depth 25/50/75/100; unikalne formId (14 formularzy, QuickContact z propem formId); Clarity reload przy wycofaniu zgody; suppressHydrationWarning na <html> (data-theme).
- Naprawy po review: header w jednym gridzie (koniec nakładania na 1280-1440, numer od 2xl, jednokolorowe logo w pillu); /projects/jedmar mobile bez poziomego scrolla (snap-scroll w kontenerze + overflow-x-clip); CountUp bez stanu „0" (start od wartości końcowej, sweep 70→100%); stopka bez zamrożonego reveala; pojedyncza klamra kontaktowa na home; QuickContact bez martwej strefy; sticky CTA tylko mobile; poprawki językowe (jedna obietnica „Odpowiadamy w 24 h", bez frazesów/anglicyzmów).

**Weryfikacja:** tsc czysty; 96/96 testów; build 32 stron; wizualnie desktop 1280/1366/1440/1680 + mobile 390 (bez poziomego scrolla, above-fold OK); tracking zweryfikowany na żywym HTML prod-builda.

**Uwaga środowiskowa:** w trakcie dysk zapełnił się do zera (ENOSPC) — wyczyszczono worktree'y/cache, zasugerowano Wojtkowi pełny offload (chip).

**Status:** wdrożone na prod (push → Vercel auto-deploy).

## 2026-07-15 — Poprawka po feedbacku: proporcjonalny iPhone + przeprojektowane karty /projekty

**Co:** (1) PhoneFrame przepisany na jednostki container-query (cqw) — dynamic island, promienie, ramka i przyciski skalują się z szerokością telefonu; koniec z wielką "pigułą" na małych rozmiarach (sztywne px przy zmiennej szerokości). (2) Karty /projekty bez fałszywego chrome'u przeglądarki ×9 — czysty kadr zrzutu na kanwie w kolorze projektu (ciemne bgColor dla dark produktów, delikatny gradient akcentu dla jasnych), pastylka z realną domeną (zamek + host), hover unosi kadr; telefon Jedmara w sizowanym wrapperze (fix konfliktu w-full vs w-[46%]).

**Weryfikacja:** tsc czysty, 98/98 testów, build 32 stron; wizualnie desktop 1440 (/projekty pełna siatka, /projects/jedmar rząd 3 telefonów) + realny mobile 375 (scrollWidth=375, karta Jedmar z poprawnym telefonem).

**Status:** wdrożone na prod.

## 2026-07-15 — PhoneFrame wg wzorca Estalo + naprawa headera na tabletach

**Co:** (1) PhoneFrame przebudowany 1:1 wg konstrukcji z estalo.pl (.es-iphone): jedna warstwa "titanium rail" (ukośny grafitowy gradient 145° + inset ringi biały/czarny), ekran bez osobnego czarnego bezela, jeden głęboki cień ambientowy + kontaktowy, bez kropki kamery; proporcje w cqw. Zrzuty apki Jedmar dociete o 110px (wypalona wyspa systemowa dublowała naszą). (2) Responsywność: desktopowy header (grid 1fr_auto_1fr) potrzebuje ~1160px — na 768 ucinał CTA, na 1024 chował logo pod pillem. Przełączony md:→xl: (desktop od 1280, tablet = mobilna nawigacja). H1 hero: dodane pośrednie stopnie skali (md:5xl → lg:3.4rem → xl:6xl → 2xl:7xl).

**Weryfikacja:** tsc, 98/98 testów, build; header sprawdzony wizualnie na 768/1024/1280, telefony na detalu Jedmara (3 szt.), /projekty 768 (2 kolumny kart OK), pooltimer 1920 premium. Cache obrazów .next/cache/images czyszczony po podmianie assetów (inaczej serwuje stare wersje!).

**Status:** wdrożone na prod.

## 2026-07-15 — Fix "powiększonych" zrzutów w telefonach

**Przyczyna:** wcześniejsze docięcie góry zrzutów apki (250px) zmieniło ich proporcje (0.508 vs ekran ramki 0.459) — object-cover skalował i przycinał boki = efekt zoomu. **Naprawa:** przywrócone ORYGINALNE zrzuty App Store (1284×2778, z paskiem systemowym i wyspą); geometria PhoneFrame przeliczona tak, by ekran = dokładnie 0.4622 (aspect 100/208.2, padding 3.5cqw) — zrzut pasuje 1:1 bez cropu; nowy prop `ownStatusBar` wyłącza rysowaną wyspę CSS, gdy zrzut ma własną (przekazany we wszystkich call sites przez matcher /-app[-\d]/; naprawiony też stary matcher /-app\d/ w featured-work). Reguła na przyszłość: zrzutów urządzeń NIE docinać — ramka ma pasować do zrzutu, nie odwrotnie.

**Weryfikacja:** tsc, 98/98, build; hero Jedmara — naturalna skala, pasek systemowy 14:11/wifi/bateria, jedna wyspa.

## 2026-08-03 — Pasek „Zaufali nam": prawdziwe logotypy klientów zamiast nazw tekstem

**Co:** Pasek zaufania na stronie głównej (i na landingach /strony-internetowe, /sklepy-internetowe — wspólny komponent) renderował wyłącznie nazwy klientów zwykłym tekstem. Teraz pokazuje realne znaki graficzne.

**Skąd assety:** z naszych repo i z oficjalnych stron klientów (to nasza tworczość) — Jedmar (wordmark z jedmar.com.pl), WKS Poznań (herb ze zdjęcia czepka z wkspoznan.pl), Domki Poznaniak (winieta z kartusza), W. Safe Finance (lockup z wsafefinance.pl). Skup Nieruchomości nie ma żadnego znaku nigdzie (własna strona nosi tylko logo Programo, favikona to generyczny domek) — zostaje złożony typograficznie, w tym samym kolorze i wadze.

**Technika:** każdy logotyp przerobiony na **płaską maskę alfa** (rampa luma Rec. 709 zachowująca antyaliasing + `trim` do własnego bounding boxa), renderowany przez CSS `mask-image` + `bg-current`. Dzięki temu jeden plik na klienta obsługuje oba motywy, kolor bierze się z tokenu (`text-on-surface/60`), a żaden firmowy kolor (złoto, granat, wypalone białe tło JPEG-a) nie bije się z leśną zielenią. Domki dodatkowo z gammą alfy 0.62 — włosowe kreski winiety inaczej znikały obok trzech cięższych znaków.

**Skalowanie:** normalizacja po **polu optycznym** (`h = sqrt(A/ratio)`), nie po wysokości bounding boxa — przy równej wysokości herb 1.13:1 przytłacza wordmark 4.45:1. Zweryfikowane renderem A/B, nie założone. Dwa ręczne odstępstwa opisane w kodzie: WKS w górę (dwa pierścienie włosowego pisma potrzebują zapasu), Domki w górę o 18%. Jeden pokrętło responsywne: `--logo-h`.

**Dostępność:** `role="img"` + `aria-label` z nazwą klienta na każdej masce; kontrast 5.8:1 (dark) / 4.5:1 (light), oba powyżej AA. Ink podniesiony z 45% na 60% — włosowe kreski logotypów potrzebują wagi, której typografia display nie potrzebuje.

**Dev-editor:** rozpoznawanie klienta czyta teraz `aria-label` z fallbackiem na `textContent` (maski to puste spany). `deleteListItem` działa na tablicy obiektów bez zmian — `removeArrayEntry` liczy zagnieżdżenia; przetestowane realnym POST-em.

**Pliki:** `src/components/trust-bar.tsx`, `src/components/dev/dev-editor.tsx`, `public/logos/{jedmar,wks-poznan,domki-poznaniak,wsafefinance}.png` (~46 kB łącznie).

**Weryfikacja:** tsc czysty, lint 0 błędów, 103/103 testy, build OK; wizualnie dark + light na 1280 oraz mobile 390 (dwa równe rzędy, bez poziomego scrolla).
