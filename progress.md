# Programo Site — Progress Log

**Projekt:** Strona portfolio firmy Programo — studio software Wojciecha Płonki i Bartosza Kolaja, Poznań. PL/EN.

---

## Uwaga: 100% AI Codebase

> Ten projekt jest w **100% pisany i utrzymywany przez agenty AI** (Claude opus-4.6 / sonnet) za pomocą Claude Code. Nie ma kodu pisanego ręcznie przez człowieka.

## Instrukcja dla agentów AI

> **OBOWIĄZEK:** Jako agent AI edytujący ten codebase — **musisz** zaktualizować ten plik po wprowadzeniu zmian. Dodaj wpis do sekcji Changelog poniżej z:
> - datą (YYYY-MM-DD)
> - krótkim opisem co zostało zmienione i dlaczego
> - dotkniętymi plikami/modułami
>
> To jest główny mechanizm śledzenia historii projektu. Git log pokazuje commity, ale ten plik daje AI kontekst semantyczny — co, dlaczego, i jakie moduły były dotknięte. Bez tego kolejny agent nie ma pełnego obrazu stanu projektu.

---

## Changelog

<!-- Dodawaj wpisy od najnowszego do najstarszego -->

| Data | Opis zmian | Dotknięte moduły |
|------|-----------|-----------------|
| 2026-06-14 | **Favicon naprawiony (główne logo zamiast obcego rombu) + analiza komponentów reklam.** Wojtek: favicon w wynikach Google ≠ nasze logo. Diagnoza: `favicon.svg` był geometrycznym rombem „Λ" (= `programo-mark.svg`), NIE brandowym znakiem; a `favicon.ico` miał tylko **32px** (Google wymaga **≥48px** → pokazywał zastępczą ikonę). **Fix:** `favicon.svg` przepisany na brandowy symbol „pr" (z `programo-symbol-gradient.svg`) na białym zaokrąglonym tle (czytelny na jasnych/ciemnych kartach, zgodny z głównym logo `programo-logo-gradient-full`). Przegenerowane rastry z nowego symbolu: `favicon.ico` (48px), `favicon-16/32/192`, `apple-touch-icon` (180); dodany `favicon-192x192.png`. `metadata.icons` w `layout.tsx` zaktualizowane (svg first, ico 48px, pełny zestaw PNG). Dodany `programo-ads-logo.png` (1200×1200 kwadrat, symbol na białym) — gotowy plik do logo firmy w Google Ads. **Analiza reklam (panel):** sitelinki = 4, wszystkie „Odpowiednia", dobre nazwy/CTR (Bezpłatna wycena 5,01% / Strony WWW 4,64% / Nasze realizacje 3,95% / Sklepy 3,33%). **BRAKI w reklamach:** nazwa firmy (DODANA „Programo"), logo firmy (kwadratowe symbole w bibliotece = „Nieodpowiednia"/odrzucone; nowy plik gotowy ale upload blokuje sandbox → robi Wojtek), obrazy (brak — Google sugeruje „+4% CTR"). ⚠️ Logo/nazwa firmy WYŚWIETLAJĄ się tylko dla zweryfikowanego reklamodawcy (do sprawdzenia: Ads → weryfikacja tożsamości). Uwaga: favicon w Google odświeża się z opóźnieniem (dni-tygodnie). `npm run build` zielony (28 tras). | `public/favicon.svg`, `public/favicon.ico`, `public/favicon-16x16.png`, `public/favicon-32x32.png`, `public/favicon-192x192.png`, `public/apple-touch-icon.png`, `public/programo-ads-logo.png` (NEW), `src/app/layout.tsx`, (panel Google Ads: nazwa firmy), `progress.md` |
| 2026-06-14 | **Enhanced Conversions for Leads — dopięte (panel + kod) + audyt wydatków kampanii.** Po stronie PANELU Google Ads (561-045-3845): zaakceptowano „Warunki dotyczące danych klienta" i włączono **„Konwersje rozszerzone dotyczące potencjalnych klientów" = Zarządzane za pomocą tagu Google** (akceptacja Wojtka: „ja wszystko akceptuję ty dopinaj"). Wcześniej tego dnia: link GA4↔Ads utworzony+aktywny (auto-tagging ON), wysłany 1 testowy lead „Test Konwersji (Claude)" przez produkcyjny formularz (odpalił generate_lead+natywną konwersję Ads, zapis do /crm+Telegram). **KOD (K3):** żeby EC faktycznie matchowało leady (nasz formularz to fetch SPA, nie natywny submit — auto-detekcja Google może nie złapać danych), `tracking.ts` rozszerzony: `trackLead` przyjmuje teraz `email?`/`phone?`; nowe helpery `hasMarketingConsent()` (czyta `programo-consent-v1`), `normEmail`/`normPhone` (E.164), `setEnhancedUserData()` → `gtag('set','user_data',{email,phone_number})` **TYLKO przy zgodzie marketingowej** (RODO; gtag hashuje SHA-256 przed wysłką), wołane przed konwersją Ads w branchu `!duplicate`. Formularze przekazują dane: `compact-lead-form` → `phone`; `quick-contact` → `email`+`phone`. **Audyt wydatków:** od startu kampanii (6 cze) wydane **345,75 zł**, 72 kliknięcia, CTR 4,08%, śr. CPC 4,80 zł, **0 zarejestrowanych konwersji** (spójne z diagnozą sprzed dzisiejszych fixów: brak key-eventu/value+currency, consent denied, formularz pod foldem). `npm run build` zielony (28 tras). ⚠️ Zostaje (latencja): `generate_lead` jako Key Event GA4 — event z testu pojawi się na liście Admina za kilka godzin, wtedy do oznaczenia; konwersja w Ads widoczna za 3–24h. Kampania WSTRZYMANA — włącza Wojtek. | `src/lib/tracking.ts`, `src/components/compact-lead-form.tsx`, `src/components/quick-contact.tsx`, (panel Google Ads), `progress.md` |
| 2026-06-14 | **Naprawa lejka konwersji „0 leadów" — tracking + landing hero + formularze (po głębokim audycie GA4/Ads + workflow research best-practices 2026).** Diagnoza z PANELI (potwierdzona na żywo): (a) GA4 kluczowe zdarzenia ustawione na szablonowe `close_convert_lead/purchase/qualify_lead` (nigdy nie odpalają) — nasz realny `generate_lead` NIE jest key eventem; (b) **brak linku Google Ads↔GA4** (0 połączeń) → atrybucja Paid Search przecieka do Direct/Unassigned; (c) Consent Mode default `denied` + mikro-ruch → undercount ~50%; (d) GA: landing `/strony-internetowe` 14 s/sesję, **tylko 29% scrolluje** — formularz był POD heroem `min-h-[88vh]`, więc niewidoczny; (e) kod `trackLead` był poprawny (formularze go wołają), problem w konfiguracji + lejku. **ZMIANY KODU:** **(1) `layout.tsx`** — dodano `gtag('set','url_passthrough',true)` + `gtag('set','ads_data_redaction',true)` po consent-default (gclid przechodzi między podstronami przy `ad_storage=denied`) oraz `allow_ad_personalization_signals` w config. **(2) `tracking.ts`** — `generate_lead` dostaje `value`(LEAD_VALUE_PLN=500, szacunkowy sygnał Smart Bidding, NIE statystyka) + `currency:'PLN'` (po zmianie GA4 IV 2026 bez value/currency event nie kwalifikuje się jako konwersja); spójna wartość w konwersji Ads; dev-log gdy konwersja Ads pominięta (duplicate/sesja — pomoc przy ręcznym teście w incognito). **(3) `compact-lead-form.tsx` przepisany** — wariant `bare` (do osadzenia w hero), **walidacja inline telefonu** (onBlur regex, kasowanie onChange), **przycisk NIE blokowany zgodą** (klik → podświetlenie checkboxa; backend `/api/contact` zostaje twardym strażnikiem RODO `z.literal(true)`), **wyraźny click-to-call „Zadzwoń teraz"** jako równorzędne CTA, trust-microcopy „Bez spamu — użyjemy numeru tylko, żeby oddzwonić", czytelne etykiety (koniec uppercase tracking-widest). **(4) Hero OBU landingów przebudowane** (`strony-internetowe`, `sklepy-internetowe`) — z `min-h-[88vh]` (formularz pod foldem) na **2 kolumny: copy + `CompactLeadForm bare` NAD foldem**; H1 result-oriented („Strona firmowa, która pracuje na Twój biznes" / „Sklep internetowy, który naprawdę sprzedaje"), zdjęcie Jedmara przeniesione do paska dowodu (zostaje LCP), kotwica „Zobacz realizacje ↓" → `#realizacje`. **(5) `/kontakt`** — `CompactLeadForm` (3 pola) na górze, nad pełnym QuickContact. **Weryfikacja:** `npm run build` zielony (28 tras, oba landingi statyczne ○); prerender potwierdza nowy H1, formularz w hero, click-to-call, microcopy, kotwice, brak `min-h-[88vh]`; layout/tracking config obecne. ⚠️ **Do zrobienia w PANELACH (Wojtek/Claude):** GA4 — oznaczyć `generate_lead` jako Key Event (po 1 teście), połączyć GA4↔Ads, Reporting Identity=Blended; Google Ads — auto-tagging ON, potwierdzić konwersję Active/Count=One. **Enhanced Conversions for Leads (K3)** wymaga akceptacji warunków Google (dane osobowe) — decyzja Wojtka. Kampania pozostaje WSTRZYMANA do zielonego E2E. **NIE dotknięto** `api/contact/route.ts` (działający serwerowy lead Telegram/CRM). | `src/app/layout.tsx`, `src/lib/tracking.ts`, `src/components/compact-lead-form.tsx`, `src/app/strony-internetowe/page.tsx`, `src/app/sklepy-internetowe/page.tsx`, `src/app/kontakt/page.tsx`, `progress.md` |
| 2026-06-08 | **CRM: usuwanie i edycja leada (prośba Wojtka: „i możliwość usunięcia i edycji leada").** Leady są w LIŚCIE Redis (JSON per element z `id`), więc usuwanie/edycja przez bezpieczny **odczyt→transformacja→przepis listy** po `id`. **(1) `leads.ts`** — `EDITABLE_LEAD_FIELDS` (name/email/phone/subject/message/projectType/budget — atrybucja i timestampy NIEzmienne); helper `rewriteLeads(transform)` (lrange całość → transform → `multi: del + rpush` zachowując kolejność newest‑first); `deleteLead(id)` (filtr po id + `hdel` meta) i `updateLead(id, patch)` (merge edytowalnych pól, zwraca zaktualizowanego leada albo null gdy brak id). Best‑effort. **(2) API `/api/leads`** — dodane `PATCH` (edycja pól, walidacja typów + capy długości: name 200/email 320/phone 50/subject 100/projectType 100/budget 100/message 2000; 404 gdy nie znaleziono) i `DELETE` (po `id`) — oba token‑zabezpieczone jak GET/POST. **(3) `LeadsDashboard.tsx`** — leady w lokalnym stanie (`items`); każda karta ma akcje **Edytuj** (formularz inline 7 pól + Zapisz/Anuluj, optymistyczny update z rollbackiem na błąd) i **Usuń** (inline potwierdzenie „Usunąć na stałe? Tak/Anuluj", usuwa z UI po sukcesie API). Status, notatka, szukajka, kopiowanie, źródło — bez zmian. `npm run build` zielony (`/crm` ƒ dynamic, TS OK). | `src/lib/leads.ts`, `src/app/api/leads/route.ts`, `src/app/crm/LeadsDashboard.tsx`, `progress.md` |
| 2026-06-08 | **CRM: status sprzedażowy + notatka pod leada (prośba Wojtka: „przycisk do zmiany statusu sprzedaży — czy zadzwoniono itp. + jakaś notatka pod leada").** Dodano trwały, edytowalny stan sprzedażowy per‑lead, trzymany OSOBNO od niezmiennej listy zgłoszeń. **(1) `leads.ts`** — typ `LeadMeta {status, note, updatedAt}` + 6 statusów pipeline `LEAD_STATUSES` (Nowy → Zadzwoniono → Brak kontaktu → Wycena wysłana → Wygrany → Przegrany; domyślny „Nowy"); hash Redis `programo:leads:meta` (klucz = `lead.id`) → `getLeadMetaMap()` (id→meta) i `updateLeadMeta(id,{status?,note?})` (merge, best‑effort, `updatedAt` request‑time). Osobny hash = aktualizacja jednego leada to pojedynczy HSET, bez przepisywania całej listy. **(2) `POST /api/leads`** — token‑zabezpieczony (ten sam cookie `crm_session` co GET, `verifyToken`), waliduje `id` + status (musi być z `LEAD_STATUSES`) + note (≤2000 zn.). **(3) `crm/page.tsx`** — równolegle `getLeads()` + `getLeadMetaMap()`, scala i przekazuje `meta` do panelu. **(4) `LeadsDashboard.tsx`** — w karcie każdego leada: `StatusControl` (kolorowany select 6 statusów, kropka + hue per status, **optymistyczny zapis** z rollbackiem na błąd) + `NoteField` (textarea, **auto‑zapis przy blur**, wskaźnik „zapisywanie…/zapisano ✓/błąd", `label`↔`id`). Cała dotychczasowa funkcjonalność (szukajka, kopiowanie e‑mail/tel, „Źródło leada", wyloguj) zachowana. Trwałość przez Upstash → przeżywa odświeżenie. `npm run build` zielony (`/crm` ƒ dynamic, TS bez błędów). | `src/lib/leads.ts`, `src/app/api/leads/route.ts`, `src/app/crm/page.tsx`, `src/app/crm/LeadsDashboard.tsx`, `progress.md` |
| 2026-06-08 | **Szybki formularz NA GÓRZE obu landingów + tracking per-formularz (prośba Wojtka: „daj też na górze szybki formularz na obu pagach… i te formularze podepnij pod tracking").** Dodano drugą instancję `CompactLeadForm` zaraz pod hero (pierwszy punkt konwersji, cel hero-CTA `#szybki-kontakt`) na OBU landingach; dotychczasowy formularz środkowy zostaje jako wzmocnienie dla scrollujących (odróżniony nagłówek „Wciąż się zastanawiasz?"/„Wciąż się rozglądasz?", eyebrow „Bez zobowiązań"). **KLUCZOWE — osobne `formId` na każdy formularz** (`strony-hero`/`strony-compact`, `sklepy-hero`/`sklepy-compact`, `quick-contact`): `formId` to ZARAZEM etykieta konwersji w GA4 ORAZ prefiks id pól (`${formId}-name`/`-phone`), więc różne formId = brak zdublowanych id w DOM + rozróżnialne źródło leada w GA4. Anchor `#szybki-kontakt` przeniesiony z formularza środkowego na GÓRNY (hero-CTA → krótka ścieżka do najbliższego formularza); środkowy ma teraz id = swój formId (brak kolizji `#szybki-kontakt`). **Tracking** (już wcześniej automatyczny, teraz rozróżnialny): każdy `CompactLeadForm` woła `trackLead({form: formId})` na sukcesie → GA4 `generate_lead` z `form_location` = formId + konwersja Google Ads (guard raz/sesję). Efekt: 3 formularze/stronę, 5 unikalnych etykiet w GA4 — widać, KTÓRY formularz konwertuje. `npm run build` zielony (landingi nadal statyczne ○). | `src/app/strony-internetowe/page.tsx`, `src/app/sklepy-internetowe/page.tsx`, `progress.md` |
| 2026-06-08 | **Poprawki z adwersaryjnego przeglądu redesignu landingów (13 znalezisk: 1 medium, 7 low, 5 nit; 0 blokerów/high).** (1) **[medium a11y] Kontrast** — trust-strip wordmarków `text-on-surface/45` (poniżej WCAG AA w domyślnym JASNYM motywie, a to ruch płatny) → `text-on-surface-variant`. (2) **[honesty] „Zaufali nam"** mieszał realnych klientów z własnym, jeszcze niewdrożonym produktem Estalo → trust-strip TYLKO realni klienci (strony: WSafe Finanse · WKS Poznań · Jedmar; sklepy: Jedmar · WKS Poznań · WSafe Finanse); Estalo zostaje w sekcji case studies jako „nasz własny produkt". (3) **[consistency] Rytm sekcji** — `SECTION` obu landingów + `case-studies.tsx` z `py-20/28/32` → `py-24/32/40` (spójnie ze współdzielonymi sekcjami na tej samej stronie: MainIntro/QuickContact/ProjectsMarquee). (4) **[a11y] Usunięto osierocony H2** „Napisz — wrócimy z wyceną w 24h" dublujący własny nagłówek QuickContact (oba landingi). (5) **[CRO] Rozróżnienie CTA** — finalne CTA „Wyceń moją stronę"/„Chcę sklep" → „Opisz projekt szczegółowo" (→ #kontakt-main = pełny formularz), żeby ta sama etykieta nie prowadziła do dwóch różnych formularzy; hero-CTA dalej krótka ścieżka do compact (#szybki-kontakt). (6) **[consistency] Mono-numer** w kartach Procesu (był `text-4xl`) → kanoniczny `font-mono text-xs` jak w Usługach/MainIntro. (7) **[CRO] Dowód przy formularzu** — compact-form dostał reużytą linię „Bezpośredni kontakt z założycielami · Odpowiadamy w 24 h". ⏸ Odłożono (A/B, zadanie #10): przeniesienie compact-form bezpośrednio pod hero / druga instancja formularza. `npm run build` zielony (landingi nadal statyczne ○). | `src/app/strony-internetowe/page.tsx`, `src/app/sklepy-internetowe/page.tsx`, `src/components/case-studies.tsx`, `src/components/compact-lead-form.tsx`, `progress.md` |
| 2026-06-08 | **Redesign landingów Ads do poziomu strony głównej + case studies + 2. formularz + fixy uczciwości (prośba Wojtka: „ładniej jak główna, więcej case studies, więcej formularzy, czytaj najlepsze landingi, spójne przyciski; deployuj").** Multi-agent: workflow research (4 kąty: top landingi / premium agency / CRO / trendy 2026 → konkretny spec) + workflow przeglądu adwersaryjnego (16 potwierdzonych znalezisk). **(A) Redesign OBU landingów (`/strony-internetowe`, `/sklepy-internetowe`) na język `MainIntro`:** porzucono wąski `article max-w-5xl` na rzecz pełnoekranowego hero (`min-h-[88vh]`, lewy F-pattern) + sekcje `bg-surface py-20–32` w kontenerze `max-w-[1400px] px-6/12/24`. Hero STATYCZNE pod LCP (bez animacji wejścia H1/subhead), eyebrow z frazą reklamy, H1 `font-headline text-7xl tracking-tighter` z message-matchem („Strony internetowe, które pracują na Twój biznes" / „Sklepy internetowe, które naprawdę sprzedają"), subhead `text-2xl font-light`, trust-strip wordmarków NAD foldem (statyczny, bez linków-off-rampów). `/sklepy` hero 2-kolumnowy ze screenem Jedmar (`next/image priority` = LCP). Karty usług/proces z płaskich kafli → wzorzec `border-t border-outline-variant/30 pt-8` + mono-numer. Dodano sekcję PROBLEM→OUTCOME. Animacje `Reveal` (whileInView, ease [0.16,1,0.3,1], stagger) TYLKO poniżej folda; ZERO Three.js/GSAP (szybkość = Quality Score). **(B) Spójność przycisków:** nowy `ui/cta-button.tsx` z DOKŁADNIE kanonicznymi klasami MainIntro (pill, uppercase tracking-widest, `hover:gap-5`, `→`) — wszystkie CTA ujednolicone (primary „Wyceń moją stronę"/„Chcę sklep internetowy", secondary click-to-call), zero płaskich `py-3 font-semibold`. Nowy `ui/reveal.tsx` (wspólna animacja wejścia). **(C) Więcej case studies:** server-component `case-studies.tsx` (dane WYŁĄCZNIE z `projects.ts`, `angle` = uczciwy 1-liner) — `/strony`: WSafeFinanse·WKS·Estalo; `/sklepy`: Estalo·Rejestr Pro jako „kompetencje pod e-commerce" + osobny blok Jedmar. **(D) Więcej formularzy:** `compact-lead-form.tsx` (imię+telefon+RODO → /api/contact, kotwica `#szybki-kontakt` = cel hero-CTA) MID-page + pełny QuickContact na dole = 2 formularze/stronę. **(E) Fixy z przeglądu adwersaryjnego (16):** **guard podwójnej konwersji** w `tracking.ts` (Ads „Lead" max raz/sesję przez sessionStorage; GA4 generate_lead co submit z flagą `duplicate`); a11y compact-form (label↔input `htmlFor`, focus-visible, success `aria-live`+focus, `subject` „Wycena projektu"); **Jedmar honesty** — usunięto niezweryfikowane „1460 produktów / 190 kategorii / 32 marki" (brak w projects.ts → „pełny katalog z kategoriami i markami"), błędny chip „Next.js" → realny stack natywny (Swift/SwiftUI, Kotlin, PrestaShop API), mylący alt „sklep zbudowany" → „aplikacja mobilna sklepu", usunięto fałszywą wzmiankę „migracja Jedmar (PrestaShop+Next.js)" z FAQ. ⚠️ **Do zrobienia po stronie Wojtka:** w Google Ads ustaw akcję konwersji „Lead" na liczenie „Jedna" (2. warstwa anty-dublowania). `npm run build` zielony (oba landingi nadal statyczne ○, TS bez błędów). | `src/app/strony-internetowe/page.tsx`, `src/app/sklepy-internetowe/page.tsx`, `src/lib/tracking.ts`, `src/components/case-studies.tsx` (NEW), `src/components/compact-lead-form.tsx` (NEW), `src/components/ui/cta-button.tsx` (NEW), `src/components/ui/reveal.tsx` (NEW), `progress.md` |
| 2026-06-09 | **CRM `/crm` — pełne wdrożenie produkcyjne (env + Upstash); logowanie i panel zweryfikowane na żywo.** Działania po stronie Vercela (NIE kod, dla kontekstu kolejnych agentów): (1) **Sekrety**: `CRM_EMAIL=biuro@programo.pl` (Production) + `CRM_PASSWORD` (Sensitive, Production+Preview) — hasło wpisał Wojtek osobiście (zasada: agent nie wpisuje haseł do pól). (2) **Logowanie zweryfikowane na żywo** — `programo.pl/crm` otwiera panel (e-mail biuro@programo.pl + hasło → dashboard „Leady"). ⚠️ Uwaga: autouzupełnianie przeglądarki podpowiada `wojciech.plonka@programo.pl` — login to **biuro@programo.pl** (zgodne z `CRM_EMAIL`). (3) **Upstash for Redis** sprovizjonowany przez Vercel Marketplace (plan **Free** 500k cmd/mies., region **iad1** = co-located z funkcjami Hobby, eviction OFF — leady nie znikają; resource `upstash-kv-byzantium-river`, status Available) i podłączony do projektu → wstrzyknięte `KV_REST_API_URL` + `KV_REST_API_TOKEN` (Production+Preview), które czyta `src/lib/leads.ts` (`getRedis()` wspiera parę KV_*). (4) **Redeploy** produkcji (`vercel redeploy` → alias programo.pl) — `/crm` pokazuje teraz stan SKONFIGUROWANY („Brak leadów" zamiast „Magazyn nieskonfigurowany"), więc nowe zgłoszenia będą się zapisywać i wyświetlać. (5) Leady **nadal lecą też na Telegram** (TELEGRAM_BOT_TOKEN/CHAT_ID były ustawione od 22 dni). Próba provisioning przez `vercel integration add` (CLI) zawisła na braku promptu — zrobione przez dashboard. Bez zmian w plikach kodu. | (infra/Vercel) `progress.md` |
| 2026-06-08 | **CRM `/crm` — przeprojektowanie UI (mega ładny + prosty) + red-team auth (prośba Wojtka: „nie mogę się zalogować, mega bezpieczne, jeden login dla mnie i Bartka, mega ładny i prosty").** Zrealizowane workflow multi-agent (panel 3 projektów → sędzia + hardening → red-team). **(A) UI — zwycięski wariant „EDITORIAL MINIMAL" (39/40):** `LoginForm.tsx` przeprojektowany — centrowany, `font-headline` „Panel CRM", podkreślenia pól (border-b), **toggle pokaż/ukryj hasło**, spinner, motion fade-in, semantyczne tokeny (działa w jasnym i ciemnym). **NOWY `LeadsDashboard.tsx`** (client, props `{leads, configured}`): masthead „Leady" + licznik z polską odmianą (zgłoszenie/zgłoszenia/zgłoszeń), **wyszukiwarka** (filtr po imieniu/e-mailu/telefonie/wiadomości/typie), karty leadów (czas absolutny pl-PL + relatywny „X dni temu", e-mail mailto + **kopiuj**, telefon tel: + kopiuj, chipsy temat/typ/budżet, treść, rozwijane **„Źródło leada"** z atrybucją utm/gclid), własny przycisk **Wyloguj**, empty-state + stan „magazyn nieskonfigurowany" (podpowiedź Upstash). `page.tsx` → server: `verifyToken` → login albo `<LeadsDashboard leads configured>`. Usunięto osierocony `LogoutButton.tsx`. **`providers.tsx`** — `/crm` to **samodzielny panel**: `usePathname` strippuje całe marketingowe chrome (navbar/footer/preloader/sticky CTA/cookie banner/analytics), zostają tylko Theme+i18n. **(B) Auth — rewizja red-teamu (approved, 0 high/med po fixach):** `verifyToken` pełna walidacja kształtu payloadu (`v===2`, `iat`/`exp` dodatnie skończone, odrzucenie `iat` z przyszłości i absurdalnie odległego `exp` — bound skew 1 min), `JSON.parse` w try/catch (atak nie wywoła 500). `crm-login`: rate-limit liczy **tylko NIEUDANE próby** (udane logowania nie zżerają budżetu; `delete(ip)` na sukcesie — założyciel nie zablokuje się logując kilka razy), **cap mapy `MAX_TRACKED_IPS=10000`** z ewikcją najstarszego (anty-OOM przy spoofowanym X-Forwarded-For), opóźnienie ~300 ms także na ścieżce 429 (brak sygnału timingowego), polskie znaki w komunikatach przywrócone. Token nadal SELF-EXPIRING (base64url(payload).base64url(HMAC), klucz = `sha256("programo-crm::v2::"+CRM_PASSWORD)`), single-secret (`CRM_PASSWORD` + opcjonalny `CRM_EMAIL`), repo PUBLICZNE → zero hardcoded. `npm run build` zielony (28 tras, `/crm` ƒ dynamic, TS bez błędów). ⚠️ **Do ustawienia w Vercel** (jedyna akcja po stronie Wojtka): `CRM_EMAIL=biuro@programo.pl`, `CRM_PASSWORD=<hasło>`, + para Upstash Redis (Storage → Upstash → Connect to Project, auto-wstrzykuje zmienne). | `src/lib/crm-auth.ts`, `src/app/api/crm-login/route.ts`, `src/app/crm/LoginForm.tsx`, `src/app/crm/LeadsDashboard.tsx` (NEW), `src/app/crm/page.tsx`, `src/components/providers.tsx`, `src/app/crm/LogoutButton.tsx` (usunięty), `progress.md` |
| 2026-06-08 | **Usunięto publiczny cennik z całej strony → model „indywidualna wycena" (prośba Wojtka: „wyjebać cennik, napisać że robimy indywidualną wycenę", nacisk na profesjonalny polski).** (1) **`/cennik` przepisany** — `pricing.tsx` z 3 sztywnych progów (Starter od 6 000 zł / Business od 15 000 zł / SaaS) na sekcję „Każdy projekt wyceniamy indywidualnie": nagłówek z filozofią (brak cennika z półki) + 3 karty „od czego zależy wycena" (Zakres i złożoność / Integracje i technologia / Termin i utrzymanie, środkowa wyróżniona) + jeden pas CTA „Poznaj koszt swojego projektu" → /kontakt z obietnicą „Widełki przed startem · Bez ukrytych kosztów · Praca etapami". (2) **i18n** — wymieniony cały blok `pricing.*` (usunięte klucze starter/business/saas z kwotami; dodane `pricing.factor1-3.*`, `pricing.ctaTitle/ctaDesc/promise`); `nav.pricing` „Cennik"→„Wycena" (PL), EN „Pricing" bez zmian. (3) **Nawigacja + stopka** — etykieta „Cennik"→„Wycena"; **URL `/cennik` zachowany** (kapitał SEO, brak redirectów). (4) **`/cennik` meta** — title „Wycena — Programo" + opis o indywidualnej wycenie. (5) **Spójność — usunięte WSZYSTKIE publiczne kwoty z reszty serwisu, by nie przeczyły nowemu modelowi**: `/ile-kosztuje-aplikacji` (strona SEO) — header/tabela/FAQ/MVP/CTA bez złotówek; kolumna cen w tabeli → orientacyjny CZAS realizacji (2–3 tyg / 4–6 tyg / od 6–8 tyg), nagłówek „Co budujemy i ile to zwykle trwa", `priceRows`→`projectRows` (`price`→`timeline`), meta + `headline` JSON-LD bez „Cennik 2026"; FAQ `/strony-internetowe` i `/sklepy-internetowe` („Ile kosztuje…") przeramowane na „koszt zależy od zakresu + indywidualna wycena"; `/software-house-poznan` FAQ bez „od kilku/kilkudziesięciu tysięcy". (6) **Dane strukturalne (JSON-LD)** — usunięte bloki `offers: AggregateOffer` z `lowPrice`/`highPrice`: `software-house-poznan` (5000–200000 PLN) i `sklepy-internetowe` (6000–60000 PLN), żeby Google nie pokazywał cen w rich results. **Zachowane świadomie**: chipsy budżetu w formularzu (`quick.budget.*` „do 10 tys. zł" itd.) — to pytanie do klienta o JEGO budżet (kwalifikacja leada), nie nasz cennik. ⚠️ Trade-off: strony tracą konkretne liczby pod zapytania „ile kosztuje" — świadomy wybór zgodny z decyzją biznesową Wojtka. `npm run build` zielony (28 tras, `/cennik` + `/ile-kosztuje-aplikacji` nadal statyczne ○). | `src/components/pricing.tsx`, `src/lib/i18n.tsx`, `src/components/footer.tsx`, `src/app/cennik/page.tsx`, `src/app/ile-kosztuje-aplikacji/page.tsx`, `src/app/strony-internetowe/page.tsx`, `src/app/sklepy-internetowe/page.tsx`, `src/app/software-house-poznan/page.tsx`, `progress.md` |
| 2026-06-08 | **Hardening auth CRM — token SELF-EXPIRING z podpisem (single-secret, repo PUBLICZNE).** Słabość poprzedniego schematu: token sesji był STATYCZNY — `HMAC-SHA256("programo-crm-v1", key=CRM_PASSWORD)` w hex, ta sama wartość „na zawsze", bez wbudowanego wygaśnięcia (kradzież cookie = bezterminowy dostęp aż do rotacji hasła). Przebudowano **`src/lib/crm-auth.ts`** zachowując WSZYSTKIE publiczne sygnatury (`CRM_COOKIE`, `verifyToken`, `verifyCreds`, `setSessionCookie`, `clearSessionCookie` — konsumenci `crm/page.tsx`, `api/leads/route.ts`, `api/crm-logout` bez zmian). Nowy token = `base64url(payloadJson) + "." + base64url(HMAC-SHA256(payloadJson, key))`, gdzie payload = `{v,iat,exp}` (exp = unix ms, +7 dni), a klucz HMAC jest WYPROWADZONY z hasła: `sha256("programo-crm::v2::" + CRM_PASSWORD)` — rotacja `CRM_PASSWORD` unieważnia wszystkie sesje. `verifyToken`: (a) split bezpieczny (odrzuca brak „.", złą liczbę części, puste części, niekanoniczny base64url przez round-trip re-encode), (b) recompute HMAC + porównanie **timing-safe na równej długości digestach** (`crypto.timingSafeEqual`), (c) parse payloadu + odrzucenie gdy `exp` nie-number/nieskończone lub `Date.now() >= exp`. Brak `CRM_PASSWORD` → `verifyCreds`/issuer/`verifyToken` failują (BRAK insecure default; repo jest PUBLICZNE — zero hardcoded sekretów). Zegar czytany w runtime wewnątrz funkcji (nigdy na poziomie modułu). `sessionToken()` (export) zastąpiony wewnętrznym `issueToken()` — `setSessionCookie` samowystarczalny (zweryfikowano: brak zewnętrznych konsumentów `sessionToken`). Cookie flags bez zmian (httpOnly, secure w prod, sameSite lax, path /, maxAge 7 dni). **`src/app/api/crm-login/route.ts`**: zachowany rate-limit 5/15min/IP (x-forwarded-for first hop / x-real-ip), dodano **stałe sztuczne opóźnienie ~300 ms przed 401** (spowolnienie brute force), 429 przy limicie, 400 przy złym JSON, generyczny komunikat (nigdy nie zdradza czy zawinił e-mail czy hasło), brak logowania sekretów/tokenów. Single-secret zachowane: jedyny wymagany env = `CRM_PASSWORD` (+ opcjonalny `CRM_EMAIL`). `npx tsc --noEmit` zielony; smoke-test runtime przeszedł (valid verifies, expired/tampered-payload/tampered-sig/no-dot/3-parts/empty odrzucone, unset secret → issue „" + verify false). | `src/lib/crm-auth.ts`, `src/app/api/crm-login/route.ts`, `progress.md` |
| 2026-06-08 | **Wewnętrzny CRM `/crm` + trwały zapis leadów (Upstash Redis).** Dotąd leady z formularza szły TYLKO na e-mail (Resend) / Telegram — nigdzie nie były przechowywane, brak panelu do przeglądania. Dodano: (1) **`src/lib/leads.ts`** — `getRedis()` budujący klienta `@upstash/redis` z env wspierający OBA warianty par (`UPSTASH_REDIS_REST_URL`+`UPSTASH_REDIS_REST_TOKEN` LUB `KV_REST_API_URL`+`KV_REST_API_TOKEN`), `null` gdy brak; typ `Lead` (wszystkie pola formularza + atrybucja Ads + `id` + `ts` ISO przekazywane z zewnątrz); `storeLead()` = `lpush` na `programo:leads` + `ltrim 0..499` (ostatnie 500), **best-effort w try/catch — nigdy nie rzuca**, no-op bez Redis; `getLeads(limit=200)` = `lrange` + JSON.parse (pomija nieparsowalne), `[]` bez Redis. (2) **`src/app/api/contact/route.ts`** — po udanej walidacji `await storeLead({...})` z `result.data` + atrybucją + `crypto.randomUUID()` + timestampem requestu, w try/catch — **nie wpływa na odpowiedź ani na flow Resend/Telegram** (zachowane bez zmian). Naprawiono też pre-existing błąd TS w dev-logu (`sanitize(email)` → `safeEmail`, bo `email` jest opcjonalny). (3) **`src/lib/crm-auth.ts`** — creds z env `CRM_EMAIL`/`CRM_PASSWORD` (BRAK hardcoded sekretu); `verifyCreds()` timing-safe (e-mail case-insensitive+trim), fail gdy `CRM_PASSWORD` nieustawione; `sessionToken()` = HMAC-SHA256("programo-crm-v1", key=CRM_PASSWORD) hex; `verifyToken()` timing-safe; cookie `crm_session` httpOnly+secure(prod)+sameSite lax+path/+7 dni. (4) **API**: `POST /api/crm-login` (rate-limit 5/15min/IP wzorem contact, `verifyCreds`, set cookie / 401), `POST /api/crm-logout` (clear cookie), `GET /api/leads` (czyta cookie via `await cookies()`, `verifyToken`, 401 albo `getLeads()`). (5) **`src/app/crm/page.tsx`** — server component, `robots noindex/nofollow`, `force-dynamic`; bez sesji → centrowany login (`LoginForm.tsx` client, POST→`router.refresh()`); z sesją → lista leadów (najnowsze na górze) z datą/imieniem/mailto/tel/tematem/typem/budżetem/treścią + **wyróżnione źródło leada** (kampania/słowo kluczowe/gclid/strona wejścia), licznik + `LogoutButton.tsx`; gdy brak Redis → przyjazny komunikat zamiast crasha. Tokeny motywu semantyczne, PL, mobile-friendly, NIE dodane do nav/sitemap (internal). `npx tsc --noEmit` zielony, `npm run build` zielony (28 tras, `/crm` + 3 API jako ƒ dynamic). ⚠️ Testy komponentów/SEO failują (pre-existing drift i18n — about/featured-work/footer/hero/tech-stack/seo, nie dotyczą CRM). ⚠️ **Do ustawienia w Vercel**: `CRM_EMAIL=biuro@programo.pl`, `CRM_PASSWORD=<hasło>`, oraz para Upstash Redis (`UPSTASH_REDIS_REST_URL`+`UPSTASH_REDIS_REST_TOKEN`) lub Vercel KV (`KV_REST_API_URL`+`KV_REST_API_TOKEN`) — provisioning 1-klik w Vercel → Storage → Upstash for Redis (auto-wstrzykuje zmienne). | `src/lib/leads.ts` (NEW), `src/lib/crm-auth.ts` (NEW), `src/app/crm/page.tsx` (NEW), `src/app/crm/LoginForm.tsx` (NEW), `src/app/crm/LogoutButton.tsx` (NEW), `src/app/api/crm-login/route.ts` (NEW), `src/app/api/crm-logout/route.ts` (NEW), `src/app/api/leads/route.ts` (NEW), `src/app/api/contact/route.ts`, `package.json`, `progress.md` |
| 2026-06-08 | **Formularz QuickContact: mniej friction + czytelna zgoda RODO (feedback Wojtka: „znaczek RODO niewidoczny, trzeba wpisać 20 znaków").** (1) **E-mail LUB telefon — wystarczy jedno**: `email` zrobiony opcjonalnym; walidacja „email‖phone" klientowo (`quick-contact.tsx`, przed wysyłką) ORAZ serwerowo w `/api/contact` (`.refine`). (2) **Wiadomość opcjonalna**: usunięto `required` + `minLength={20}` z `<textarea>` i ze schematu zod — można wysłać samo imię + telefon. (3) **Checkbox RODO czytelny**: był `bg-transparent` + cienki `border-outline-variant/50` na półprzezroczystej karcie = praktycznie niewidoczny → teraz `h-5 w-5`, `border-2 border-outline`, `bg-surface`, w wyróżnionym kontenerze (`bg-surface/70` + border), tekst `text-on-surface/80`, link polityki `text-primary`. (4) Klucze i18n `quick.optional/contactHint/contactRequired`; etykiety email/wiadomość → „(opcjonalnie)" + podpowiedź „Wystarczy e-mail LUB telefon". `route.ts`: mail (Resend) + Telegram z warunkowym e-mailem/wiadomością (guard `safeEmail`/`safeMessage`). ⚠️ Uwaga niezależna: dostarczanie leadów (Resend→biuro@programo.pl / Telegram) działa TYLKO gdy w Vercel ustawione `RESEND_API_KEY` / `TELEGRAM_*` — do weryfikacji testem formularza. | `src/components/quick-contact.tsx`, `src/app/api/contact/route.ts`, `src/lib/i18n.tsx`, `progress.md` |
| 2026-06-08 | **CRO „D-lite" na landingach Ads — koniec gubienia leadów (kompozycja istniejących komponentów).** Z analizy multi-perspektywicznej + red-teamu: oba landingi Ads (`/strony-internetowe`, `/sklepy-internetowe`) kierowały oba CTA „Bezpłatna wycena" przez `<Link href="/kontakt">` (double-hop = strata leadów na przejściu). Zmiany na OBU stronach: (1) **Koniec double-hopa** — 2× CTA „Bezpłatna wycena" (hero + dolne) zmienione z `/kontakt` na kotwicę `href="#kontakt-main"` do formularza inline na tej samej stronie; linki `tel:` bez zmian. (2) **Formularz inline** — wstawiony `<QuickContact />` (sekcja `id="kontakt-main"`, walidacja, zgoda RODO, `trackLead` odpala konwersję Google Ads tylko po udanym submit z atrybucją gclid/utm + guard double-fire) z krótkim nagłówkiem „Napisz — wrócimy z wyceną w 24h". (3) **Dowód społeczny** — `<ProjectsMarquee />` (realne screenshoty z `projects.ts`) + `<TrustBar />`. **TrustBar UŻYTY** — zweryfikowano `trust.stat*`: „7" wdrożonych produktów (= 7 realnych projektów w portfolio), „100%" własny kod bez podwykonawców, „do 24 h" czas odpowiedzi (spójne z copy landinga), „PL · EN" dwujęzyczność — to fakty jakościowe/strukturalne, nie zmyślone liczby marketingowe, więc zgodne z regułą „no fabricated stats". Komponenty client wstawione bezpośrednio do server-page (już są w `<I18nProvider>` z `layout.tsx`). Hero/H1/metadata/JSON-LD BEZ ZMIAN (kapitał Quality Score). Bez Three.js. Build zielony, obie strony nadal statyczne (○). | `src/app/strony-internetowe/page.tsx`, `src/app/sklepy-internetowe/page.tsx`, `progress.md` |
| 2026-06-08 | **Dodano transkrypcję + analizę masterclassu Jono Catliffa „Claude Code Google Ads ($730K)".** Wojtek poprosił o transkrypcję filmu YT (`-EInjdpjKy0`, 1:08:17) i analizę pod kątem zastosowania w Programo. Pobrane auto-napisy (yt-dlp), oczyszczone i pogrupowane wg 26 rozdziałów do `docs/google-ads-masterclass-transkrypcja.md` (~14,3k słów). Wnioski operacyjne (nie kod): strona ma już ~80% infry z filmu (atrybucja gclid/utm w `tracking.ts`, konwersja formularza, Consent Mode v2, ~50 negatywów, landingi `/strony-internetowe` `/sklepy-internetowe` `/ile-kosztuje-aplikacji` `/software-house-poznan`). Luki vs film: (1) SKAG-i + repoint final URL (czeka na weryfikację tożsamości Google, kampania WSTRZYMANA), (2) domknięcie pętli ROAS przez programo-hub jako CRM → Enhanced/Offline Conversions upload, (3) audytorium remarketingowe (RLSA), (4) Claude Code skill `/generate-ads` + podłączenie Claude do Google Ads API, (5) social proof na landingach (case studies już są). Brak zmian w kodzie aplikacji. | `docs/google-ads-masterclass-transkrypcja.md`, `progress.md` |
| 2026-06-07 | **GA4 — podmiana ID pomiaru na właściwą właściwość Wojtka.** Strona wysyłała dane do `G-TGLPLMVV91` — właściwości GA4, której Wojtek NIE ma na swoim koncie (w jego Analytics tylko „Skup Nieruchomosci PL" 395800319), więc analityka programo.pl leciała do cudzej/martwej właściwości i nigdzie jej nie widział. Zdiagnozowane na żywo przez podgląd requestów: beacon `region1.google-analytics.com/g/collect?tid=G-TGLPLMVV91&en=page_view` realnie leciał (tag był wpięty), ale do złego ID + ze zgodą `gcs=G100` (denied) = dane modelowane. Wojtek utworzył nową właściwość „Programo" → nowe ID **`G-KT2R144BYG`**. Podmienione `GA_ID` w `layout.tsx`. Tag Google Ads `AW-18196600478` bez zmian — **leady/konwersje działają niezależnie od GA4** (osobny mechanizm). | `src/app/layout.tsx`, `progress.md` |
| 2026-06-07 | **Jedmar = aplikacje NATYWNE (Swift/SwiftUI + Kotlin), nie Capacitor — korekta Wojtka.** Wpis Jedmara błędnie opisywał apkę jako hybrydę Capacitor (React/Next.js/TS w powłoce Capacitor). Poprawione na **natywne**: `projects.ts` — „Jak zbudowane" (dwie natywne aplikacje: iOS Swift/SwiftUI + Android Kotlin), bullet „Dlaczego tak" (natywnie zamiast hybrydy Capacitor/RN — płynność, dostęp do aparatu/powiadomień/gestów), `metric` „iOS + Android · natywnie", `tech` (`Capacitor/React/Next.js/TS` → `iOS/Android/Swift/SwiftUI/Kotlin/PrestaShop API`), feature #1; PL+EN. `sklepy-internetowe` — usunięto „(Capacitor)" z sekcji realizacji Jedmar (opis + chip „Natywne iOS + Android") i z karty usługi „Aplikacje mobilne do sklepów". Capacitor zostawiony tam, gdzie realny (Estalo Capacitor 8, /stack, /software-house-poznan, /ile-kosztuje, llms.txt). | `src/lib/projects.ts`, `src/app/sklepy-internetowe/page.tsx`, `progress.md` |
| 2026-06-07 | **Landingi Ads dostępne z górnego menu + klikalnej sekcji Oferta (prośba Wojtka).** Dotąd `/strony-internetowe` i `/sklepy-internetowe` dało się otworzyć tylko z URL/reklamy lub stopki. Dodano: (1) **górne menu** — nowe pozycje „Strony WWW" → `/strony-internetowe` i „Sklepy" → `/sklepy-internetowe` (nowe klucze i18n `nav.websites`/`nav.stores`); menu 5 → 7 pozycji. (2) **Sekcja Oferta** (`offer.tsx`, na `/oferta` i stronie głównej) — karta „Strony internetowe" + NOWA karta „Sklepy internetowe" są teraz **klikalne** (overlay `<Link className="absolute inset-0">` na całą kartę + cue „Zobacz →"), prowadzą do landingów; dodany item e-commerce (klucze `offer.ecommerce.title/desc`), karty przenumerowane 01–06, CTA „07". Build do weryfikacji. | `src/lib/i18n.tsx`, `src/components/navbar.tsx`, `src/components/offer.tsx`, `progress.md` |
| 2026-06-07 | **Fix: podwójny navbar + podwójna stopka na stronach projektów.** `ProjectDetailClient` renderował WŁASNY `<Navbar/>` + `<Footer/>` (oraz zagnieżdżony `<main>` i `<I18nProvider>`), mimo że globalny `Providers` już opakowuje KAŻDĄ stronę w navbar + stopkę + `<main id="main-content">` + i18n. Efekt: **2 navbary na każdej stronie projektu** (nakładały się idealnie w tej samej pozycji `fixed` → wyglądały jak jeden, stąd niezauważone) i **2 stopki na ostatnim projekcie** (Pool System — brak „next project", więc `{!next && <Footer/>}` renderował drugą). Zagnieżdżony `I18nProvider` mógł też desynchronizować przełącznik języka na stronach projektów. Usunięto własny Navbar/Footer/main/I18nProvider — `ProjectDetailClient` korzysta teraz z globalnej powłoki `Providers` (1 navbar, 1 stopka, 1 `main`, wspólny kontekst języka). Pozostałe sekcje (hero, story, galeria telefonów, capabilities, „Next Project") bez zmian. `npm run build` (25 tras) zielony. Jedyne miejsce z duplikacją w repo (grep potwierdził). | `src/app/projects/[slug]/ProjectDetailClient.tsx`, `progress.md` |
| 2026-06-07 | **Landingi Ads spójne z całokształtem + dostępne z UI; Jedmar/WKS odświeżone.** (A) **`/strony-internetowe` + `/sklepy-internetowe` przepisane na semantyczne tokeny motywu** — z surowych `var(--theme-*)`+`opacity` na `bg-surface`/`text-on-surface`/`text-on-surface-variant`; płaskie obrysowe karty → **wypełnione `bg-surface-container-low`** z `border-outline-variant` (jak stopka/reszta strony); przyciski `bg-primary → hover:bg-primary-container` (identycznie jak CTA w navbarze); eyebrow `text-primary`. Teraz **reagują na przełącznik jasny/ciemny** i pasują do reszty. Dodany odstęp pod pływający navbar (`pt-28/md:pt-32`, wzorem `/oferta`) + **naprawiony nieprawidłowy zagnieżdżony `<main>`** (landing `<main>`→`<div>`, bo `Providers` już renderuje `<main id="main-content">`). (B) **Dostępność z UI:** oba landingi dodane do stopki („Mapa strony": Strony internetowe + Sklepy internetowe) + link „Zobacz case Jedmar →" na `/sklepy-internetowe` (wcześniej osierocone — wejście tylko z reklamy). (C) **Jedmar = case study aplikacji mobilnej** (`projects.ts`): sekcje Co zrobiliśmy/Jak zbudowane/Dlaczego/Efekt, `kind:"mobile-app"`, tech +Capacitor/iOS/Android, 8 features, `liveUrl`→`jedmar-shop.vercel.app`, metric „iOS + Android · Capacitor" (bez zmyślonych liczb), Efekt szczerze: apka gotowa, jeszcze niepublikowana w App Store/Google Play. `ProjectDetailClient`: galeria w **ramkach telefonu w stylu App Store** — warunkowo TYLKO dla `kind:"mobile-app"` (pozostałe projekty bez zmian, zweryfikowane). (D) **WKS Poznań:** 3 świeże zrzuty z live `wks-strona.vercel.app` (Playwright+system Chrome→WebP, <90 KB) nadpisane. `tsc --noEmit` + `npm run build` (25 tras) zielone. | `src/app/strony-internetowe/page.tsx`, `src/app/sklepy-internetowe/page.tsx`, `src/components/footer.tsx`, `src/lib/projects.ts`, `src/app/projects/[slug]/ProjectDetailClient.tsx`, `public/screenshots/wks-{hero,cap2,cap3}.webp`, `progress.md` |
| 2026-06-07 | **Tracking konwersji: formularz = jedyna podstawowa konwersja Ads + domknięcie `/audyt`.** (A) **Tracking (runda 2, wg syntezy §4.4):** wcześniej `trackContactClick()` odpalał TĘ SAMĄ etykietę konwersji Ads „Lead" (`AW-18196600478/tYIqCM3A_rkcEJ6t6ORD`) co formularz — kliknięcia `tel:`/`mailto:` zawyżały i zaśmiecały podstawowy sygnał leada (Smart Bidding goniłby tani klik w telefon). Teraz: kliknięcia tel/mail = **tylko event GA4 `contact_click`** (obserwacja), a **wysłanie formularza = jedyna podstawowa konwersja Ads** (`trackLead` wyłącznie na gałęzi sukcesu `/api/contact`). Dodany guard jednokrotnego odpalenia (`submittingRef`) w `quick-contact.tsx` — konwersja leci dokładnie raz na udane wysłanie (ochrona przed double-submit/re-render); `finally` zwalnia ref przy błędzie. Etykieta konwersji bez zmian; Consent Mode v2 (advanced) potwierdzony OK. (B) **`/audyt` — domknięcie:** usunięty ostatni ślad trasy — link „Darmowy audyt strony" → `https://programo.pl/audyt` w `public/llms.txt`. Reszta repo czysta (src/, sitemap.ts, seo.test.ts bez odwołań; katalogi `audyt` + `api/audit` skasowane wcześniej). `tsc --noEmit` zielony. ⚠️ **Do zrobienia w panelu Ads** (czeka na weryfikację tożsamości Google): zapis repointu Grupy 2 (`/oferta`→`/strony-internetowe`), repoint Grupy 3 (`/audyt`→`/strony-internetowe`), Enhanced Conversions for Leads ON. Negatywy rozszerzone do ~50 (z 22). Kampania nadal WSTRZYMANA. ⚠️ **Pre-existing** (nie z tej zmiany, do osobnej naprawy): `seo.test.ts` „canonical URLs" regex `[a-z]+` odrzuca myślnik w slugu `wks-poznan`; testy komponentów (drift i18n). | `src/lib/tracking.ts`, `src/components/quick-contact.tsx`, `public/llms.txt`, `progress.md` |
| 2026-06-06 | **Nowy landing `/strony-internetowe` (Grupa 2 Ads) — runda 2.** Reklamy Grupy 2 („Strony WWW") lądowały na generycznej `/oferta` (słaby Quality Score + konwersja wg recenzenta #2 — „najsłabsze ogniwo"). Utworzono dedykowany landing wzorem działającego `/sklepy-internetowe`: H1 „Strony internetowe, które pracują na Twój biznes", 6 usług (strona firmowa, landing/sprzedażowa, sklep→link do /sklepy-internetowe, redesign+migracja, wydajność+SEO/Core Web Vitals, utrzymanie), sekcja „dlaczego Programo" + chipy stacku, proces 3-krokowy, FAQ z widełkami (prosta od kilku tys., rozbudowana od kilkunastu tys. — bez zmyślonych liczb), **click-to-call** (`tel:+48509123434`), pasek realizacji. Sekcje „redesign" + „wydajność/SEO" łapią też intencję Grupy 3 („naprawa/optymalizacja"). Dodany do `sitemap.ts` (prio 0.95). ⚠️ Repoint Grupy 2 (`/oferta`→`/strony-internetowe`) i Grupy 3 (`/audyt`→`/strony-internetowe`) do zrobienia w panelu Ads. Build 29/29 ✓. | `src/app/strony-internetowe/page.tsx` (NEW), `src/app/sitemap.ts`, `progress.md` |
| 2026-06-06 | **Optymalizacja landingów pod Google Ads — runda 1 (po syntezie 3 audytów).** (A) **`/sklepy-internetowe`**: Jedmar poprawiony z „PWA mobile-first" → **„aplikacja mobilna iOS + Android (Capacitor)"** (na prośbę Wojtka — Jedmar to apka DO sklepu, nie PWA/cały sklep); dodany **click-to-call** (`tel:+48509123434` w hero i dolnym CTA — niskozaporowa konwersja, odpala tag `tel:`, skraca „hop" do `/kontakt`); pasek realizacji (Jedmar/Estalo/WKS/WSafe + „bezpośredni kontakt z założycielami"); usługa „Aplikacje i PWA do sklepów" → „Aplikacje mobilne do sklepów". (B) **Usunięto całą funkcję `/audyt`** (Wojtek nie chce tej podstrony): skasowane `src/app/audyt/` + `src/app/api/audit/` (route + lead), usunięte odwołania w `footer.tsx`, `sitemap.ts`, `ile-kosztuje-aplikacji`, `seo.test.ts` (próg ≥10). ⚠️ Grupa reklam „Naprawa/optymalizacja stron" wskazuje teraz na nieistniejący `/audyt` → repoint przy strojeniu Ads (kampania WSTRZYMANA, brak ruchu). Synteza/plan: `docs/google-ads-audyt-synteza-2026-06.md`. Build zielony. | `src/app/sklepy-internetowe/page.tsx`, `src/components/footer.tsx`, `src/app/sitemap.ts`, `src/app/ile-kosztuje-aplikacji/page.tsx`, `src/__tests__/seo.test.ts`, usunięte `src/app/audyt/` + `src/app/api/audit/`, `progress.md` |
| 2026-06-06 | **Estalo — status „wkrótce" + świeże premium screeny.** Wojtek: pokazać Estalo jako „wkrótce", nie „już ruszamy". (1) `status` `live`→`planned` → karty automatycznie pokazują badge „Wkrótce/Coming soon". (2) `metric` → „Solo · Enterprise · Portal — wkrótce / soon". (3) `longDescription` PL/EN: „Dostępny/Two plans" → „Premiera wkrótce — / Launching soon —"; z `description.en` usunięto twarde liczby (791 testów itd.) dla framingu „wkrótce". (4) `ProjectDetailClient`: etykieta linku `liveUrl` warunkowa — „Live Site" tylko dla `status==='live'`, inaczej „Podgląd". (5) **3 świeże zrzuty WebP** zastępujące stare (sprzed premium-redesignu landingu): hero (badge „● Wkrótce" + luksusowy telefon), sekcja głosowej notatki (landscape), telefon z dopasowaniami AI + foto wnętrza z panoramą Warszawy (portret 4:5) — puppeteer headless z `prefers-reduced-motion` (stany końcowe animacji) z landingu, konwersja sharp→WebP. Zweryfikowane (next start + puppeteer): galeria, opis „Premiera wkrótce", listing „— WKRÓTCE". Test projects 12/12, build 27/27 ✓. | `src/lib/projects.ts`, `src/app/projects/[slug]/ProjectDetailClient.tsx`, `public/screenshots/estalo-hero.webp`, `public/screenshots/estalo-otodom.webp`, `public/screenshots/estalo-portal.webp`, `progress.md` |
| 2026-06-06 | **Pool System — totalna przebudowa wpisu projektu + screeny.** Wpis projektu basenowego (slug `pool-system`) miał czarny hero, dwa generyczne akapity i 0 zrzutów. Przepisany całościowo: subtitle „Od ściany basenu po klubowe command center", pełny `longDescription` (Co robi / Na czym jest zbudowany / Dlaczego tak) z realną architekturą (touchpady ESP32-C3 + box ESP32-S3, ESP-NOW, BLE Polar HR, lokalny AP + WebSocket, iOS SwiftUI, web coach/athlete Next.js+Supabase, command center, eksport LENEX/CSV), status `planned`→`development`, `liveUrl` swimplatform.vercel.app, realny `tech` stack, 8 capability z tytułami (format „Tytuł — opis"), `metric` „±5 ms · 1–8 torów". Dodane 3 zrzuty WebP do `public/screenshots/`: hero (blurred ambient z landingu), command-center dashboard (landscape 1600px), realna apka iOS zawodnika złożona na brandowym tle 4:5 — wygenerowane headless Chrome (landing) + symulator iOS, przetworzone Pillow + cwebp. Zweryfikowane wizualnie (dev): cinematic hero z tytułem POOL SYSTEM, asymetryczna siatka screenów (dashboard + apka iOS) i sekcja Capabilities renderują się poprawnie. Testy projects 12/12, build produkcyjny 27/27 stron OK. | `src/lib/projects.ts`, `public/screenshots/pool-system-hero.webp`, `public/screenshots/pool-system-cap2.webp`, `public/screenshots/pool-system-cap3.webp` |
| 2026-06-06 | **Śledzenie konwersji Google Ads (formularz + kliknięcia kontaktowe):** Pod kampanię Search „Programo – Search – E-commerce (PL)" (konto 561-045-3845) podpięto konwersję Google Ads `AW-18196600478`, działanie „Przesłanie formularza kontaktowego". Wykorzystano istniejący gtag.js (GA4) — nie dublujemy ładowania tagu. (1) `layout.tsx`: dodatkowy `gtag('config','AW-18196600478')` obok GA4; tag Ads gated przez Consent Mode v2 (zgoda marketingowa — zgodne z RODO/banerem zgód). (2) `tracking.ts`: helper `trackAdsConversion()` (`send_to: AW-18196600478/tYIqCM3A_rkcEJ6t6ORD`, value 1.0 PLN) wołany w `trackLead()` (sukces formularza kontaktowego) ORAZ `trackContactClick()` (kliknięcia `tel:`/`mailto:` łapane globalnie przez `analytics-tracker`). Efekt: każdy lead — formularz, telefon, email — liczony jako konwersja, co odblokowuje optymalizację kampanii pod konwersje. `tsc --noEmit` + `npm run build` zielone. | `src/app/layout.tsx`, `src/lib/tracking.ts`, `progress.md` |
| 2026-06-03 | **Przebudowa portfolio: −Athlix, Jedmar=apka mobilna, Solvio=coming soon, +Pool System:** Na prośbę Wojtka (korekta tego, co realnie robiliśmy). (1) **Usunięto Athlix** z `projects.ts` + wszystkich referencji: `projects-marquee` MARQUEE_SLUGS, `trust-bar` WORDMARK_SLUGS, `software-house-poznan` (lista projektów + FAQ → podmienione na Pool System), obrazy `athlix-*.webp` skasowane. (2) **Jedmar** przepisany ze „sklep e-commerce" na **aplikację mobilną iOS+Android DO sklepu** (to było zlecenie — nie budowaliśmy całego sklepu): nowy subtitle/description/longDescription/features/tech (Capacitor/iOS/Android)/tags (Mobile/iOS/Android)/metric; **mobilne** zrzuty `jedmar-app1/2/3.webp` (Playwright mobile 412×900, popupy Cookiebot/newsletter odrzucone) zamiast desktopowych `jedmar-clean*`. (3) **Solvio** status → `planned` (coming soon — to też tylko apka mobilna). (4) **NOWY Pool System** (`pool-system`, `planned`, `nasze-systemy`): innowacyjny system timingowy (pomiar czasu) — hardware + aplikacja mobilna, faza demo; opis bez zmyślonych szczegółów technicznych (zasada „no fabricated stats"); brak screenshotów → kafel `bgColor` + badge. (5) **Badge „Wkrótce/Coming soon"** dodany do `featured-work` (ProjectCard) i `projects-marquee` (ProjectTile) dla `status !== "live"` (i18n `work.comingSoon` istniał, nieużywany). `projects.test.ts`: EXPECTED_SLUGS −athlix +pool-system, REMOVED_SLUGS +athlix. Build zielony (7 projektów). `jedmar-hero.webp` ZOSTAJE (używany przez `/sklepy-internetowe`). | `src/lib/projects.ts`, `src/components/projects-marquee.tsx`, `src/components/featured-work.tsx`, `src/components/trust-bar.tsx`, `src/app/software-house-poznan/page.tsx`, `src/__tests__/projects.test.ts`, NEW `public/screenshots/jedmar-app1/2/3.webp`, usunięte `athlix-*.webp` + `jedmar-clean1/2.webp`, `progress.md` |
| 2026-06-03 | **Fix animacji na Windows (scroll-linked parallax) — iOS/Mac NIETKNIĘTE:** Zgłoszenie: na iOS gładko, na Windowsie animacje „fatalnie". Diagnoza: scroll-linked parallax + rozmyte skalowane hero w `ProjectDetailClient` (strony /projects/[slug]) są masłowe na Apple (momentum scroll), a na Windowsie/desktopie (kółko myszy = duże skoki delty) sprężyny/transformy goniące scroll szarpią i overshootują. **Uwaga dla kolejnych agentów:** wcześniejsze podejrzane — `custom-cursor.tsx`, `ui/lenis-provider.tsx`, `horizontal-intro.tsx`, `hero.tsx` — to **martwy legacy po redesignie, nigdzie nie montowany** (home = `main-intro` + `trust-bar` + `projects-marquee` + `quick-contact`). Fix: nowy hook `useSmoothInput()` (true tylko na Apple/touch lub bez reduced-motion); w `ProjectDetailClient` `ParallaxImage` oraz scroll-scale/parallax hero renderują się **statycznie na Windowsie** (czysty `object-cover` zamiast szarpania), a na iOS/Mac jak dotąd. Zero zmian wizualnych na Apple. Marquee (rAF translate, GPU) zostawiony — najmniej jankowy typ; do ewentualnej tury 2 jeśli user wskaże. Build zielony. | `src/lib/use-smooth-input.ts` (NEW), `src/app/projects/[slug]/ProjectDetailClient.tsx`, `progress.md` |
| 2026-06-03 | **Portfolio: bogate galerie zdjęć (WebP) dla 5 realizacji — BEZ zmian wyglądu:** Strona projektu (`ProjectDetailClient`) renderuje galerię (kafel 16:10 + 4:5) tylko gdy `screenshots.length > 1`; `solvio` i `rejestr-pro` nie miały żadnych zdjęć (płaski kafel w marquee / litera w bento), a `jedmar/wks-poznan/wsafefinanse` miały tylko hero (brak galerii). Złapano headless (Playwright + systemowy Chrome `--channel=chrome`, 1600×1000, deviceScaleFactor 1.5, wait na render framer-motion) po kilka sekcji z każdego live'a, oceniono montage-arkuszami (ImageMagick), wybrano 2 najładniejsze sekcje/projekt i **skonwertowano do WebP** (konwencja maina): solvio (hero+feature cards), rejestr-pro (hero+features+CTA), wks (trenerzy+dyscypliny), wsafe (sekcje usług premium), jedmar **re-capturowany z odrzuceniem popupów Cookiebot/newsletter/czat** → czysty sklep (kategorie+promo, siatka produktów+marki). Każda z 5 realizacji ma teraz `screenshots: [hero, sekcja1, sekcja2]` w `.webp` → pełne galerie na /projects/[slug] + realne kafle w marquee/bento. Zachowane pole `metric`, WebP i redesign maina (nałożone na aktualny prod, nie cofnięte). Build zielony (7 stron projektów). Opisy przejrzane — bez zmian (już wysokiej jakości). | `src/lib/projects.ts`, NEW `public/screenshots/{solvio-hero,solvio-cap2,solvio-cap3,rejestr-pro-hero,rejestr-pro-cap2,rejestr-pro-cap4,jedmar-clean1,jedmar-clean2,wks-cap2,wks-cap3,wsafefinanse-cap2,wsafefinanse-cap3}.webp`, `progress.md` |
| 2026-05-29 | **NOWY landing e-commerce `/sklepy-internetowe` (feat/ecommerce-landing):** Dedykowana strona pod kampanię Google Ads skupioną w 100% na e-commerce (styl Jedmar). Sekcje: hero + CTA (obraz tymczasowo `/screenshots/jedmar-hero.png` — do podmiany na obraz generowany przez Wojtka), usługi (sklepy WooCommerce/Shopify/PrestaShop/headless, migracje, integracje Allegro/BaseLinker/płatności/KSeF, aplikacje/PWA do sklepów, headless/wydajność, utrzymanie), realizacja Jedmar (1460 prod., PrestaShop API, PWA), proces, FAQ, CTA. Schema: BreadcrumbList + Service (AggregateOffer 6000–60000 PLN) + FAQPage. Wpięte w `sitemap.ts` (priority 0.95) i `llms.txt`. URL docelowy nowej kampanii Search „E-commerce". | `src/app/sklepy-internetowe/page.tsx` (NEW), `src/app/sitemap.ts`, `public/llms.txt`, `progress.md` |
| 2026-05-29 | **Portfolio: −baulx/−learnai, +Solvio, +Rejestr Pro (Posnania już wcześniej):** Wycięto baulx i learnai z `projects.ts`. Dodano **Solvio** (SaaS AI do wydatków — OCR paragonów, porównywarka cen, raporty; solvio-lac.vercel.app) i **Rejestr Pro** (wyszukiwarka firm z KRS — oficjalne API MS, profile spółek, powiązania osób, link do RDF; rejestr-pro.vercel.app) jako „nasze-systemy", live. Bez screenshotów → kafelek marquee renderuje czysty `bgColor` (komponent to wspiera, design Bartka nietknięty). Spójność: `projects-marquee.tsx` MARQUEE_SLUGS (estalo/solvio/athlix/rejestr-pro/jedmar/wks/wsafe), `software-house-poznan` (lista + FAQ + przykłady — inaczej linki do /projects/baulx = 404), `public/llms.txt`, przepisany `projects.test.ts` (był czerwony przez length(4) — teraz zielony i durable). 7 projektów. | `src/lib/projects.ts`, `src/components/projects-marquee.tsx`, `src/app/software-house-poznan/page.tsx`, `public/llms.txt`, `src/__tests__/projects.test.ts`, `progress.md` |
| 2026-05-29 | **Usunięto KS Posnania z portfolio (nie jest klientem Programo):** Na prośbę Wojtka wycięty obiekt `ks-posnania` z `src/lib/projects.ts` (był błędnie wpisany jako realizacja). Sitemap (generowany z tablicy `projects`), `/projekty` i marquee aktualizują się automatycznie. Build zielony (7 projektów). Screenshot `/screenshots/ks-posnania-hero.png` pozostawiony jako nieużywany (opcjonalnie do sprzątnięcia). | `src/lib/projects.ts`, `progress.md` |
| 2026-05-29 | **Optymalizacja zależności + perf (feat/seo-formalities cd.) — BEZ zmian wyglądu:** Usunięto martwy `src/components/ui/hero-background-3d.tsx` (nieimportowany nigdzie, jak wcześniej `contact.tsx`) oraz odinstalowano nieużywane ciężkie zależności: `three`, `@react-three/fiber`, `@react-three/drei`, `gsap` (gsap nieużywany w ogóle; reszta tylko w martwym pliku) → mniejszy `node_modules`, szybszy install, mniejsza powierzchnia supply-chain; shipowany bundle bez zmian (nie były importowane). Dodano `preconnect`/`dns-prefetch` do `googletagmanager.com` i `clarity.ms` (szybszy handshake z analityką, lepsze CWV). `lenis` + `framer-motion` zostają (realnie używane). | `package.json`, `package-lock.json`, `src/app/layout.tsx`, usunięte `src/components/ui/hero-background-3d.tsx`, `progress.md` |
| 2026-05-29 | **SEO/a11y/perf formalności (feat/seo-formalities) — BEZ zmian wyglądu:** (1) Stopka: dodana nawigacja „mapa strony" linkująca kluczowe podstrony, w tym wcześniej osierocone `/software-house-poznan`, `/ile-kosztuje-aplikacji`, `/audyt` (lepszy crawl, link equity, UX) — wkomponowana w istniejący układ stopki. (2) `layout.tsx`: schemat `Organization` → `ProfessionalService` (+logo, email, telefon, adres+region, `areaServed` Poznań/Wielkopolska/Polska, `sameAs` GitHub+LinkedIn, role founderów, alternateName, priceRange, knowsLanguage) pod rich results/GEO; `foundingDate` celowo pominięty (brak pewnego źródła — zasada „no fabricated stats"). (3) `next.config.ts`: nagłówki bezpieczeństwa (X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, HSTS) + `images.formats` AVIF/WebP + `minimumCacheTTL` + `poweredByHeader:false` (świadomie BEZ CSP — ryzyko zerwania inline gtag/Clarity). (4) `providers.tsx`: link „Przejdź do treści" (skip-to-content, WCAG, sr-only — niewidoczny do czasu fokusu klawiaturą). (5) `ProjectDetailClient`: opisowe `alt` zamiast „Detail 1/2". Wygląd niezmieniony. | `next.config.ts`, `src/app/layout.tsx`, `src/components/providers.tsx`, `src/components/footer.tsx`, `src/app/projects/[slug]/ProjectDetailClient.tsx`, `progress.md` |
| 2026-05-29 | **Tracking Faza 1 — konwersje GA4 + atrybucja gclid/UTM (feat/tracking-phase1):** NOWE `src/lib/tracking.ts` — przechwyt `gclid/gbraid/wbraid` + `utm_*`, trwałe w localStorage („ostatni klik kampanii wygrywa"), helpery `trackLead`/`trackContactClick` zgodne z Consent Mode v2 (eventy lecą bezwarunkowo, CM v2 modeluje gdy brak zgody). NOWE `src/components/analytics-tracker.tsx` montowane w `providers.tsx`: przechwyt atrybucji na mount + delegowane śledzenie klików `tel:`/`mailto:` w całym serwisie. `quick-contact.tsx` i `audyt/AuditClient.tsx` odpalają GA4 `generate_lead` na sukcesie i doklejają atrybucję do payloadu. `/api/contact` + `/api/audit/lead` przyjmują pola atrybucji (Zod) i wstrzykują źródło leada (źródło/medium/kampania/słowo/gclid/strona wejścia) do maila i Telegrama. Usunięto martwą atrapę `contact.tsx` + jej nieaktualny test `contact-form.test.tsx` (prawdziwy formularz to QuickContact). | `src/lib/tracking.ts` (NEW), `src/components/analytics-tracker.tsx` (NEW), `src/components/providers.tsx`, `src/components/quick-contact.tsx`, `src/app/audyt/AuditClient.tsx`, `src/app/api/contact/route.ts`, `src/app/api/audit/lead/route.ts`, usunięte `src/components/contact.tsx` + `src/__tests__/components/contact-form.test.tsx`, `progress.md` |
| 2026-05-29 | **/audyt + /software-house-poznan + OG image + llms.txt + /ile-kosztuje-aplikacji (gałąź feat/audyt-seo-og, baza origin/main):** Pogodzono rozjechany lokalny eksperyment — przeprojektowany one-pager odłożony na gałąź `experiment/conversion-landing`, jako kanon zostaje LIVE (origin). Przeniesiono na bazę live: lead magnet `/audyt` (page + AuditClient + `/api/audit` scoring PageSpeed/heurystyka + `/api/audit/lead` z RODO-zgodą → Telegram + Resend) oraz landing SEO `/software-house-poznan`. NOWE `src/app/opengraph-image.tsx` — brandowany OG 1200×630 przez `next/og` (naprawia brak og:image na KAŻDYM udostępnieniu, m.in. LinkedIn/FB). NOWE `public/llms.txt` (widoczność w LLM/GEO). NOWA strona `/ile-kosztuje-aplikacji` (answer-first, schema FAQPage+Breadcrumb, widełki spójne z /cennik). `sitemap.ts` +3 URL (software-house-poznan 0.95, ile-kosztuje 0.9, audyt 0.9) ORAZ generowanie wpisów projektów z tablicy `projects` — wszystkie 8 case studies (w tym klienci Jedmar/WKS/Posnania/WSafe) trafiają teraz do sitemap.xml (wcześniej tylko 4). `seo.test.ts` — naprawiony już-zepsuty `toHaveLength(5)` (sitemap miał 11) → asercje obecności URL + brak duplikatów. `layout.tsx` — dodany `@id` do schematu Organization (żeby `provider` w schematach podstron się rozwiązywał). AuditClient `--theme-text-2-rgb`→`--theme-text-1-rgb` (niezdefiniowany na origin). | `src/app/audyt/*`, `src/app/api/audit/**`, `src/app/software-house-poznan/page.tsx`, `src/app/opengraph-image.tsx` (NEW), `public/llms.txt` (NEW), `src/app/ile-kosztuje-aplikacji/page.tsx` (NEW), `src/app/sitemap.ts`, `src/__tests__/seo.test.ts`, `src/app/layout.tsx`, `progress.md` |
| 2026-04-11 | **Light mode toggle (feat/concept-a-fusion):** Added full light mode theme with inverted colors. Dark mode (default) looks EXACTLY the same — zero visual changes. (1) `globals.css` — Added `:root` and `[data-theme="light"]` CSS variable sets: `--theme-bg-1..4`, `--theme-border-1..2`, `--theme-text-1..2`, `--theme-accent`, RGB variants for opacity-based usage (`--theme-*-rgb`), `--theme-alt-*` for cream/dark-forest panel inversion, and `--theme-nav-*` for navbar pill. `@theme inline` now references all theme vars. Body + logoGlow + shimmerSweep keyframes use RGB vars. (2) `src/lib/theme.tsx` — New `ThemeProvider` context with `useTheme()` hook. Persists to `localStorage["programo-theme"]`, toggles `data-theme` attr on `<html>`. (3) `layout.tsx` — Inline `<script>` runs before hydration to set `data-theme` from localStorage (prevents flash on first load). (4) `page.tsx` — Wraps `<I18nProvider>` in `<ThemeProvider>`. (5) `navbar.tsx` — Added sun/moon SVG theme toggle button on both desktop (next to lang toggle) and mobile pill. All `#051F20` text → `var(--theme-nav-text)`, all pill bg/borders → `var(--theme-nav-pill*)`, opacity variants use rgba with `--theme-nav-text-rgb`. (6) `hero.tsx` — All `#051F20`/`#0A2A28`/`#163832`/`#DAF1DE`/`#8EB69B` → theme vars. `whileHover={{ backgroundColor: "#0A2A28" }}` → `var(--theme-bg-2)`. Shadow glows use theme vars. (7) `horizontal-intro.tsx`, `featured-work.tsx`, `tech-stack.tsx`, `contact.tsx`, `about.tsx` — All theme hex codes replaced with CSS var references. `about.tsx` cream panel uses `--theme-alt-*` vars (inverted: dark forest in light mode). Brand colors (React/TypeScript/etc.) and project accent colors preserved — never replaced. Build: 0 errors, 14 pages. | `src/app/globals.css`, `src/lib/theme.tsx` (new), `src/app/layout.tsx`, `src/app/page.tsx`, `src/components/navbar.tsx`, `src/components/hero.tsx`, `src/components/horizontal-intro.tsx`, `src/components/featured-work.tsx`, `src/components/about.tsx`, `src/components/tech-stack.tsx`, `src/components/contact.tsx`, `progress.md` |
| 2026-04-11 | **Scroll progress, project previews, about whileInView reveals, WSafeFinanse rename (feat/concept-a-fusion):** (1) NEW `src/components/scroll-progress.tsx` — Fixed vertical progress bar on the right (40vh track, gradient fill, spring-smoothed `scaleY` from `useScroll`) plus top horizontal bar on mobile. `z-[60]`, `pointer-events-none`. Mounted in `page.tsx` HomeContent OUTSIDE of `<PageTransition>` so it stays visible during transitions. (2) `projects.ts` — Renamed `safe-finanse` -> `wsafefinanse`, title `Safe Finanse` -> `WSafeFinanse` (liveUrl `wsafefinance.pl` and accentColor `#2563eb` unchanged). Added `screenshots` to all 4 new projects: jedmar-hero.png, wks-hero.png, ks-posnania-hero.png, wsafefinanse-hero.png. Added `liveUrl` to WKS Poznan (`wks-strona.vercel.app`) and KS Posnania (`ks-posnania.vercel.app`). (3) `featured-work.tsx` — Added always-visible "VISIT ↗" pill button in top-right of ProjectCell for `category === "strony-zrobione"` with `liveUrl`. Uses `e.stopPropagation()` + `pointer-events-auto` to escape the parent `<Link>` wrapper and open live site in a new tab. Border uses `accentColor` at 40% alpha. (4) `about.tsx` — Replaced all `useScroll`+`useTransform` scroll-progress animations with `whileInView` + `viewport={{ once: true, margin: ... }}` so animations trigger when entering view (fix for anchor jump to `#about` showing final state already). `RevealText` words stagger via `delay: i * 0.12`, `FadeInText` simple y+opacity, quote section: ghost FUTURE scale-in, quote fade+y, borders scaleX from center. Removed `useRef`, `useScroll`, `useTransform` imports (only `motion` left). Build: 0 errors, 14 static pages. | `src/components/scroll-progress.tsx` (NEW), `src/app/page.tsx`, `src/lib/projects.ts`, `src/components/featured-work.tsx`, `src/components/about.tsx`, `public/screenshots/*-hero.png`, `progress.md` |
| 2026-04-11 | **Pre-production polish (feat/concept-a-fusion):** (1) `about.tsx` — Split into half-dark/half-cream layout. TOP (dark `bg-[#0A2A28]`): RevealText + heading + paragraphs with light text (`text-on-surface`, `text-primary`). BOTTOM (cream `bg-[#FAF8F4]` with `rounded-t-[32px] 2xl:rounded-t-[48px]`): stats + animated quote + founders with dark text (`text-[#1A1816]`, `text-[#6B6560]`, `text-[#8EB69B]`). Added scroll-driven animated quote section with `useScroll`/`useTransform` hooks: FUTURE ghost text scales 0.5->1 with opacity 0->0.05, quote fades in with y:60->0, top/bottom borders scale from center. Mobile spacing reduced (mt-20 instead of mt-32). (2) `projects.ts` — Replaced placeholder projects "sklep-x" and "korpo-y" with 4 real projects: Jedmar (e-commerce, #ffd333), WKS Poznan (sports club, #c41e3a), KS Posnania (sports club, #004e75), Safe Finanse (financial advisory, #2563eb). All in category "strony-zrobione". First 4 projects (Estalo/Baulx/Athlix/LearnAI) unchanged. (3) Responsive polish: `contact.tsx` send button h-28/w-28 on mobile (was h-40/w-40), stat values capped at 2xl:text-7xl (was 5vw which got too large at 2560px). Build: 0 errors, 14 static pages generated (8 project slugs). | `src/components/about.tsx`, `src/lib/projects.ts`, `src/components/contact.tsx`, `progress.md` |
| 2026-04-11 | **Navbar contrast fix + About section cream background (feat/concept-a-fusion):** (1) `navbar.tsx` — Fixed unreadable text on white pill nav. Changed all text colors from theme tokens (`text-on-surface`, `text-on-surface-variant`, `text-primary`) to explicit dark `text-[#051F20]` with appropriate opacities. Desktop: logo, nav links (active/inactive/hover), language toggle all use `#051F20`. Mobile pill: hamburger lines `bg-[#051F20]`, logo and language toggle `text-[#051F20]`. Removed `MagneticWrapper` from CTA button (replaced with plain `<a>`), removed unused import. Mobile overlay menu kept as-is (dark bg + light text). (2) `about.tsx` — Changed section background from `bg-[#0A2A28]` (dark green) to `bg-[#FAF8F4]` (warm cream). Added `rounded-b-[32px] 2xl:rounded-b-[64px]` for smooth transition to dark section below. All text colors updated for dark-on-light: headings `text-[#1A1816]`, paragraphs `text-[#1A1816]/80`, labels `text-[#6B6560]`, accents `text-[#8EB69B]`. Stats, borders, founder cards, Premium Agency circle, decorative elements all updated. RevealText made more dramatic: scroll offset `["start 0.9", "end 0.3"]`, y translation `[80, 0]`, added scale `[0.9, 1]`, opacity starts at `0.02`. FadeInText more dramatic: offset `["start 0.95", "start 0.5"]`, opacity starts at `0.0`, added y transform `[30, 0]`. Build: 0 errors. | `src/components/navbar.tsx`, `src/components/about.tsx` |
| 2026-04-11 | **Fix mobile crash + animations + accent colors + lighter sections (feat/concept-a-fusion):** (1) `hero.tsx` — CRITICAL: fixed hydration mismatch crash on mobile. Replaced all `{!isMobile && <Component />}` conditional renders with `<div className="hidden md:contents">` CSS approach. Components always render in DOM but are hidden on mobile via CSS. Removed `isMobile` state + useEffect entirely. Replaced `style={{ height: isMobile ? ... }}` with CSS `h-[150vh] md:h-[200vh]`. Added staggered `animate-fadeSlideIn` entrance animation on each bento cell (delays 0.1s-0.9s). Added `animate-logo-glow` pulsing shadow on "pr." text. Added `animate-shimmer-sweep` diagonal gradient on "digital craftsmanship" cell. Replaced framer-motion scanner lines with direct DOM manipulation via refs for perf. (2) `horizontal-intro.tsx` — Removed `isMobile` state + useEffect. Replaced inline height style with CSS `h-[200vh] md:h-[250vh]`. Added individual letter animations on "programo" text (`animate-letterReveal` with staggered delays). Added vertical parallax on manifesto block via `useTransform`. Made ticker more energetic with 8 copies + `animate-tickerPulse` glow effect. (3) `featured-work.tsx` — Removed `isMobile` state + useEffect + prop. Each project card now uses `project.accentColor` for: scanning line color, status dot glow, bottom overlay left border, and tag backgrounds on hover. Removed `grayscale group-hover:grayscale-0` — images now show in color with dark overlay that lifts on hover. (4) `tech-stack.tsx` — Section background changed to `bg-gradient-to-b from-[#051F20] via-[#0B2B26] to-[#051F20]`. TechCard background: `bg-[#0B2B26]`, border: `border-[#235347]/30`. Added `techColors` map (15 technologies with unique colors). Icon circle uses per-tech color. Added dynamic top accent line on hover in tech color. Added radial gradient glow on hover. Heading now has gradient text (`bg-gradient-to-r from-[#DAF1DE] via-[#8EB69B] to-[#DAF1DE]`). Tech-card-line pseudo-element uses `--tech-color` CSS var. (5) `about.tsx` — Background changed from `bg-surface-container` to `bg-[#0A2A28]` for lighter contrast. Removed special characters (Polish diacritics) that could cause encoding issues. (6) `contact.tsx` — Added top accent gradient line `via-[#8EB69B]` at section top. (7) `globals.css` — 6 new keyframe animations: `fadeSlideIn`, `logoGlow`, `shimmerSweep`, `letterReveal`, `tickerPulse`. Tech-card-line pseudo uses `--tech-color` CSS variable. Build: 0 errors. | `src/components/hero.tsx`, `src/components/horizontal-intro.tsx`, `src/components/featured-work.tsx`, `src/components/tech-stack.tsx`, `src/components/about.tsx`, `src/components/contact.tsx`, `src/app/globals.css`, `progress.md` |
| 2026-04-11 | **Variant A — Dense Bento + Horizontal Scroll Intro fusion (feat/concept-a-fusion):** (1) Nowy komponent `horizontal-intro.tsx` — horizontal scroll intro z gigantycznym "programo" textem, "Software Engineering" subtitle, "Digital Precision" manifesto block, 3-warstwowy parallax (background serif text, main content, foreground ticker), scroll-driven motion z useSpring (damping:40, stiffness:80), mobile-optimized height (200vh mobile / 250vh desktop), GPU-accelerated transforms, `contain: layout style paint` na sticky container. (2) `page.tsx` — dodano HorizontalIntro przed Hero w main content flow. (3) `globals.css` — marquee slow-down: slideLeft 40s→90s, slideRight 50s→110s. Dodano `.tech-card-line::before` pseudo-element animacja (gradient line slides across top on hover). (4) `tech-stack.tsx` — TechCard: dodano hover glow (`hover:shadow-[0_0_30px_rgba(142,182,155,0.1)]`), gradient border effect (`hover:bg-gradient-to-br hover:to-primary/5`), icon rotation on hover (`group-hover:rotate-12`), decorative dot pulse (`group-hover:animate-pulse`), `tech-card-line` class for pseudo-element. Section heading: staggered entrance animations z custom easing, dodano animated gradient separator line. (5) `hero.tsx` — Mobile optimization: dynamic height (150vh mobile / 200vh desktop), ukryto AIScannerCell/QR/Radar na mobile, powiększono logo "pr." na mobile (text-8xl), uproszczono grid na mobile (grid-rows-4). Build: 0 errors. | `src/components/horizontal-intro.tsx` (NEW), `src/app/page.tsx`, `src/app/globals.css`, `src/components/tech-stack.tsx`, `src/components/hero.tsx` |
| 2026-04-11 | **Deep performance fix + large screen optimization (feat/concept-dense-bento):** (1) layout.tsx — usunięto LenisProvider wrapper i noise overlay div, body renderuje bezpośrednio children. (2) preloader.tsx — kompletny rewrite: usunięto words array i word cycling, tylko "PROGRAMO" fade-in, progress 0→100 w 0.3s (interval 15ms, step 5), usunięto 500ms setTimeout delay, exit animation 1s→0.4s. (3) tech-stack.tsx — TechCard: motion.div→plain div z CSS hover transition (hover:-translate-y-2.5), usunięto framer-motion props (initial/whileInView/whileHover), usunięto index prop. Marquee duplication 3x→2x (30 items mniej). Usunięto background blur decoration div. (4) contact.tsx — MagneticWrapper→plain div, usunięto import. Usunięto bottom "CONTACT US" marquee animation. (5) hero.tsx — 2xl breakpoint: pr. text 2xl:text-[12vw], "digital craftsmanship" 2xl:text-7xl, grid cells 2xl:p-10 (9 cells). (6) featured-work.tsx — 2xl breakpoint: "Archiwum" heading 2xl:text-[6rem], project cells 2xl:min-h-[450px], project title 2xl:text-4xl. Build: 0 errors. |  `src/app/layout.tsx`, `src/components/preloader.tsx`, `src/components/tech-stack.tsx`, `src/components/contact.tsx`, `src/components/hero.tsx`, `src/components/featured-work.tsx` |
| 2026-04-11 | **Performance optimization (feat/concept-dense-bento):** Redukcja lag na M2 Mac. (1) hero.tsx — AIScannerCell: dodano 32ms throttle na mousemove (~30fps zamiast każdego eventu), obniżono spring stiffness 500→200. Ambient scanner: zamieniono framer-motion animate loop na CSS animation (animate-scanner-sweep). Bar chart: 6 indywidualnych motion.div animate loops zamienione na CSS animation (animate-bar-pulse). QR pattern: ~13 indywidualnych motion.div opacity animate loops zamienione na CSS animation (animate-qr-blink). (2) featured-work.tsx — AnimatePresence mode="popLayout"→"wait" (eliminuje layout recalculations), usunięto layout="position" z ProjectCell, zmniejszono scale animację 0.8→0.95 i czas 0.5s→0.3s. Zastąpiono backdrop-blur-sm i backdrop-blur-md na solidne bg-[#051F20]/90 i bg-[#051F20]/95 (usunięto warunkowe isMobile checks). (3) globals.css — dodano 3 GPU-accelerated CSS keyframes: scanner-sweep (translate3d), bar-pulse (scaleY), qr-blink (opacity). Build: 0 errors. | `src/components/hero.tsx`, `src/components/featured-work.tsx`, `src/app/globals.css` |
| 2026-03-24 | **Athlix project page overhaul — images + capabilities:** (1) projects.ts — Athlix entry: usunięto 4 nieistniejące PLACEHOLDER screenshoty, zastąpiono 3 prawdziwymi ikonami aplikacji (athlix-trainpilot-icon.png, athlix-trainmate-icon.png, athlix-health-icon.png). Rozbudowane features z 15 do 18 pozycji pogrupowanych tematycznie (Garmin+Strava Sync, AI Coaching, Performance Analytics, Nutrition Engine, BLE HR, WHOOP Reverse-Engineered, 20+ HRV Algorithms, Recovery & Readiness, Strain & TRIMP, Sleep Analytics, Social Discovery, Gym Module, Community, Body Composition, Seasonal Supplements, PWA+Capacitor, Security). longDescription wzbogacone o metryki (839 testów, 142 API, 64 tabel, 1879 LOC algorytmów). description zaktualizowane z konkretnymi liczbami. (2) ProjectDetailClient.tsx — ProjectImage: dodana obsługa icon-based screenshots (isIcon detection), renderowanie centered na radial-gradient tle z dot pattern, rounded shadow box, accent glow. Hero section: nowy ecosystem layout dla icon screenshots — 3 ikony w rzędzie z labelami (TrainPilot/TrainMate/Health), animowane staggered entrance, gradient tło z accentColor. Middle/bottom image sections: automatyczne ukrywanie dla hasIconScreenshots (bo ikony już w hero). Przekazywanie accentColor do wszystkich ProjectImage instancji. Next Project footer: pomijanie icon-based screenshots. (3) projects.test.ts — naprawiono pre-istniejący bug: test oczekiwał athlix status "development" ale dane mają "live". Build: 0 errors, 10/10 static pages. 17/17 projects tests passing. | `src/lib/projects.ts`, `src/app/projects/[slug]/ProjectDetailClient.tsx`, `src/__tests__/projects.test.ts` |
| 2026-03-22 | **Preloader simplification + scroll-reveal animations:** (1) preloader.tsx — kompletny rewrite: usunięto char-by-char stagger animation (~1.9s), zastąpiono prostym opacity 0→1 fade-in (400ms) całego słowa "Programo" + 300ms hold + 500ms curtain slideUp. Total ~1s zamiast ~1.9s. Tekst: font-headline text-3xl font-semibold tracking-tight. Tło: bg-on-surface. Exit: translateY(-100%) z easePageTransition. (2) contact.tsx — scroll-reveal animations: dark card clipPath inset(10% 5% 10% 5% round 32px)→inset(0%) over 0.8s, left column staggered content (label/h2/desc/emails fade+translateY z staggerItem), right column form slides in from x:40 with 0.3s delay, background gradient scales 0.5→1 on scroll. Imports: easeEntry, durationMedium, durationSlow, staggerItem. (3) about.tsx — section-level clipPath reveal inset(3% 2% 0% 2% round 40px)→inset(0%) subtle, stat boxes enhanced with scale(0.9→1) + opacity + translateY stagger (was just opacity+y). All existing animations preserved (word-by-word heading, animated counters, founder stagger, Poznań clip-reveal). Build: 0 errors, 10/10 static pages. | `src/components/preloader.tsx`, `src/components/contact.tsx`, `src/components/about.tsx` |
| 2026-03-22 | **Fix screenshot display across portfolio site:** Naprawiono dimension mismatches powodujace zly wyswietlanie screenshotow. (1) projects.ts — Estalo screenshots: dodano estalo-1.png (1440x900 full-page) jako PIERWSZY, usunieto estalo-2.png (620x420, za maly na hero) i estalo-email-gen.png (zmniejszenie do 6). Baulx bez zmian. Athlix juz nie mial screenshots (512x512 PWA icons nie sa screenshotami). (2) ProjectDetailClient.tsx — Hero screenshot: object-contain→object-cover, max-w-5xl→max-w-6xl, usunieto border border-outline-variant/10. Gallery screenshots: object-contain→object-cover, max-w-5xl→max-w-6xl, usunieto border, space-y-4→flex flex-col gap-6, zachowano rounded-2xl overflow-hidden. Wszystkie animacje zachowane (motion initial/animate/whileInView). (3) featured-work.tsx — juz mial poprawne object-cover, bez zmian potrzebnych. | `src/lib/projects.ts`, `src/app/projects/[slug]/ProjectDetailClient.tsx` |
| 2026-03-22 | **About, Contact & Footer typography/spacing refinement:** Premium portfolio best practices. About: h2 text-5xl/7xl→text-3xl/4xl/5xl + font-semibold + tracking-tight, paragraphs added text-base md:text-lg + font-normal + max-w-xl, founder names text-3xl/4xl→text-2xl/3xl font-medium, founder role labels text-xs→text-[11px] font-medium, stat numbers text-5xl→text-4xl font-semibold, stat labels text-xs→text-[11px] font-medium, stat box padding p-12→p-8 md:p-10, section py-40→py-24/32/40 responsive, grid gap-24→gap-16 lg:gap-20, heading mb-12→mb-8, founders mt-16 pt-12→mt-12 pt-8. Contact: h2 text-5xl/7xl/8xl→text-4xl/5xl/6xl font-semibold, section label text-xs→text-[11px] font-medium, email links font-headline text-xl/2xl→text-lg/xl font-normal, form labels text-xs→text-[11px], submit font-semibold→font-medium, section py-40→py-20/28/32, card padding px-24 py-32→px-16 py-20/24, card rounded-[40px]→rounded-[32px]. Footer: logo text-4xl→text-2xl font-semibold, copyright text-sm→text-xs, availability text-xs→text-[10px] font-medium, footer py-20→py-12/16, gap-16→gap-10, bottom pt-12→pt-8, rounded-t-[40px]→rounded-t-[32px], mt-20→mt-12. All animations preserved. | `src/components/about.tsx`, `src/components/contact.tsx`, `src/components/footer.tsx` |
| 2026-03-22 | **Navbar typography & spacing refinement:** Premium portfolio best practices. Typography: Logo "Programo" text-3xl font-medium -> text-xl font-semibold tracking-tight (too big for nav). Nav links text-sm font-semibold tracking-tight -> text-[13px] font-medium (too heavy, removed tracking-tight). Language toggle text-sm -> text-[13px] font-medium. CTA button text-xs font-semibold tracking-widest -> text-[13px] font-medium tracking-wide, padding px-6 py-3 -> px-5 py-2.5. Mobile logo text-2xl font-bold tracking-tighter -> text-lg font-semibold tracking-tight. Mobile menu links text-3xl -> text-2xl font-normal. Mobile CTA font-semibold -> font-medium. Spacing: Desktop pill px-10 py-4 -> px-8 py-3, nav link gap-12 -> gap-8, right side gap-6 -> gap-4, mobile pill px-6 gap-8 -> px-5 gap-6. All animations preserved (hide/show scroll, layoutId indicator, AnimatePresence mobile menu, clipPath reveal). | `src/components/navbar.tsx` |
| 2026-03-22 | **Featured Work & Tech Stack typography/spacing refinement:** Premium portfolio best practices. Featured Work: section h2 text-5xl/7xl→text-3xl/4xl/5xl + font-semibold + leading-tight + tracking-tight, section label text-xs→text-[11px] font-medium, project category tags font-bold→font-medium, project h3 text-4xl→text-2xl/3xl font-medium, description added explicit font-normal. Spacing: section py-32→py-20/28/32 responsive, header mb-24→mb-16, grid gap-x-12 gap-y-32→gap-x-8 gap-y-20 md:gap-x-10 md:gap-y-24, image mb-8→mb-6, odd card offset md:mt-24→md:mt-16. Tech Stack: section label text-xs font-bold→text-[11px] font-medium, h2 text-5xl/7xl→text-3xl/4xl/5xl + font-semibold + leading-tight + tracking-tight, description added text-base/lg font-normal, tech item name font-bold→font-medium. Spacing: section py-40→py-20/28/32 responsive, header mb-32→mb-16/mb-20, h2 mt-6→mt-4, marquee gap-4→gap-3. All animations preserved (clip-path reveals, directional stagger, marquee, motion imports). | `src/components/featured-work.tsx`, `src/components/tech-stack.tsx` |
| 2026-03-22 | **Hero typography & spacing refinement:** Premium portfolio best practices. Typography: overline label font-bold→font-medium, h1 text-7xl/9xl/10rem→text-5xl/7xl/8xl + font-semibold + tracking-tight + leading-[0.95], description text-xl/2xl→text-base/lg + font-normal + max-w-2xl (was max-w-xl), CTA buttons font-bold→font-medium + tracking-widest→tracking-wide + px-8 py-4→px-6 py-3, "O nas" link font-bold→font-normal. Spacing: section padding refined (px-8 pt-28 pb-32→px-6 pt-24 pb-20 with md/lg breakpoints), added inner max-w-7xl container, mt-8→mt-6 (h1→desc gap), mt-12→mt-10 (desc→CTA gap). All animations preserved (SplitHeading, SplitDescription, parallax, fadeUp, useReducedMotion). | `src/components/hero.tsx` |
| 2026-03-22 | **Fix screenshot rendering in project detail page:** Naprawiono problem z zoomed/stretched screenshots na stronach projektów. Obrazy miały hardcoded `width={1920} height={1080}` ale rzeczywiste screenshoty mają różne wymiary (np. 2268x730, 1440x900), co powodowało zniekształcenia. Zmieniono zarówno hero screenshot jak i gallery screenshots na: kontener `relative aspect-[16/9]` z `<Image fill className="object-contain">` zamiast `width/height`, dodano `bg-surface-container-low` jako tło pod object-contain, `border border-outline-variant/10` jako subtelną ramkę, zmniejszono max-width z `max-w-7xl` na `max-w-5xl`. | `src/app/projects/[slug]/ProjectDetailClient.tsx` |
| 2026-03-22 | **Featured work screenshot images:** Zmieniono sekcję Featured Work — karty projektów z screenshots[] (Estalo, Baulx) teraz wyświetlają pierwszy screenshot jako next/image zamiast gradient placeholder z literą. Import Image z next/image, conditional render: screenshots.length > 0 → Image fill + object-cover + responsive sizes, fallback → dotychczasowy gradient z literą (Athlix, LearnAI). Zachowane wszystkie animacje: clip-path reveal, hover scale(1.05), directional stagger. | `src/components/featured-work.tsx` |
| 2026-03-22 | **Comprehensive projects data update:** Przebudowa danych wszystkich 4 projektow w projects.ts. (1) Estalo — rozszerzone opisy (longDescription z sekcjami AI/Portal/Enterprise/Security), tech stack z 6 do 12 pozycji, features z 8 do 18 (PL+EN), nowe screenshots (6 zamiast 2), tagi "Marketplace"→"Enterprise". (2) Baulx — subtitle skrocony, description+longDescription zaktualizowane (nowe szczegoly: 103 moduly JS, 1909 testow, konkretne post-procesory CNC), tech stack zaktualizowany (dodano Supabase, Anthropic Claude), features z 12 do 14 (dodano metryki: testy, moduly, WCAG), tagi "SaaS"→"3D". (3) Athlix — rozszerzony z 2 do 3 aplikacji (TrainPilot+TrainMate+Health), subtitle/description/longDescription kompletnie przebudowane z pelnym opisem kazdej apki, tech stack z 6 do 13 pozycji (dodano Pusher, Strava API, Garmin, Google Maps, Recharts etc), features z 8 do 15 (PL+EN), tagi "Social/Analytics"→"Health/AI", year "2026"→"2025-2026". (4) LearnAI — tech stack: "AI"→"Azure OpenAI". TypeScript compiles: 0 errors. | `src/lib/projects.ts` |
| 2026-03-22 | **Footer parallax + mobile menu clipPath reveal:** (1) footer.tsx — dodano scroll-linked parallax: useRef + useScroll({ target, offset: ["start end","end end"] }) + useTransform mapuje scrollYProgress na y:[60,0], wewnetrzny content laguje za scrollem tworzac efekt "reveal/emerging". Bottom row (copyright + availability) uzywa fadeInUp variant z whileInView. Import: useRef, motion, useScroll, useTransform, easeEntry, durationMedium, fadeInUp z @/lib/motion. overflow-hidden na footer. (2) navbar.tsx — mobile menu overlay zmieniony z prostego opacity fade na clipPath circle reveal: "circle(0% at 90% 5%)" -> "circle(150% at 90% 5%)" (rozszerza sie z top-right gdzie jest hamburger button), duration 0.6s z easeEntry, exit: reverse clip-path. Link stagger zwiekszone z 0.08s do 0.1s, dodano x:-30 initial state (slide from left) obok istniejacego y:20 dla bardziej dramatycznego wejscia. Dodano easeExit import. Exit animations na linkach. (3) ProjectDetailClient.tsx — naprawiono Turbopack parsing error: em dash character "—" zamieniony na "&mdash;" HTML entity (2 wystapienia w prev/next nav). Build: 0 errors, 10/10 static pages. | `src/components/footer.tsx`, `src/components/navbar.tsx`, `src/app/projects/[slug]/ProjectDetailClient.tsx` |
| 2026-03-22 | **Marquee ticker tech stack + hover underline animations:** (1) tech-stack.tsx — kompletny rewrite: statyczny grid zastąpiony dwurzędowym continuous horizontal scrolling marquee. Dwa rzędy (8+7 technologii) scrollują w przeciwnych kierunkach (lewo/prawo). Framer-motion `useAnimationControls` z `repeat: Infinity`, `repeatType: "loop"`, `ease: "linear"`, duration 30s. Items triplicated (3x) dla seamless loop. Pause on hover via `controls.stop()` / `controls.start()`. Pill/card styling: rounded-xl, border, bg-surface-container-lowest. Section header zachowany z whileInView animations (fadeInUp, clipRevealUp z @/lib/motion). (2) globals.css — dodano `.hover-underline` CSS class z pseudo-element ::after animated underline: scaleX(0)->scaleX(1) na hover, transform-origin flip (right->left) dla directional slide effect, cubic-bezier(0.16,1,0.3,1) easing. (3) footer.tsx — GitHub i LinkedIn links zmienione z Tailwind `hover:underline` na custom `.hover-underline` class dla premium animated underline effect. (4) Bonus fixes: naprawiono pre-existing TypeScript cast w preloader.tsx (number[] -> [number,number,number,number]) i pre-existing JSX parse error w ProjectDetailClient.tsx (indentation fix). Build: 0 errors, 10/10 static pages. | `src/components/tech-stack.tsx`, `src/app/globals.css`, `src/components/footer.tsx`, `src/components/preloader.tsx`, `src/app/projects/[slug]/ProjectDetailClient.tsx` |
| 2026-03-22 | **Branded preloader & page transitions:** (1) Nowy komponent preloader.tsx — full-viewport ciemna kurtyna (bg-on-surface, z-[9998]) z animacją tekstu "Programo" char-by-char (translateY(100%)->0, stagger 0.04s, easeEntry), hold 0.3s, potem kurtyna slideUp (-100%) z easePageTransition. AnimatePresence mode="wait" do zarządzania exit animation. useState+useEffect timer (~1.9s). SSR-safe. (2) Nowy komponent page-transition.tsx — wrapper z framer-motion motion.div: opacity 0->1, y 20->0, duration 0.6s, easeEntry. (3) Integracja w page.tsx: Preloader dodany na top-level wewnątrz I18nProvider, cały HomeContent (Navbar+main+Footer) owinięty w PageTransition. (4) Integracja w ProjectDetailClient.tsx: content owinięty w PageTransition dla spójnej animacji wejścia na podstronach projektów. Import easeEntry/easePageTransition/durationMedium z @/lib/motion. Build: 0 errors, 10/10 static pages. | `src/components/preloader.tsx` (nowy), `src/components/page-transition.tsx` (nowy), `src/app/page.tsx`, `src/app/projects/[slug]/ProjectDetailClient.tsx` |
| 2026-03-22 | **Magnetic button wrapper:** Nowy komponent MagneticWrapper (magnetic.tsx) — opakowuje dowolny element i dodaje efekt magnetycznego przyciągania kursora. Używa framer-motion useMotionValue + useSpring z springMagnetic config (stiffness 150, damping 15, mass 0.27). Na onMouseMove oblicza offset od centrum elementu i przesuwa go max 8px w stronę kursora. Na onMouseLeave springuje z powrotem do (0,0). Props: children + strength (domyślnie 0.3 — mnożnik siły). Zintegrowany w: (1) navbar.tsx — CTA button "Napisz do nas" owinięty w MagneticWrapper. (2) hero.tsx — "Zobacz projekty" border button owinięty w MagneticWrapper. Dodatkowo naprawiono pre-existing TypeScript errors w page-transition.tsx i preloader.tsx (easeEntry/easePageTransition cast as unknown as number[] → as [number, number, number, number]). Build: 0 errors. | `src/components/magnetic.tsx` (nowy), `src/components/navbar.tsx`, `src/components/hero.tsx`, `src/components/page-transition.tsx`, `src/components/preloader.tsx` |
| 2026-03-22 | **Custom cursor component:** Dodano premium custom cursor z framer-motion. (1) Nowy komponent custom-cursor.tsx — 20px circle podazajacy za myszka z spring physics (springCursor: stiffness 150, damping 15, mass 0.1). (2) mix-blend-mode: difference dla kontrastu na kazdym tle. (3) Trzy warianty: default (20px), pointer (40px, na hover a/button), view (60px z tekstem "View", na elementach data-cursor="view"). (4) Detekcja interactive elements przez data-cursor attribute + auto-detection a/button z MutationObserver dla dynamicznych elementow. (5) Ukryty na mobile — window.matchMedia('(pointer: coarse)') check. (6) SSR-safe — useState(true) dla isTouchDevice, useEffect do detekcji. (7) AnimatePresence dla "View" label appear/disappear. (8) Zintegrowany w page.tsx wewnatrz I18nProvider ale poza HomeContent. (9) globals.css: @media (pointer: fine) { * { cursor: none !important; } } ukrywa domyslny kursor na non-touch devices. Build: 0 errors. | `src/components/custom-cursor.tsx` (nowy), `src/app/page.tsx`, `src/app/globals.css` |
| 2026-03-22 | **Premium animations (About section):** Dodano zaawansowane animacje do sekcji About. (1) AnimatedCounter component -- inline helper z useMotionValue + useTransform + animate, liczby "4+" (0->4), "2" (0->2), "2024" (2020->2024) animuja sie po wejsciu w viewport, easing [0.16,1,0.3,1], duration 1.5s. (2) "Poznan" clip-path reveal -- tekst pojawia sie animacja clipPath inset(0 100% 0 0) -> inset(0 0% 0 0). (3) Staggered stat boxes -- 4 boxy fade-in + slide-up z 0.1s stagger (custom index 0-3, prawa kolumna offset +2). (4) Word-by-word heading reveal -- naglowek rozdzielony na slowa, kazde z opacity+y+blur animacja, stagger 0.06s. (5) Paragraphs fade-in z translateY, staggered delays. (6) Founders stagger -- wjezdzaja z founderVariants (staggerItem delay). Import z @/lib/motion: easeEntry, durationMedium, durationSlow, staggerItem, fadeInUp. viewport={{ once: true }}. Build: 0 TS errors. | `src/components/about.tsx` |
| 2026-03-22 | **Premium navbar animations:** (1) Hide-on-scroll-down / show-on-scroll-up z requestAnimationFrame scroll tracking, delta thresholds (8px down / 5px up) i always-visible w top 100px. (2) Animated active section indicator z framer-motion `layoutId="nav-indicator"` — 2px primary underline smoothly morphs/slides between active nav links via springGentle. (3) Enhanced backdrop blur/shadow — scrolled state: bg-white/80, backdrop-blur-2xl, stronger shadow. (4) All transitions use shared motion constants (easeEntry, easeHover, durationFast, durationMedium, springGentle) z `@/lib/motion`. (5) Mobile nav also hides/shows on scroll (disabled when menu open). Build: 0 errors. | `src/components/navbar.tsx` |
| 2026-03-22 | **Premium animations (Hero):** Dodano zaawansowane animacje do sekcji Hero. (1) Split-text char-by-char reveal — nagłówek "Programo" rozbity na pojedyncze `<span>` znaki, każdy w overflow-hidden kontenerze, animacja translateY(100%) → 0 ze stagger 0.03s i easeEntry [0.16,1,0.3,1]. (2) Word-by-word reveal — opis (z i18n `t("hero.desc")`) dzielony na słowa, każde słowo fade+translateY ze stagger 0.02s i easeDefault. (3) Parallax on scroll — useScroll+useTransform: h1 przesuwa się -80px, opis -40px, scroll indicator +30px z fade-out. (4) useReducedMotion() — pełne wsparcie prefers-reduced-motion: przy reduced motion plain fade-in bez parallax/split. (5) Import easing/stagger/duration z @/lib/motion (easeEntry, easeDefault, staggerChar, staggerWord, durationMedium, durationSlow). Build: 0 errors TS. | `src/components/hero.tsx` |
| 2026-03-22 | **Premium animations (Featured Work):** Dodano zaawansowane animacje do sekcji Featured Work. (1) Clip-path image reveal on scroll — obrazy kart pojawiają się animacją clipPath inset od dołu do góry z easeEntry [0.16,1,0.3,1] i duration 0.9s. (2) Directional stagger — karty o parzystym indeksie (0,2) wjeżdżają z lewej (x:-60), nieparzyste (1,3) z prawej (x:60). (3) Section title clip-path reveal — nagłówek "Nasze realizacje" używa clipRevealUp variant z @/lib/motion. (4) Hover enhancements — gradient placeholder skaluje się do 1.05 na hover (overflow-hidden na kontenerze), subtle overlay z dużą literą inicjałową projektu. (5) Import easing/duration/stagger z @/lib/motion (easeEntry, easeDefault, durationSlow, durationMedium, staggerItem, clipRevealUp). viewport={{ once: true, margin: "-100px" }}. Build: 0 errors. | `src/components/featured-work.tsx` |
| 2026-03-22 | **Visual redesign:** Kompletna zmiana wyglądu strony z ciemnego sage/beige motywu na jasny, ciepły editorial design (amber/cream). (1) Fonty: Inter + Playfair Display → Newsreader + Plus Jakarta Sans. (2) Kolory: nowa paleta Material Design 3 — surface #f9f9f9, primary #775a19 (amber), on-surface #1a1c1c, outline-variant #d1c5b4. (3) Navbar: floating pill nav z rounded-full, backdrop-blur-xl, glass effect. (4) Hero: duża editorial typografia (7-10rem), grid layout 12-kolumnowy. (5) Featured Work: staggered grid (2nd/4th offset), editorial category labels, placeholder image cards. (6) About: rounded-t-[40px] container, stat boxy z editorial-shadow, sekcja founders z role labels. (7) Tech Stack: centered grid z gap-px separator, hover effects. (8) Contact: dark inverted section (bg-on-surface, rounded-[40px]), two-column layout (info + form). (9) Footer: light bg, rounded-t-[40px], social links, green availability indicator. (10) Project detail + 404: zaktualizowane do nowej palety kolorów. (11) CLAUDE.md zaktualizowany. Build: 0 errors. | `globals.css`, `layout.tsx`, `navbar.tsx`, `hero.tsx`, `featured-work.tsx`, `about.tsx`, `tech-stack.tsx`, `contact.tsx`, `footer.tsx`, `not-found.tsx`, `ProjectDetailClient.tsx`, `CLAUDE.md` |
| 2026-03-19 | **RUNDA 4 (pełna reweryfikacja):** Ponowny full audit PROMPT.md — naprawiono brakujące elementy z poprzednich rund. (1) Featured Work: dodano "Live" green badge (bg-emerald-400) dla live projektów, layout zmieniony z 1-kolumnowego na grid 2x2 (grid-cols-1 md:grid-cols-2), hover scale(1.02), mniejsze padding/font w karcie. (2) Navbar: dodano active section detection (IntersectionObserver na #work/#about/#stack/#contact), podświetlenie aktywnej sekcji. (3) WCAG AA contrast fix: sage-muted zmieniony z #7a8f7c (3.13:1) na #4e6752 (5.60:1) — teraz spełnia 4.5:1 na obu tłach. (4) i18n: dodano klucz "work.live" (Na żywo/Live). (5) API: dodano console.log fallback gdy brak RESEND_API_KEY. (6) Testy 102 → 117: +2 i18n (toggle, nested keys), +3 hero (lang toggle, flex-col, animations), +2 featured-work (live green badge, grid layout), +1 navbar (scroll bg), +2 contact-form (success reset, i18n toggle), +1 footer (i18n EN), +1 contact API (rate limit reset), +3 SEO (JSON-LD Organization, SoftwareApplication, canonical URLs). Build: 0 errors, 0 warnings, all pages ○ Static. Lint: 0 errors/warnings. Tests: 117/117 passing. | `src/components/featured-work.tsx`, `src/components/navbar.tsx`, `src/lib/i18n.tsx`, `src/app/globals.css`, `src/app/api/contact/route.ts`, `src/__tests__/i18n.test.ts`, `src/__tests__/components/hero.test.tsx`, `src/__tests__/components/featured-work.test.tsx`, `src/__tests__/components/navbar.test.tsx`, `src/__tests__/components/contact-form.test.tsx`, `src/__tests__/components/footer.test.tsx`, `src/__tests__/api/contact.test.ts`, `src/__tests__/seo.test.ts` |
| 2026-03-19 | **RUNDA 4 kontynuacja:** Full Production Audit wg PROMPT.md — pełna weryfikacja 78+ checklisty. Rozszerzono testy z 93 → 102: hero (+1: scroll indicator), featured-work (+2: status badge colors, article elements), navbar (+2: logo href, mobile menu open), contact-form (+2: max length validation, subject select options count), footer (+2: copyright text, semantic footer element). Audyt page-by-page: Hero, Featured Work, About, Tech Stack, Contact, Footer, Navbar, 4× Project Detail, 404, API, SEO, i18n, Accessibility — wszystko zgodne z checklistą. Build: 0 errors, 0 warnings, all pages ○ Static. Lint: 0 errors/warnings. Tests: 102/102 passing. | `src/__tests__/components/hero.test.tsx`, `src/__tests__/components/featured-work.test.tsx`, `src/__tests__/components/navbar.test.tsx`, `src/__tests__/components/contact-form.test.tsx`, `src/__tests__/components/footer.test.tsx` |
| 2026-03-18 | **RUNDA 4:** Full Production Audit. (1) Custom 404 page z i18n PL/EN — styled consistent z resztą strony. (2) Hero: mobile max 80svh cap (widać content poniżej) + vertical button stack na mobile (flex-col). (3) About: stats "3+" → "4+" (4 produkty), fix bug "Bartosz Kołaj" → "Bartosz Kolaj" (złe HTML entity), dodano placeholder avatary inicjałów founderów (WP, BK). (4) Featured Work: stagger animation z whileInView (0.1s delay między kartami), IntersectionObserver. (5) Contact: dodano kontakt@programo.pl jako główny email + social links (GitHub, LinkedIn) w sekcji kontakt. (6) Screenshot alt text: bardziej opisowy (tytuł + subtitle + zrzut ekranu PL/EN). (7) ProjectDetailClient: "Project not found" → i18n t("notFound.title"). (8) Testy: 70 → 93 (nowe: about.test.tsx ×6, tech-stack.test.tsx ×5; rozszerzono: i18n +2, projects +4, contact API +4, SEO +2). IntersectionObserver mock w test setup. (9) Build: 0 errors, 0 warnings, all pages ○ Static. Lint: 0 errors/warnings. Tests: 93/93 passing. | `src/app/not-found.tsx` (nowy), `src/components/hero.tsx`, `src/components/about.tsx`, `src/components/featured-work.tsx`, `src/components/contact.tsx`, `src/app/projects/[slug]/ProjectDetailClient.tsx`, `src/__tests__/setup.ts`, `src/__tests__/components/about.test.tsx` (nowy), `src/__tests__/components/tech-stack.test.tsx` (nowy), `src/__tests__/i18n.test.ts`, `src/__tests__/projects.test.ts`, `src/__tests__/api/contact.test.ts`, `src/__tests__/seo.test.ts` |
| 2026-03-18 | **RUNDA 3:** (1) Contact form z Zod walidacją, rate limiting, Resend API, inline errors, toast, i18n PL/EN. (2) SEO: robots.ts, sitemap.ts (5 URLs), JSON-LD Organization w layout + SoftwareApplication na project pages, OG/Twitter meta, canonical URLs. (3) 70 testów vitest (i18n, projects, contact API, hero, featured-work, navbar, contact-form, footer, SEO). (4) Accessibility: skip-to-content, role=navigation, aria-labels, focus-visible, html lang dynamic. (5) i18n: 30+ nowych kluczy (contact form, 404, a11y), localStorage persistence, html lang toggle. (6) Performance: font latin-ext subset, prefers-reduced-motion CSS. (7) Tech stack rozszerzony z 6 do 15 technologii. (8) Footer: social links (GitHub, LinkedIn), copyright 2024–YYYY. (9) About: rok założenia 2024. | `src/app/api/contact/route.ts`, `src/app/robots.ts`, `src/app/sitemap.ts`, `src/app/layout.tsx`, `src/app/page.tsx`, `src/app/projects/[slug]/page.tsx`, `src/app/projects/[slug]/ProjectDetailClient.tsx`, `src/lib/i18n.tsx`, `src/components/contact.tsx`, `src/components/footer.tsx`, `src/components/navbar.tsx`, `src/components/tech-stack.tsx`, `src/components/about.tsx`, `src/app/globals.css`, `vitest.config.ts`, `src/__tests__/**` |
| 2026-03-18 | Task 8: Zamiana 2x `<img>` na Next.js `<Image />` w ProjectDetailClient.tsx (hero + gallery screenshots) z width/height/priority. Task 9: Aktualizacja danych projektów w projects.ts — Athlix status→development, tech→Next.js+Capacitor+Clerk+Neon+TimescaleDB; Baulx subtitle/description/longDescription/features zaktualizowane do modelu 10 produktów (JetBrains) | `src/app/projects/[slug]/ProjectDetailClient.tsx`, `src/lib/projects.ts` |
| 2026-03-17 | Inicjalizacja pliku progress.md | — |
