"use client";

// V5 Dark Editorial: Three.js particles replaced with CSS gradient orbs
// This component is now a no-op — gradient orbs are rendered directly in hero.tsx
export default function HeroBackground3D() {
  return null;
}
