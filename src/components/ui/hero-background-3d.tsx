"use client";

/**
 * This component was originally a Three.js particle system.
 * Replaced with a CSS grid pattern for the Bento SaaS redesign.
 * The grid pattern is now applied via CSS classes in the Hero component.
 * This file is kept as a no-op export to avoid breaking any remaining imports.
 */
export default function HeroBackground3D() {
  return null;
}
